# Журнал заседаний борда C-1

Последние 8 заседаний. Новые — сверху. Полный дек каждого заседания — в `compas-ops/board/` и в Telegram; здесь остаётся протокол.
Протокол заседания: повестка, три числа, принятые решения (ID из `12_DECISIONS`), эскалации, что вытеснено из плана, состав присутствовавших ролей и кворум.

---

## N5 · понедельник 31.08.2026, 10:00 МСК

**Кворум.** CEO (ведёт), COO, CPO, CMO, CTO, CFO, Senior Legal, главный бухгалтер, InfoSec, CDO — десять ролей, все дали ответ (CDO обязателен: измеримость и tracking plan ПРАКТИКИ в повестке; Legal/InfoSec обязательны: активный инцидент SSH-ключа, промостраница, новая находка о постороннем доступе к compas-ops; CFO/бухгалтер обязательны: незакрытый договор эквайринга в повестке).

**Распоряжения учредителя.** `state/15_ORDERS.md` прочитан целиком — ни одно открытое распоряжение не адресовано БОРДу.

**Повестка.** Первым пунктом — новая находка ведущего CEO-агента перед началом заседания: в полной (не shallow) истории `main` самого compas-ops найдена цепочка минимум из 24 прямых коммитов постороннего происхождения, не относящихся к СИМПАС. Вторым — уже стоявшая в повестке (`D-260831-01`) вторая подряд полностью тихая ночь во всех трёх продуктах. Далее — контроль исполнения N4, три числа, круг ролей, портфельный приоритет, срочные вмешательства, спорные вопросы, `/human` очередь.

**Главная находка заседания.** Вершина `main` compas-ops (`59a922d`, сегодня 14:04:46 UTC, автор `Claude <noreply@anthropic.com>`, сообщение «fix: espeak-ng замедлить+нормализовать громкость») добавляет разом все 146 файлов СИМПАС — в её родителе их не было. Между этим коммитом и последним легитимным (`1f1fad3`, «Ночной цикл 2026-08-31») — минимум 23 прямых коммита подряд (30.08 19:40 — 31.08 14:04 UTC, без единого мёржа, тот же автор), ни один не имеет отношения к СИМПАС: сообщения про посторонний проект («Hermes», «Knowledge-бот», `gigaam-benchmark`, деплой секретов и вебхуков на чужой сервер). Это не products/ — это единственный источник правды самой компании (устав §5.3). Находка переоткрывает диагноз `B-260823-01`: посторонний процесс с доступом к тому же токену/раннеру — более правдоподобное объяснение неаудируемых коммитов в products/, чем сломанное расписание `implement.yml`, которым это объясняли четыре цикла подряд.

**Контроль исполнения решений N4 (`D-260827-04..08`, доклад COO).** 04 (область `B-260824-03` расширена на `deploy-moments.yml`) — зафиксировано, барьер не построен. 05 (`B-260813-10`: авторство → CPO) — документ написан 28.08, но приёмка CTO не зафиксирована четвёртый цикл подряд — эскалировано отдельно (`D-260831-06`). 06 (`H-260816-01`: снять страницу, без правок) — не исполнено, страница и расхождение цены на месте. 07 (личность «Eliah Martynov» — вопрос учредителю) — передано в `H-260818-01`, ответа нет. 08 (расследовать источник через `gh run list`/аудит-лог) — не выполнено: у CTO в этой среде по-прежнему нет `gh`-доступа (`PRODUCTS_READ_TOKEN` не задан, `gh auth status` не авторизован) — пятый цикл подряд то же самое ограничение, теперь дополненное куда более серьёзной находкой той же природы.

**Три числа.** Деньги: данных нет, `H-260813-03` — девятнадцатые сутки тишины; `H-260824-01` (эквайринг Т-Банка) — седьмой день просрочки, платежи ПРАКТИКИ идут боевым образом без подтверждённого договора. Продукт: 0 из ~39 порогов `05_METRICS §4` считаются по SQL (CDO); ни одна задача в бэклоге не двигает North Star — CPO подтвердил прямым ответом на обязательный вопрос; МОМЕНТЫ — продукт, который третий раз подряд голодает (открытый бэклог для него пуст). Доверие: барьер «агент не пишет в main без PR» не работает ни на одном из трёх продуктов пятнадцатый+ цикл подряд, и теперь то же самое подтверждено для `main` самого compas-ops — доверие к git-истории как источнику фактов подорвано на уровне, которого не было ни на одном из предыдущих четырёх заседаний.

**Решения заседания (полный текст — `state/12_DECISIONS.md`):**
- `D-260831-04` — найдена чужая история коммитов в `main` compas-ops, заведены `B-260831-01`/`H-260831-01`, диагноз `B-260823-01` переоткрыт.
- `D-260831-05` — портфельный приоритет N5: находка о compas-ops встаёт первым пунктом, остальной порядок (токен/ключ → промостраница → tracking plan) сохраняется.
- `D-260831-06` — `B-260813-10`: четвёртый цикл подряд без явного ответа CTO, эскалировано отдельной строкой, срок — 01.09.2026.

**Вето, подтверждённые без изменений.** CTO: стоп-кран на любой релиз всех трёх `products/` до включения branch protection. InfoSec: запрет новых доступов и продуктовых фич до того же условия. Legal: запрет любой правки текста/цены промостраницы ЗАПИСОК — разрешено только снятие.

**Эскалации ⚠️.** Новая находка о постороннем доступе к `main` compas-ops — первой строкой, до сводки. `H-260818-01`/`H-260818-02` не закрыты пятнадцатый+ цикл подряд. `H-260813-03` — девятнадцатые сутки тишины данных. Промостраница ЗАПИСОК — решение борда не исполнено пятый раз подряд (`H-260816-01`). `H-260824-01` — седьмой день просрочки, боевые деньги без подтверждённого договора.

**Портфельный приоритет на 3–4 дня (защитил CPO).** Порядок: (1) объяснить/закрыть доступ постороннего процесса к compas-ops (`H-260831-01`); (2) тем же расследованием закрыть `B-260823-01`/`H-260818-01`/`H-260818-02`, если источник общий; (3) снять промостраницу ЗАПИСОК ровно одним действием (`H-260816-01`); (4) CTO даёт явный ответ по tracking plan ПРАКТИКИ (`B-260813-10`); (5) `H-260813-03`/`H-260824-01` идут тем же темпом параллельно.

**Что вытеснено.** Custdev ПРАКТИКИ, биллинг-трекинг Т-Банк — той же причиной, что и четыре предыдущих борда: безопасность, целостность и процесс исполнения бьют всё по уставу §9.

**Спорные вопросы.** Ни одного разногласия между ролями — все десять сошлись на приоритете новой находки и на неизменности прежних вето.

**`/human` очередь на выходе (8/5, обоснованное превышение — устав §6.2):** `H-260831-01` (новая — чужая история в compas-ops) → `H-260818-02` (SSH-ключ) → `H-260813-03` (доступ к биллингу) → `H-260818-01` (токен + branch protection) → `H-260816-01` (снять промостраницу) → `H-260813-05` (снять обещание в письме/экране входа) → `H-260813-04` (публичность репозиториев) → `H-260824-01` (договор эквайринга Т-Банка).

**Заседание состоялось** — три решения принято, ни одно не отложено без даты.

**Доставка.** Эскалация ⚠️ (чужая история в compas-ops, SSH-ключ и токен пятнадцатый+ цикл, промостраница нарушена пятый раз, девятнадцатые сутки тишины данных) уходит отдельным сообщением первой, до сводки — `outbox/2026-08-31_board_escalation.txt`.

---

## N4 · четверг 27.08.2026, 10:00 МСК

**Кворум.** CEO (ведёт), COO, CPO, CMO, CTO, CFO, Senior Legal, главный бухгалтер, InfoSec, CDO — десять ролей, все дали ответ (CDO обязателен: измеримость и tracking plan ПРАКТИКИ в повестке; Legal/InfoSec обязательны: активный инцидент промостраницы и барьер безопасности; CFO/бухгалтер обязательны: незакрытый договор эквайринга в повестке).

**Распоряжения учредителя.** `state/15_ORDERS.md` прочитан целиком — ни одно открытое распоряжение не адресовано БОРДу.

**Повестка.** Первым пунктом — пересмотр диагноза `B-260823-01`: трек «реализация» произвёл 21 коммит за ночь 26.08 неаудируемым механизмом. Далее — контроль исполнения N3, три числа, круг ролей, портфельный приоритет, срочные вмешательства, спорные вопросы, `/human` очередь.

**Контроль исполнения решений N3 (`D-260824-03..10`, доклад CEO).** 03 (D0 МОМЕНТОВ) — выполнено полностью. 04 (tracking plan ПРАКТИКИ, чек-лист+срок 25.08) — просрочено, пятый сорванный срок; переназначено CPO этим заседанием (`D-260827-05`). 05 (ретровиза моста аналитики) — выполнено. 06 (промпт implement читает бэклог) — выполнено лишь частично, корневая причина (расписание/источник коммитов) не устранена и оказалась серьёзнее, чем считалось. 07 (реестр событий) — выполнено и подтверждено. 08 (усиление H-260818-01 новыми находками) — зафиксировано, сама угроза не снята. 09 (снять промостраницу, не чинить) — не исполнено, а нарушено содержательно (`D-260827-03`, подтверждено повторно этим заседанием). 10 (эквайринг Т-Банка формальной задачей) — просрочено, третьи сутки.

**Три числа.** Деньги: данных нет, `H-260813-03` — пятнадцатые сутки тишины; платежи ПРАКТИКИ идут боевым образом без подтверждённого договора эквайринга третий день (CFO/бухгалтер). Продукт: 0 из ~39 порогов `05_METRICS §4` считаются по SQL (CDO); шестнадцатый цикл подряд без задачи, двигающей North Star (CPO). Доверие: барьер «агент не пишет в main без PR» не работает ни на одном из трёх продуктов десятый цикл подряд; CTO живой проверкой подтвердил, что в `zapiski` **все** последние 30 коммитов — прямые пуши без единого мёржа, не только часть.

**Главная находка заседания.** CEO-агент проверил `log/runner/` в `compas-ops`: implement-логи существуют только за 17–18.08. Шаг записи лога в `implement.yml` безусловен (`if: always()`) — штатный прогон не мог не оставить след. Значит 20 коммитов 23.08 и 21 коммит 26.08 произведены не этим воркфлоу, а неизвестным каналом с доступом к тому же `PRODUCTS_READ_TOKEN`. Диагноз `B-260823-01`/`D-260827-02` ужесточён этим заседанием (`D-260827-08`): вопрос больше не «почему промпт не читает бэклог», а «кто вообще пишет в products/ помимо этого воркфлоу» — расследование переходит на историю запусков GitHub Actions и аудит-лог организации, недоступные этой сессии.

**Решения заседания (полный текст — `state/12_DECISIONS.md`):**
- `D-260827-04` — найдена третья статическая страница (МОМЕНТЫ, 399 ₽/мес) под тем же автодеплоем без ревью, область `B-260824-03` расширена.
- `D-260827-05` — `B-260813-10`: пятый сорванный срок PO-ПРАКТИКА, авторство документа переходит CPO, шестой срок тому же владельцу не назначается.
- `D-260827-06` — `H-260816-01` переведена в статус активного инцидента: следующее исполнение обязано только снять страницу, правки текста запрещены.
- `D-260827-07` — личность «Eliah Martynov»: та же почта, что у мёржей `compas-psy`; вопрос закрывается прямым обращением к учредителю, не расследованием, присоединён к `H-260818-01` без нового слота очереди.
- `D-260827-08` — `log/runner/` не содержит ни одного implement-лога после 18.08: источник 21 коммита 26.08 — не `implement.yml` в штатном виде; расследование переносится на аудит-лог GitHub.

**Эскалации ⚠️.** Барьер агент/продакшен не работает десятый цикл подряд, и источник обхода теперь доказанно не тот воркфлоу, на который грешили раньше — сама природа риска ещё не понята. `H-260818-01`/`H-260818-02` просрочены на восьмые/девятые сутки. `H-260813-03` — пятнадцатые сутки тишины данных. Промостраница ЗАПИСОК — решение борда нарушено содержательно второй раз подряд. `H-260824-01` — третий день просрочки, боевые деньги без подтверждённого договора.

**Портфельный приоритет на 3–4 дня (защитил CPO).** Порядок: расследовать фактический источник прямых пушей через GitHub audit log (не products/, не implement.yml — вопрос шире, чем считалось) → закрыть `H-260818-01`/`H-260818-02`/`H-260813-03`/`H-260824-01` → снять промостраницу ЗАПИСОК ровно одним действием → CPO пишет tracking plan ПРАКТИКИ → `B-260822-01`/расширенный `B-260824-03` как управленческая гигиена.

**Что вытеснено.** Custdev ПРАКТИКИ, выкат биллинг-трекинга Т-Банк — шестнадцатый учётный цикл подряд той же причиной: устав §9.

**Спорные вопросы.** Ни одного разногласия между ролями — все десять сошлись на приоритете находки об источнике прямых пушей и на ужесточении рекомендации по промостранице до «только снять».

**`/human` очередь на выходе (7/5, обоснованное превышение — устав §6.2):** `H-260818-02` (SSH-ключ) → `H-260813-03` (доступ к биллингу) → `H-260818-01` (токен + branch protection, усилена находками этого заседания) → `H-260816-01` (снять промостраницу, теперь инцидент) → `H-260813-05` (снять обещание в письме/экране входа) → `H-260813-04` (публичность репозиториев) → `H-260824-01` (договор эквайринга Т-Банка).

**Заседание состоялось** — пять решений принято, ни одно не отложено без даты.

**Доставка.** Эскалация ⚠️ (ужесточённый диагноз об источнике прямых пушей, промостраница-инцидент, токен/ключ десятый цикл, пятнадцатые сутки тишины данных) уходит отдельным сообщением первой, до сводки — `outbox/2026-08-27_board_escalation.txt`.

---

## N3 · понедельник 24.08.2026, 10:00 МСК

**Кворум.** CEO (ведёт), COO, CPO, CMO, CTO, CFO, Senior Legal, главный бухгалтер, InfoSec, CDO — десять ролей, все дали ответ (CDO обязателен: приёмка двух tracking plan и подтверждение аналитического реестра в повестке; Legal/InfoSec обязательны: активные инциденты безопасности и промостраница с юридическим риском; CFO/бухгалтер обязательны: незакрытая находка про договор эквайринга в повестке).

**Распоряжения учредителя.** `state/15_ORDERS.md` прочитан целиком — ни одно открытое распоряжение не адресовано БОРДу; единственное распоряжение с адресатом БОРД (`O-260817-04`) закрыто ещё на N2. Повестка формируется целиком ночным циклом 24.08 и находками ролей.

**Повестка.** 1) Контроль исполнения решений N2. 2) Три числа. 3) Круг ролей. 4) Портфельный приоритет на 3–4 дня. 5) Срочные вмешательства в бэклог. 6) Спорные вопросы. 7) `/human` очередь. Первым пунктом — новая находка ночного цикла: агент под личностью `Claude` пишет напрямую в `main` `cmpas.ru`, в обход PR (`B-260824-01`).

**Контроль исполнения решений N2 (`D-260820-04..09`, доклад COO).** 04 (переформулировка + PR по промостранице) — переформулировка сделана, PR не появился, находка расширилась: код цены сменился ещё раз, страница нестабильна как цель. 05 (PR #91 не закрывать до branch protection) — стоп-кран держится корректно, protection не включена. 06 (H-260818-01/02 единый приоритет, эскалация к 21.08) — сработало, седьмой цикл подряд не закрыто, найдено отягчающее (агент, не только бот, пишет в main; токен лежит в `.git/config` открытым текстом; `deploy-docker.yml` деплоит на любой push). 07 (откат цены ЗАПИСОК к 21.08) — сработало, откат назначен, четвёртый пропущенный срок — трек «реализация» не читал бэклог. 08 (реестр событий, срок 20.08) — сделано с опозданием 24.08, тесты не были подтверждены собственным прогоном. 09 (сборка PDF, P1) — сделано, устойчиво четвёртые сутки. Итог: 1 полностью выполнено, 1 идёт по плану (стоп-кран), 1 выполнено поздно (теперь подтверждено полностью — см. решения), 3 просрочены.

**Три числа.** Деньги: данных нет, `H-260813-03` — двенадцатые сутки тишины; отдельно CFO и бухгалтер подтвердили, что боевые платежи ПРАКТИКИ через Т-Банк идут без подтверждённого договора эквайринга (находка с N2, переведена в формальную задачу). Продукт: 0 метрик дерева `05_METRICS §1.3` посчитаны по SQL (CDO); двенадцатый цикл подряд без задачи, двигающей North Star (прямой ответ CPO на обязательный вопрос). Доверие: барьер «агенты не мержат/не пушат в main» не работает ни на одном из трёх уровней одновременно (ревью, прямой пуш, автодеплой на push) — подтверждено независимо CTO и InfoSec живым `git log` и проверкой `deploy-docker.yml`.

**Главная находка заседания.** Три независимых технических барьера между действием агента и продакшеном (PR-ревью, «нет прямых пушей», «нет автодеплоя без ревью») не работают одновременно на одном и том же токене — том же, что физически лежит открытым текстом в `.git/config` локальных клонов продуктовых репозиториев. Это не три отдельных находки, это одна дыра, подтверждённая с трёх разных сторон в одну ночь.

**Решения заседания (полный текст — `state/12_DECISIONS.md`):**
- `D-260824-03` — `product/moments/D0_TRACKING_PLAN.md` принят CDO без правок, `B-260813-11` закрыта.
- `D-260824-04` — `B-260813-10` (tracking plan ПРАКТИКИ): второй сорванный срок PO, борд даёт явный чек-лист приёмки и новый срок 25.08.
- `D-260824-05` — `B-260824-02` (мост аналитики ЗАПИСКИ→ПРАКТИКА): ретроактивная виза InfoSec и CDO без содержательных возражений, процессное нарушение зафиксировано.
- `D-260824-06` — промпт трека «реализация» расширен читать P0-бэклог, исполнено этим же заседанием (правка `.runner/prompt-implement.md`).
- `D-260824-07` — `B-260818-04` подтверждена дважды независимым зелёным прогоном тестов (CTO и CDO), оговорка снята.
- `D-260824-08` — новые находки (автодеплой на push, токен в `.git/config`) усиливают `H-260818-01`, новых `/human` не заводится.
- `D-260824-09` — `H-260816-01` уточнена: рекомендация борда — снять промостраницу сегодня, не чинить.
- `D-260824-10` — находка бухгалтера/CFO о договоре эквайринга Т-Банка становится формальной задачей очереди `H-260824-01`.

**Эскалации ⚠️.** Барьер между агентом и продакшеном не работает технически ни на одном уровне (первая строка сводки). `H-260818-01`/`H-260818-02` не закрыты седьмой цикл подряд. `H-260813-03` — двенадцатые сутки тишины данных. Промостраница ЗАПИСОК — восьмые сутки действующего нарушения, рекомендация ужесточена до «снять».

**Портфельный приоритет на 3–4 дня (защитил CPO).** Порядок: остановить механизм неконтролируемой записи в `main` (ротация токена закрывает разом `B-260824-01`, `B-260822-01`, `B-260824-03`) → закрыть `H-260818-01`/`H-260818-02`/`H-260813-03` → довести откат и P0-долг ЗАПИСОК (`B-260819-02`, `B-260819-01`, `B-260813-01`, `B-260813-08`) → `B-260817-04`(PR #91)/`B-260813-10` как управленческая гигиена ниже по списку.

**Что вытеснено.** Custdev ПРАКТИКИ, выкат биллинг-трекинга Т-Банк (ждёт бэкапа и решения человека) — тринадцатый учётный цикл подряд той же причиной: безопасность, данные и процесс исполнения бьют всё по уставу §9.

**Спорные вопросы.** Ни одного голосового разногласия между ролями не возникло — все девять ролей независимо сошлись на одной приоритетной находке (`B-260824-01`) и на снятии промостраницы вместо починки. Единственное уточнение позиции (не спор) — CMO и Legal ужесточили рекомендацию по `H-260816-01` с «снять либо починить» до «снять», зафиксировано `D-260824-09`.

**`/human` очередь на выходе (7/5, обоснованное превышение — устав §6.2, доступы/инциденты и деньги не конкурируют за лимит):** `H-260818-02` (SSH-ключ, активный инцидент) → `H-260813-03` (доступ к биллингу) → `H-260818-01` (токен + branch protection) → `H-260816-01` (снять промостраницу) → `H-260813-05` (снять обещание в письме/экране входа) → `H-260813-04` (публичность репозиториев) → `H-260824-01` (новая — подтвердить договор эквайринга Т-Банка).

**Заседание состоялось** — восемь решений принято, ни одно не отложено без даты.

**Доставка.** Эскалация ⚠️ (барьер не работает ни на одном уровне, токен и ключ седьмой цикл не закрыты, двенадцатые сутки тишины данных) уходит отдельным сообщением первой, до сводки — `outbox/2026-08-24_board_escalation.txt`.

---

## N2 · четверг 20.08.2026, 10:00 МСК

**Кворум.** CEO, COO, CPO, CMO, CTO, CFO, Senior Legal, главный бухгалтер, InfoSec, CDO — десять ролей, все присутствовали (CDO обязателен: повестка включает измеримость и находку по собственному реестру; Legal/InfoSec обязательны: активный инцидент безопасности и подтверждённая публикация с юридическим риском; CFO/бухгалтер обязательны: биллинг Т-Банка и B-260819-02 в повестке).

**Повестка.** 1) Контроль исполнения решений N1. 2) Три числа. 3) Круг ролей. 4) Портфельный приоритет на 3–4 дня. 5) Срочные вмешательства в бэклог. 6) Спорные вопросы. 7) `/human` очередь. Плюс явное распоряжение `O-260817-04`, адресованное борду 17.08 и не обработанное три цикла подряд.

**Контроль исполнения решений N1 (`D-260817-01..05`).** 01 (агенты пишут код в ветке+PR) — выполнено. 02 (убрать автодеплой + восстановить `main` ЗАПИСОК) — испорчено процессом: правка в `CLAUDE.md` cmpas.ru попала на `HEAD` не через PR #91, а прямым ботовским коммитом мимо ревью — тем же каналом, который решение должно было закрыть как риск; `main` в ЗАПИСКАХ не восстановлен. 03 (МОМЕНТЫ бесплатны) — держится. 04 (ЗАПИСКИ идут к единому биллингу) — нарушается: 19.08 в код добавлен третий платёжный провайдер против направления решения. 05 (чек-ин МОМЕНТОВ по касанию) — выполнено, регресса нет. Итог: 2 из 5 чисто выполнены, 1 испорчено процессом, 1 активно нарушается, 1 держится без изменений.

**Три числа.** Деньги: данных нет, `H-260813-03` — восьмые сутки тишины, порог P0 (`05_METRICS §4.5`) пробит второй день подряд, CFO — функция физически не работает. Продукт: 0 метрик из дерева `05_METRICS §1.3` посчитаны по SQL (доклад CDO); восьмой цикл подряд без задачи, двигающей North Star. Доверие: минимум 15 прямых коммитов бот-идентичностей в `main` `cmpas.ru` мимо PR за окно 18.08 10:06–12:42 UTC при живом деплое на push — барьер «агенты не мержат» сегодня держится только на честном слове.

**Главная находка заседания.** Распоряжение `O-260817-04` (адресат БОРД, 17.08 08:21 UTC) три дня содержало прямой ответ учредителя на пункт деки N1: по `H-260816-01` — «она уже опубликована!». Ни одно решение N1 не отразило это, очередь `/human` продолжала формулировать вопрос как «публиковать ли», хотя ответ уже был дан. Найдено независимо ролями CEO, CMO, Legal и InfoSec при подготовке к N2 — совпадение усиливает достоверность. Legal квалифицировал текущее состояние страницы как недостоверную рекламу (ст. 5 ФЗ «О рекламе», ст. 14.3 КоАП) и нарушение ст. 10 ЗоЗПП по цене — действующее, не гипотетическое нарушение.

**Решения заседания (полный текст — `state/12_DECISIONS.md`):**
- `D-260820-04` — `O-260817-04` закрыто с опозданием; `H-260816-01` переформулирована из «публиковать ли» в «снять либо срочно исправить уже опубликованную страницу», статус приравнен к активному инциденту.
- `D-260820-05` — PR #91 (устранение автодеплоя cmpas.ru) не закрывается до включения branch protection на `main`.
- `D-260820-06` — `H-260818-01` и `H-260818-02` подтверждены единым неразделимым приоритетом сегодняшнего дня.
- `D-260820-07` — цена/триал/провайдер ЗАПИСОК (`B-260819-02`) откатываются автоматически трек-реализацией, если решение учредителя не подтверждено дословно в `12_DECISIONS` к ночному циклу 21.08.
- `D-260820-08` — `B-260818-04` (реестр не пускает анонимные события МОМЕНТОВ/ЗАПИСОК) поднята с P1 до P0, владелец CDO+CTO, срок сегодня.
- `D-260820-09` — сборка PDF дека (`B-260815-03`) поднята с P2 до P1 после шести циклов подряд без результата.

**Эскалации ⚠️.** Промостраница ЗАПИСОК — активный инцидент, не решённый вопрос (см. выше). InfoSec: барьер «агенты не мержат» не подкреплён технически второй цикл подряд, доказательная база растёт. CTO: вето на любой релиз в `products/` до включения branch protection на всех трёх репозиториях.

**Портфельный приоритет на 3–4 дня (защитил CPO).** Порядок: SSH-ключ (`H-260818-02`) → доступ к биллингу (`H-260813-03`) → ротация токена (`H-260818-01`) → подтверждение цены ЗАПИСОК (`B-260819-02`) → CI-безопасность ЗАПИСОК (`B-260819-01`, S-размер, трек «реализация» сегодня же) → реестр анонимных событий (`B-260818-04`) → снятие AUDIT.md/THREAT-MODEL.md из публичного ЗАПИСОК (`B-260813-01`, четвёртый день просрочки решения N1).

**Что вытеснено.** Custdev ПРАКТИКИ, платформенная аналитика (`B-260813-02/03/06`) — восьмой день подряд той же причиной: безопасность и данные бьют всё по уставу §9.

**`/human` очередь на выходе (6/5, с обоснованным превышением лимита, порядок по блокирующей силе):** `H-260818-02` (SSH-ключ, инцидент) → `H-260813-03` (доступ к биллингу) → `H-260818-01` (токен + branch protection) → `H-260816-01` (переформулирована — снять/поправить активную публикацию) → `H-260813-05` (снять обещание в письме/экране входа) → `H-260813-04` (публичность репозиториев).

**Заседание состоялось** — шесть решений принято, ни одно не отложено без даты.

**Доставка.** Эскалация ⚠️ (промостраница ЗАПИСОК опубликована с 17.08 без обработки борда, два просроченных доступа) отправлена отдельным сообщением первой, до сводки — `outbox/2026-08-20_board_escalation.txt`. Маршрут A (напрямую): секреты на месте, `deliver.py` отправил текст и дек (8 слайдов, `deck_lint` — чисто, 0 ошибок). Сборка PDF снова не уложилась в 90 секунд на этом раннере (те же dbus/GCM-ошибки headless-браузера, что весь период 15–19.08) — отправлен HTML-фолбэк, доставка не потеряна (код выхода 0). Этой же находкой поднят приоритет `B-260815-03` (`D-260820-09`).

---

## N1 · понедельник 17.08.2026, 10:00 МСК

**Кворум.** CEO, COO, CPO, CMO, CTO, CFO, Senior Legal, главный бухгалтер, InfoSec, CDO — десять ролей, все присутствовали (CDO обязателен: повестка включает измеримость; Legal/InfoSec обязательны: персональные данные и публичные коммуникации в повестке; CFO/бухгалтер обязательны: в повестке деньги — Т-Банк, биллинг-трекинг).

**Повестка (задана заранее, `21_BOARD_LOG` версия до N1) плюс распоряжение учредителя `O-260817-01`:**
1. Утверждение устава и метрик — что не работает.
2. Состояние доступов `H-260813-01…03`.
3. Публичность репозитория ЗАПИСКИ (`B-260813-01`).
4. Первый портфельный приоритет на неделю от CPO.
5. Что не измеряем.
6. (Добавлено 17.08, P0 срочно) `O-260817-01` — разрешить агентам писать код `products/` в ветке и открывать PR.

**Три числа.** Деньги: данных нет, `H-260813-03` просрочена, CFO — функция физически не может работать без доступа к биллингу. Продукт: NSM ни одного из трёх продуктов не измеряется (CDO — 0 из дерева `05_METRICS §1.3`); пятый цикл подряд (13→17.08) в плане нет ни одной задачи, двигающей North Star. Доверие: `14_SIGNALS_INBOX` пуст, ORM не собирается — обращений и упоминаний бренда не видно вообще.

**Что не работает в уставе/метриках (пункт 1 повестки).** CTO обнаружил структурный пробел: `daily.yml`/`board.yml` — единственный раннер с `concurrency: rhythm` и таймаутом 60 минут; отдельного трека разработки, который предполагали устав и `07_REPOS`, физически не существует — сегодняшняя дневная ёмкость разработки равна 0. Сборка PDF дека ломается пятый цикл подряд (`B-260815-03`) без попытки исправить сам образ раннера. Очередь `/human` третьи сутки на лимите 5/5, три задачи просрочены одновременно.

**Контроль исполнения.** Прошлых решений борда нет — N1 первое заседание. Из внебордовых решений: `D-260813-01…08`, `D-260815-01…05` — все в статусе «принято»/«выполнено», кроме `D-260813-04` (текст+PDF в обоих ритмах) — де-факто просрочено: PDF не собирается пять циклов подряд, откат на HTML маскирует невыполнение.

**Решения заседания (полный текст — `state/12_DECISIONS.md`):**
- `D-260817-01` [CHARTER] — устав §6.1 и `07_REPOS §2` изменены: агенты открывают PR в ветке `feature/<id>` в продуктовых репозиториях, мёрж/деплой — только учредитель. Техническая активация — `H-260817-01`.
- `D-260817-02` — первый PR после активации токена — устранение автодеплоя в `products/cmpas.ru/CLAUDE.md` (`B-260817-04`), второй — восстановление `main` в `zapiski` (`B-260815-02`); только потом фичи.
- `D-260817-03` — МОМЕНТЫ остаются бесплатными, платный экран и промостраница 399 ₽/мес не публикуются (`B-260816-03` закрыта).
- `D-260817-04` — ЗАПИСКИ обязаны прийти к единому аккаунту/биллингу портфеля (уже требование устава §1); итоговая цена — решение учредителя, покрыто `H-260816-01`. Отдельно: критические файлы публичного `zapiski` (`AUDIT.md`, `THREAT-MODEL.md`, вендорная дизайн-система) убираются первой волной PR, не дожидаясь решения о публичности целиком.
- `D-260817-05` — приватность МОМЕНТОВ: чек-ин самочувствия возвращается к записи только по касанию, задним числом не визируется.

**Эскалации ⚠️.** CFO: функция не работает без доступа к биллингу — `H-260813-03` остаётся top-1 по блокирующей силе. COO: очередь `/human` физически переполнена третьи сутки, включая новую `H-260817-01`.

**Портфельный приоритет на 3–4 дня (защитил CPO).** До активации `PRODUCTS_READ_TOKEN` (`H-260817-01`) ни один P0 с кодовым решением не может быть закрыт агентами — только решён на бумаге. Первая волна PR после активации, по порядку: (1) `B-260817-04` автодеплой, (2) `B-260817-03` чек-ин МОМЕНТОВ, (3) `B-260815-02` ветка main ЗАПИСОК, (4) вынос критических файлов из публичного `zapiski` (`B-260813-01`), затем фичи `B-260817-01`/`B-260817-02`.

**Что вытеснено.** Custdev ПРАКТИКИ, инвентарь событий ЗАПИСОК, проверка листингов МОМЕНТОВ (`B-260813-13`) — риском безопасности/приватности пятый цикл подряд, устав §9. `B-260813-10`/`B-260813-11` (tracking plan ПРАКТИКИ, D0-воронка МОМЕНТОВ) возвращены CTO PO на доработку — нет полноценного документа-спецификации.

**Пробелы измерения (пункт 5, доклад CDO).** 0 метрик из дерева `05_METRICS §1.3` посчитаны по SQL. Единственная фаза, где узкое место не инженер, а доступ, — Ф0 (биллинг), заблокирована `H-260813-03`. CDO наложил вето на пороги активации в `B-260813-10`/`-11` как неизмеренные гипотезы.

**`/human` очередь на выходе (5/5, порядок по блокирующей силе):** `H-260813-03` (доступ к биллингу) → `H-260813-05` (снять обещание, готова формулировка Legal) → `H-260813-04` (публичность репо, готов список InfoSec) → `H-260816-01` (публиковать ли промостраницу ЗАПИСОК, факты подтверждены CMO) → `H-260817-01` (новая — активировать токен записи в `products/`). `H-260815-06` (ссылки на листинги МОМЕНТОВ) вытеснена из лимита, не срочная.

**Заседание состоялось** — пять решений принято, ни одно не отложено без даты.

**Доставка.** Маршрут A (напрямую): секреты на месте, `deliver.py` отправил текст и дек (8 слайдов, `deck_lint` — чисто, ноль ошибок). Сборка PDF снова не уложилась в 90 секунд на этом раннере (те же dbus/GCM-ошибки headless-браузера, что 15–17.08) — отправлен HTML-фолбэк, доставка не потеряна (код выхода 0). Воспроизводит `B-260815-03`, уже в бэклоге P2, новых действий не требуется.
