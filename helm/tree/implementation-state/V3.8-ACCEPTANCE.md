# V3.8-ACCEPTANCE — построчная сверка с acceptance-списком владельца

Составлено 30.08.2026 по прямому требованию «Evidence before completion».
Каждая строка — либо имя конкретного теста, который это проверяет, либо
честное «не проверено / неприменимо» с причиной. **Строка без теста не
считается пройденной**, сколько бы кода за ней ни стояло.

Прогон на момент составления: **415/415 зелёных**, панель —
`npm run typecheck` чист, `npm run build` собирается,
`node tests/brief-compliance.mjs` — 27 проверок.

> **READY по v3.8 НЕ объявляется.** Живой деплой на VPS не выполнялся ни
> для v3.7 (ZIP batch ingest), ни для v3.8; независимого review не было.
> Оба условия владелец поставил явно. Ниже — что проверено тестами, а
> не что работает на проде.

---

## 1. Регрессия v3.7 и существующего корпуса

| Критерий | Чем подтверждено |
|---|---|
| Тесты v3.7 ZIP batch ingest проходят | `tests/test_knowledge_zip_safety.py`, `tests/test_knowledge_batch_intake.py` — в общем прогоне 415/415, ни один не менялся ради v3.8 |
| Существующий корпус владельца не потерян и не переразобран | Миграция `ef1ba5467e14` — backfill присваивает все существующие строки одному `SYSTEM_OWNER` без reparse/rechunk/re-embedding; проверено downgrade→upgrade round-trip'ом и backfill'ом на ЗАРАНЕЕ вставленной строке, а не на пустой схеме (см. Evidence P8.6.1) |
| Одиночный документ и ZIP-путь не переписаны | `git diff` v3.7→v3.8: `ingest.py`/`chat_intake.py`/`batch_intake.py` получили только tenant-параметр и проверку квоты; `zip_safety.py` не тронут вовсе |

## 2. Micro-Memory «Запомни»

| Критерий | Чем подтверждено |
|---|---|
| Remember текстом — 0 платных вызовов | `test_recall_makes_zero_paid_calls`; структурно — `test_knowledge_answer_composition_cannot_reach_a_model_or_owner_style` (модули записи/чтения памяти не импортируют клиента модели вовсе) |
| Запрещённый секрет не сохраняется | `test_try_remember_rejects_forbidden_secret_without_writing_anything` |
| Точный повтор не создаёт второй активной записи | `test_try_remember_exact_repeat_is_deduped` |
| Явный временной маркер даёт срок; без маркера срок не выдумывается | `test_parse_temporal_expiry_today_is_end_of_local_day`, `..._tomorrow_...`, `test_parse_temporal_expiry_none_without_explicit_cue` |
| Markdown-зеркало соответствует записи в БД | `test_try_remember_stores_fact_with_confirmation` (сверяет файл зеркала) |
| Нормальная Micro-Memory не запускает парсер/чанкер | `test_try_remember_stores_fact_with_confirmation` — источник не создаётся; переполнение уходит в SOURCE отдельным путём (`test_try_remember_routes_overflow_text_to_source_not_memory`) |
| **Remember голосом — 0 платных вызовов** | ❌ **не проверено: не реализовано.** GigaAM нигде в кодовой базе не подключён; голосового пути нет ни для владельца, ни для secondary |

## 3. Recall

| Критерий | Чем подтверждено |
|---|---|
| Ссылка возвращается точно | `test_bookmark_url_returned_byte_exact` — сравнение РАВЕНСТВОМ с `canonical_text`, не вхождением |
| Идентификатор возвращается точно | `test_identifier_returned_exact` |
| Точный URL/идентификатор не переписывается локализатором | Локализатора нет вовсе; структурно — `test_knowledge_answer_composition_cannot_reach_a_model_or_owner_style` |
| Временный факт исчезает из текущего recall в момент запроса | `test_expired_memory_is_excluded_from_current_recall` — истёкшая запись остаётся `ACTIVE` в БД и всё равно не возвращается: срок режет предикат, а не фоновая рутина (её нет) |
| Исторический вопрос может использовать истёкшее | `test_expired_memory_is_available_to_historical_query`, `test_expired_status_row_is_available_to_historical_query` |
| «Забудь» (DISABLED) не всплывает и от исторической формулировки | `test_disabled_memory_never_returns_even_historically` |
| `напомни` + будущий триггер → не recall | `test_future_reminder_phrase_is_not_memory_recall`, `test_future_reminder_does_not_answer_from_memory` |
| `напомни [номер/ссылку/…]` без триггера → recall | `test_reminder_word_without_future_trigger_is_recall`, `test_temporal_word_without_action_is_still_recall` |
| recall делает 0 вызовов LiteLLM/OpenRouter | `test_recall_makes_zero_paid_calls` + структурный тест импортов |
| **Маршрутизация в REMINDER_TASK для SYSTEM_OWNER** | ⚠️ **частично.** Подсистемы задач/напоминаний в HELM нет вовсе, поэтому проверяется только то, что фраза НЕ съедается памятью и эскалируется. Создания напоминания нет и проверять нечего |

## 4. Мультитенантность и изоляция

| Критерий | Чем подтверждено |
|---|---|
| Одинаковый SHA256 у двух пользователей не даёт дедуп | `test_ingest_text_does_not_dedup_across_users`, `test_register_file_for_ingest_does_not_dedup_across_users`, `test_stage_batch_does_not_dedup_archive_across_users` |
| FTS пользователя A никогда не возвращает данные B (документы) | `test_secondary_user_probe_does_not_see_system_owner_document_corpus` |
| FTS пользователя A никогда не возвращает память B | `test_memory_of_one_user_is_never_recalled_by_another`, `test_search_memories_filters_by_tenant_directly`, `test_search_memories_returns_nothing_for_unknown_tenant` |
| RLS — второй слой, не декоративный | `test_rls_hides_rows_when_no_tenant_is_bound`, `test_rls_hides_other_tenants_rows`, `test_rls_rejects_insert_for_a_different_tenant_than_the_bound_guc` (тестовая роль понижена до `NOSUPERUSER`, иначе RLS обходился бы всегда) |
| Воркер обрабатывает job в тенанте самого job'а | `test_process_job_binds_rls_from_the_jobs_own_tenant_not_another_users` |
| Память не дедупится и не течёт между пользователями | `test_try_remember_does_not_dedup_or_leak_across_users` |
| **vector/Graphify/cache пользователя A не возвращают данные B** | ⚠️ **неприменимо, а не пройдено.** Ни embeddings/pgvector, ни Graphify, ни кэш (Redis/иной) в кодовой базе не существуют — проверять нечего. Как только появятся, критерий вернётся открытым |
| **Скачивание чужого источника отклоняется** | ⚠️ **неприменимо, а не пройдено.** Эндпоинта скачивания оригинала (§14.15 DOCUMENT_REQUEST) не существует ни для одной роли — отклонять нечего. Критерий обязан быть перепроверен вместе с реализацией скачивания |

## 5. Разделение ролей и доступ

| Критерий | Чем подтверждено |
|---|---|
| Одноразовый инвайт-токен | `test_consume_invite_is_one_use_only` |
| Истёкший/переиспользованный/отозванный инвайт отклонён | `test_consume_invite_rejects_expired_token`, `test_start_with_expired_invite_is_rejected`, `test_start_with_reused_invite_is_rejected` |
| Несовпадение ожидаемого Telegram id отклонено | `test_consume_invite_rejects_expected_telegram_id_mismatch`, `test_start_with_mismatched_expected_id_is_rejected` |
| Привязка по verified `from.id` в приватном чате | `test_start_with_valid_invite_binds_identity_and_activates`, `test_group_chat_message_is_ignored` |
| Один Telegram-аккаунт не может стать двумя активными пользователями | `test_same_telegram_identity_cannot_claim_two_active_users` (в двух файлах: модуль и вебхук) |
| Неверный/отсутствующий webhook-секрет отклонён | `test_webhook_rejects_wrong_secret`, `test_webhook_rejects_missing_secret`, `test_webhook_fails_closed_when_no_secret_configured` |
| Неизвестный пользователь не получает доступ к Knowledge | `test_unknown_user_gets_no_access`, `test_resolve_active_user_by_identity_returns_none_for_unknown_identity` |
| Приостановленный пользователь заблокирован | `test_suspended_user_is_rejected`, `test_resolve_active_user_by_identity_returns_none_for_suspended_user`, `test_suspend_user_blocks_access_and_is_idempotent` |
| KNOWLEDGE_USER не доходит до Hermes по конструкции | `test_secondary_user_probe_never_reaches_needs_reasoning_paid_ai` (поведение) + `test_knowledge_user_webhook_cannot_reach_a_model` (структурно, разбор импортов) |
| KNOWLEDGE_USER не может approvals/system/Git/n8n/SignalAI | Вебхук `/hooks/knowledge-telegram` не имеет ни одного пути к action registry/approval engine — в модуле нет ни импорта, ни вызова; структурно закреплено тем же тестом импортов |
| **Owner chief bot по-прежнему отклоняет secondary-пользователя** | ⚠️ **проверено отсутствием изменений, не тестом.** `TELEGRAM_ALLOWED_USERS` — конфигурация Hermes на хосте, её в этом репозитории нет. `git diff v3.7..v3.8` по `hermes/plugins/helm-control/__init__.py` содержит ТОЛЬКО путь Remember; `owner_id`-гейт в `api/auth.py` не изменён ни строкой. Живой проверки не было |
| Стили не пересекаются между пользователями | `test_knowledge_answer_composition_cannot_reach_a_model_or_owner_style`. Сегодня свойство держится тем, что стиля и ступени синтеза нет вовсе (см. P8.6.6) |

## 6. Квоты, очередь, панель

| Критерий | Чем подтверждено |
|---|---|
| Честная очередь между тенантами | `test_claim_next_job_is_fair_round_robin_across_tenants` |
| Квота хранилища/дневного объёма | `test_check_and_record_ingest_rejects_over_storage_quota`, `..._over_daily_ingest_quota`, `test_daily_ingest_counter_resets_on_a_new_day`, `test_register_file_for_ingest_rejects_when_storage_quota_exceeded` |
| Предел глубины очереди | `test_check_queue_depth_rejects_at_cap` |
| Владелец не видит контент чужих пользователей из панели | `test_list_users_never_exposes_content_or_telegram_id`; структурно — эндпоинты раздела не называют ни одной tenant-scoped таблицы |
| Passkey-церемония привязана к операции и к цели | `test_stepup_for_invite_cannot_suspend`, `test_stepup_for_one_user_cannot_suspend_another`, `test_stepup_challenge_is_single_use` |
| Владельца нельзя приостановить из раздела «Пользователи» | `test_owner_cannot_be_suspended_through_users_section`, `test_owner_quota_is_not_editable_here` |
| KNOWLEDGE_USER получает Knowledge-only оболочку, а не разделы владельца | `test_knowledge_user_is_refused_owner_sections` (параметризован по всем шести разделам), `test_knowledge_user_cannot_approve`, `test_knowledge_user_cannot_invite_or_suspend_others` — отказ сервера, не отсутствие пункта меню |
| Оболочка показывает только СВОЁ | `test_knowledge_shell_shows_only_own_memories`, `test_knowledge_shell_for_owner_shows_owner_corpus` — тенант берётся из сессии, параметра для чужого id нет |
| Вход KNOWLEDGE_USER не стал обходом owner-входа | `test_usernameless_login_cannot_mint_an_owner_session` — usernameless-путь отвергает владельческий passkey (403), иначе одного passkey хватало бы вместо Telegram-виджета + passkey |
| Токен доступа в панель одноразовый, короткоживущий, только для ACTIVE | `test_enrollment_token_is_one_use`, `test_enroll_start_rejects_used_and_expired_tokens`, `test_enroll_start_rejects_suspended_user`, `test_panel_token_refused_for_user_who_never_joined_the_bot` |
| Токен панели привязан к целевому пользователю | `test_panel_token_scope_is_bound_to_the_target_user`, `test_owner_cannot_issue_a_panel_token_for_himself` |
| Счётчики Второго мозга не смешиваются между пользователями | `test_counts_do_not_leak_between_users`, `test_repeated_ingest_of_the_same_text_does_not_double_count`, `test_duplicate_remember_does_not_double_count` |

## 7. Бэкап и восстановление

| Критерий | Чем подтверждено |
|---|---|
| Бэкап покрывает Knowledge | `scripts/backup.sh` — `pg_dumpall` (весь кластер, включая `knowledge_users` и связанные) + каталог `/opt/helm-knowledge` целиком |
| **Restore сохраняет владельца и одного вторичного пользователя** | ⚠️ **проверка ДОБАВЛЕНА, но не выполнена.** `scripts/restore_test.sh` теперь проверяет: ровно одна строка `SYSTEM_OWNER`, число `KNOWLEDGE_USER`, отсутствие осиротевших `knowledge_sources` (ссылка на несуществующего тенанта — худший исход: база выглядит целой, а изоляция уже не та), и что Markdown-зеркал памяти восстановлено не меньше, чем строк памяти. Скрипт требует restic-доступа к живому репозиторию бэкапов — **запустить его можно только на VPS, здесь он не выполнялся** |

---

## Что осталось до READY

1. **Живой деплой** — ни v3.7 (ZIP), ни v3.8 на VPS не накатывались. Всё
   выше — тесты, а не работающий сервер.
2. **`KNOWLEDGE_TELEGRAM_BOT_TOKEN`** — владелец ещё не завёл бота через
   BotFather (точные шаги — `docs/adr/ADR-025`). До этого вебхук
   fail-closed отклоняет всё, и ни один живой secondary-пользователь
   физически не существует.
3. **`scripts/restore_test.sh` на VPS** — проверка тенантности в бэкапе
   написана, но ни разу не выполнена.
4. **Независимое review** — не проводилось.

Пункт про вход KNOWLEDGE_USER в панель закрыт решением владельца от
30.08.2026 (отдельный enrollment-токен) и реализован.

Оставшиеся четыре пункта не закрываются написанием ещё какого-то кода:
все требуют действий владельца или живого сервера.
