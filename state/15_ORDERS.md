# Распоряжения учредителя

Прямые указания, отданные из Telegram. Ведёт бот `tools/tg_orders.py`, исполняют агенты.
Формат — `04_TEMPLATES §9`. Обновлено: 2026-08-13.

**Как это работает.** Учредитель пишет боту команду с текстом, распоряжение попадает сюда со статусом «принято». Ближайший цикл — ночной или борд, смотря кому адресовано, — читает этот файл **первым**, до бэклога, и исполняет открытые распоряжения. Исполнив, агент меняет статус и пишет, что сделано.

**Приоритет.** Распоряжение учредителя старше приоритетов бэклога: если оно помечено срочным, оно вытесняет план дня, а вытесненное уезжает с пометкой в `20_DAILY_LOG`. Обычное распоряжение встаёт в план ближайшего цикла наравне с P1 и не может лежать дольше двух циклов непринятым — на третий цикл агент обязан либо исполнить, либо объяснить в очереди учредителю, почему не может.

**Граница.** Распоряжение не отменяет устав §6.1. Если исполнение требует денег, публикации от лица бренда, релиза в прод, изменения цен и оферты или работы с персональными данными — агент не исполняет, а оформляет задачу учредителю по `04_TEMPLATES §4` с вариантами и рекомендацией и ставит распоряжению статус «упёрлось в границу». Это не отказ, а единственный законный способ его выполнить.

**Статусы:** принято → в работе → исполнено | упёрлось в границу | снято учредителем.

**Ретенция:** исполненные и снятые распоряжения старше 30 дней сжимаются в одну строку `12_DECISIONS` и удаляются отсюда.

---

## Открытые
#### Исправление адресации в распоряжениях O-260817-05 и O-260817-06 · 17.08.2026

Оба распоряжения ссылались на «фазу Ф3/Ф4 `charter/13_TRACKING_PLAN.md`». Ссылка неверна, и это ошибка постановщика, а не исполнителя: фазы Ф0–Ф5 описаны в `charter/12_ANALYTICS.md §6`, а `13_TRACKING_PLAN.md` — про деньги и транспорт. По смыслу: ЗАПИСКИ — Ф3, МОМЕНТЫ — **Ф2** (не Ф4; Ф4 — это витрины в Metabase). Агент реализации назвал расхождение вслух и работал по явным правилам из текста распоряжений, а не по несуществующей секции — это верное поведение. Документы поправлены тем же днём: в `13_TRACKING_PLAN.md` добавлены границы документа, в `03_SOURCES.md` и `12_ANALYTICS.md` провайдер приведён к факту (Т-Банк, не ЮKassa).

#### O-260817-09 · Юридическая обвязка клиентского пути записи
- **Адресат:** ПРАКТИКА (реализация) · приёмка Legal
- **Принято:** 2026-08-17 от Илья, рабочая сессия (устав §5.4)
- **Приоритет:** P0 — идёт вместе с `O-260817-08`, отдельной выкатки не имеет
- **Статус:** принято
- **Основание:** `product/practice/CJM_booking_v2.md` §7, `CMPAS_LEGAL_IMPLEMENTATION.md` §§3.2, 6.4, 15.
- **Предварительная проверка (не исполнение):** `CMPAS_LEGAL_IMPLEMENTATION.md` не найден нигде в дереве `compas-ops` — сплошным поиском по репозиторию. Это не блокирует весь заказ: пункты 1, 4 и часть 5 — чистый код, не требуют текста пакета (`DiaryClient.consentVersion/consentDate/consentHash` уже есть в схеме, `ConsentVersion.text` уже хранит версионированный текст согласия отдельно от `LegalDocument` privacy/terms — пункт 2 технически выполним прямо сейчас, ссылать галочку на уже существующий `ConsentVersion.text`, а не сочинять новый). Пункт 3 (реквизиты сторон, срок хранения, порядок отзыва — конкретные формулировки) прямо требует текста из отсутствующего пакета — по границе самого распоряжения это вопрос к Legal, а не место для импровизации. Следующий цикл может либо получить документ, либо начать с пунктов 1/4/5(частично)/2, отдельно эскалировав пункт 3.
- **Как исполнять:** ветка `feature/client-legal`, pull request.
  1. **Согласие перестаёт зависеть от канала.** `checkConsentRequired`/`saveConsent` в `src/app/bot/actions.ts` привязаны к `telegramUserId`; клиент из Max или браузера согласие даёт, но сохранить его не к чему, и спрашивается оно каждый раз. Привязать к `DiaryClient`, а не к каналу. Тест: клиент, записавшийся из Max, имеет сохранённые `consentVersion`, `consentDate`, `consentHash`, и при повторном заходе согласие не спрашивается.
  2. **Развести согласие и политику.** Сегодня галочка «Я даю согласие…» ссылается на `/legal/privacy`. Политика не акцептуется (правило §3.2 пакета). Ссылку в галочке вести на текст согласия, политику показывать отдельной ссылкой «как обрабатываются данные».
  3. **Назвать стороны.** На экране согласия: оператор — психолог с реквизитами из его профиля; обработчик — ПРАКТИКА, по поручению; состав данных (имя, телефон, время записи); цель; срок; порядок отзыва. Если реквизитов психолога в профиле нет — показывать имя и предупреждать психолога в его интерфейсе, **не придумывать реквизиты**.
  4. **Отзыв согласия работает.** Кнопка на личной странице «Мои встречи»: отзыв фиксируется событием, будущие записи не создаются, специалист уведомляется. Историю согласий не удалять.
  5. **Сервисные и не-сервисные сообщения разделены в коде отправки**, а не только в интерфейсе: напоминания и подтверждения идут всегда; сообщение «давно не виделись» выключается одним касанием и это не влияет на напоминания.
- **Границы:** не сочинять юридических формулировок — тексты только из пакета; отсутствующий текст выносить как вопрос Legal. Не трогать пакет документов психолога (профессиональное соглашение, особые условия, заверение оператора) — другая очередь. Ничего о клиентах психолога не передаётся в рекламу СИМПАС. Не мержить.

#### O-260817-08 · Путь записи, шаг 1: поделиться ссылкой, честный перенос, приватная персональная ссылка
- **Адресат:** ПРАКТИКА
- **Принято:** 2026-08-17 от Илья, рабочая сессия (устав §5.4)
- **Приоритет:** P0 — блокирует выкатку MVP платформы
- **Статус:** исполнено (все три пункта закрыты, три отдельных pull request, ни один не смёржен)
- **Пункт 1 исполнен:** pull request [`compas-psy/cmpas.ru#79`](https://github.com/compas-psy/cmpas.ru/pull/79), ветка `feature/share-link`, не смёржен. Единый компонент `ShareSheet`/`ShareButton` (Max, Telegram, WhatsApp, копировать, QR) в `/diary`, `/diary/availability` (карточка со ссылкой была скрыта на телефоне — теперь видна), `/diary/clients` (десктопная и мобильная кнопки). В Android `SettingsScreen` теперь показывает настоящую `bookingLink` из `GET /api/mobile/dashboard` вместо выдуманного адреса, с копированием, системным шерингом и QR; удалены неиспользуемые `ExpandableActionMenu.kt` и заглушка quick-action `booking-link` (распоряжение разрешало подключить или удалить — подключать было бы придумыванием новой UI-точки входа вне спецификации). **Развилка, решена не молча:** у MAX нет публичного web-URL для «поделиться ссылкой» (в отличие от Telegram/WhatsApp) — кнопка использует Web Share API с честным fallback на копирование, а не выдуманный URL. Тесты: `npx vitest run` 6/6 (новый модуль `buildShareUrls.ts`; фреймворка тестов раньше не было — добавлен `vitest`), `eslint`/`tsc --noEmit`/`next build` — сверено с `main` через `git stash`, 0 новых ошибок; Android `gradle :app:testDebugUnitTest` 4/4, `gradle :app:assembleDebug` собирается.
- **Пункт 2 исполнен:** pull request [`compas-psy/cmpas.ru#82`](https://github.com/compas-psy/cmpas.ru/pull/82), ветка `feature/reschedule-atomic`, не смёржен. Кнопка «Перенести» (веб-ссылка, Telegram-бот, MAX-бот) больше не ведёт на страницу новой записи — новая страница `/client/reschedule/[sessionId]` (токен тот же `clientActionToken`, `clientActionToken` не тронут) с выбором времени, перенос — один `UPDATE` существующей строки `DiarySession`, а не delete+create: у клиента физически не бывает ни нуля, ни двух активных броней. Общая логика `rescheduleSessionAtomic()` вынесена в `src/lib/session-reschedule.ts` и используется и специалистом (`RescheduleModal`, поведение не изменилось), и клиентом. **Развилка, решена не молча:** распоряжение просило «в той же транзакции, что и создание новой» (подразумевая delete+create) — сделал через один `UPDATE` вместо этого: та же гарантия ровно одной сессии, но без второй строки и без нужды переносить `homeworks`/заметки на новый `id`; специалистская сторона уже была устроена так, просто вынес в общий модуль. Тест обязателен по распоряжению: 4 кейса с замоканным Prisma, включая «конфликт по времени → update не вызывается вообще» — старая бронь остаётся нетронутой. `npx vitest run` 4/4, `eslint`/`tsc --noEmit`/`next build` — 0 новых ошибок относительно `main`.
- **Пункт 3 исполнен:** pull request [`compas-psy/cmpas.ru#83`](https://github.com/compas-psy/cmpas.ru/pull/83), ветка `feature/signed-client-link`, не смёржен. Открытый `?c=<DiaryClient.id>` в персональной ссылке записи заменён на HMAC-подписанный токен со сроком 30 дней (`personalClientToken`/`resolvePersonalClientToken` в `client-workflow.ts`) — старый формат принимается ещё 90 дней (до 2026-11-15, фиксированная дата от сегодня, поскольку у уже разосланных ссылок нет метки времени отправки) и логируется как устаревший. Переведены на подписанный токен все места, где `?c=` строился вручную (Telegram- и MAX-боты, кнопки «Написать»/«Копировать и отправить» в `/diary/clients`), а не только `clientBookingLink()` — иначе основной объём ботового трафика остался бы уязвимым. `clientActionToken` не тронут. **Развилка, решена не молча:** Telegram deep-link `?start=psy_X_c_Y` в инлайн-ответе бота несёт ту же категорию риска (форвардится, сырой id), но это не `?c=`-параметр, а другой механизм (стартовая полезная нагрузка бота) — распоряжение называет именно `?c=<DiaryClient.id>`, не трогал, назвал в PR. Тесты: 8 кейсов в `client-workflow.test.ts` (подпись/резолв/порча/протухание/легаси/пустые значения), `npx vitest run` 8/8; `eslint`/`tsc --noEmit`/`next build` — 0 новых ошибок относительно `main`.
- **Основание:** `product/practice/CJM_booking_v2.md` (версия 2, решение учредителя: ПРАКТИКА не берёт денег с клиента). Пункты 1, 2 и 8 порядка работ §4.
- **Как исполнять:** три куска, один pull request на каждый, ветки `feature/share-link`, `feature/reschedule-atomic`, `feature/signed-client-link`.
  1. **Поделиться ссылкой одинаково везде.** Единый компонент «Поделиться» с листом действий (Max, Telegram, WhatsApp, скопировать, QR) в четырёх местах: `/diary` (кнопка есть — привести к общему виду), `/diary/availability` (сегодня блок в `hidden lg:block` — показать на телефоне), `/diary/clients` (кнопка «Написать» на десктопе только копирует — либо переименовать, либо научить отправлять). В приложении Android: экран «Ссылка для записи» в `SettingsScreen.kt` показывает выдуманный адрес `cmpas.ru/book/ilya-martynov` и не даёт ни ссылки, ни копирования — заменить на настоящую ссылку из `GET /api/mobile/dashboard` (поле `bookingLink` уже приходит и нигде не читается), добавить копирование, системный шеринг и QR (`QrCode.kt` в проекте уже есть, используется в `InviteSheet`). Мёртвый `ExpandableActionMenu` и заглушку `quick-action/booking-link` — либо подключить, либо удалить, но не оставлять.
  2. **Перенос — одно действие.** Сегодня `a=reschedule` в `src/app/api/client/session-action/route.ts` — редирект на страницу записи, старая сессия остаётся, клиент получает две брони. Сделать настоящий перенос: экран выбора нового времени, старая сессия снимается в той же транзакции, что и создание новой, и только при успехе. То же для кнопок `reschedule_session_*` в `telegram-bot.ts` и `max-bot.ts`. Тест обязателен: после переноса у клиента ровно одна активная сессия.
  3. **Персональная ссылка подписывается.** Сегодня `?c=<DiaryClient.id>` — открытый идентификатор без подписи: любой, кому переслали ссылку, действует от имени клиента. Заменить на подписанный параметр с сроком 30 дней, старый формат принимать ещё 90 дней и логировать как устаревший. Не трогать `clientActionToken` — это отдельная задача.
- **Границы:** не трогать деньги — клиентского биллинга в продукте нет и не будет на этом этапе. Не менять схему базы там, где можно обойтись. Новое поведение за флагами. Не мержить.

#### O-260817-07 · Реализовать состояние S1-R: возврат по ссылке после паузы
- **Адресат:** ПРАКТИКА
- **Принято:** 2026-08-17 от Илья, рабочая сессия (устав §5.4)
- **Приоритет:** P1
- **Статус:** исполнено
- **Основание:** развилка, честно поднятая при исполнении `O-260817-03`: состояния S1-R не было в документе-источнике. Спецификация дописана — `product/practice/CJM_booking_v1.md` версия 1.2, этап 3-бис.
- **Как исполнять:** реализовать этап 3-бис по документу. Хранение: только предпочтение и отметка последнего визита, срок 30 дней, ни имени, ни контакта, ни незаполненных полей формы. Тест: возврат по ссылке не порождает ни одного исходящего сообщения клиенту; в сохранённом состоянии нет полей имени и контакта. За тем же флагом `timeSuggestEnabled`. Ветка `feature/booking-s1r`, pull request. Не мержить.
- **Что сделано:** pull request [`compas-psy/cmpas.ru#77`](https://github.com/compas-psy/cmpas.ru/pull/77), ветка `feature/booking-s1r`, не смёржен. **Развилка по базе ветки, решена и объяснена в PR, не молча:** распоряжение просит ветвиться «от актуального main» и одновременно требует тот же флаг `timeSuggestEnabled` — а этот флаг и весь модуль подбора времени существуют только в ещё не смёрженной `feature/booking-a-b` (`O-260817-03`, PR #76). Ветвился от `feature/booking-a-b`, не от `main`; PR #77 открыт с базой `feature/booking-a-b` и содержит только диф самого S1-R (3 файла) — после мёржа #76 базу можно переключить на `main` без конфликтов. Реализация: чистый модуль `src/lib/booking/return-state.ts` (`serializeReturnState`/`readReturnState`), хранит структурно только `{preference, lastVisitAt}` — полей для имени/контакта в типе нет вообще; localStorage-ключ на специалиста, TTL 30 дней. На странице записи (за флагом, не в `private` режиме) — баннер «С возвращением» с пересчитанными на момент открытия временами (не кэш: занятый слот просто не покажется, без объяснений и без «вы не завершили»), кнопки «Начать заново» и «Не запоминать меня на этом устройстве» (стирает немедленно, без модалки). Тесты — `tests/return-state.test.ts`, 9 кейсов: округление TTL на границе 30 дней, битый JSON, неизвестный `preference`, явная проверка что сериализованное состояние не содержит `name`/`contact`/`phone`, и структурная проверка, что модуль не импортирует ничего сетевого — гарантия на уровне кода, что возврат не может породить исходящее сообщение. `npx vitest run` — 22/22 зелёных; `eslint`/`tsc --noEmit`/`next build` на изменённых файлах — построчно сверено с базовой веткой, 0 новых ошибок. **Не сделано, названо в PR как зона вне размера задачи:** «если специалист изменил цену/правило отмены — показать новое» — страница записи сегодня вообще не показывает ни цену, ни правило отмены никому, ни новым, ни возвращающимся клиентам; чинить это здесь означало бы затащить в PR механику §1.2 целиком. Полного UI-теста нет — в репозитории нет инфраструктуры для тестирования React-компонентов (тот же пробел, что в #75/#76).

#### O-260817-04 · Вернуть чек-ин самочувствия МОМЕНТОВ к записи только по касанию
- **Адресат:** МОМЕНТЫ
- **Принято:** 2026-08-17 от Илья, рабочая сессия (устав §5.4)
- **Приоритет:** P0 · срочно · приватность
- **Статус:** исполнено
- **Распоряжение:**
  > Закрыть вопрос с согласиями в МОМЕНТАХ, поднятый на борде.
- **Как исполнять:** исправить `B-260817-03`. Блокер `B-260817-08` снят: токен продуктовых репозиториев имеет права на запись и pull request, доказательство — открытый `compas-psy/cmpas.ru#75`. Вернуть поведение `AftercareScreen.kt` и `StartSheet.kt` к документированному: значение чек-ина пишется **только при касании слайдера**, без касания — `null`. Тест, который падает, если значение записывается без взаимодействия. Привести в соответствие `docs/PRODUCT.md`, `docs/QA-TESTPLAN.md`, `docs/PRIVACY-DPO.md` — сейчас они описывают правильное поведение, а код делает другое; после правки кода документы верны и трогать их не нужно, но проверить и зафиксировать в PR. Отдельно: оценить, что делать с уже собранными за месяц значениями — предложить вариант в описании PR, решение принимает учредитель. Ветка `feature/checkin-touch-only`, pull request. Не мержить.
- **Что сделано:** pull request [`compas-psy/compas-voice#1`](https://github.com/compas-psy/compas-voice/pull/1), ветка `feature/checkin-touch-only`, не смёржен. `touched`-состояние возвращено в `StartSheet.kt` и `AftercareScreen.kt` (выставляется только в `onValueChange` слайдера, сбрасывается «Пропустить чек-ин»); логика «пишем значение только если тронули» вынесена в чистую функцию `checkInToRecord()` (`ui/components/CheckInGate.kt`) и покрыта юнит-тестом (в репозитории нет инфраструктуры для Compose UI test, добавлять её ради одной проверки не стал). Заодно вернул условие показа карточки сравнения `before != null && touched` в `AftercareScreen.kt` — той же регрессией потерянное. `docs/PRODUCT.md`, `docs/QA-TESTPLAN.md`, `docs/PRIVACY-DPO.md` проверены построчно — уже верны, не трогал. `gradle testDebugUnitTest` — 10/10 зелёных (8 старых `PracticeCatalogTest` не изменены), `gradle assembleDebug` чист. **Развилка для учредителя (не решил молча):** что делать с уже собранными за 12.07–17.08 значениями — технически нельзя отличить задним числом реальное касание от дефолтной пятёрки, `touched` не хранился. Данные не покидали устройство (у `compas-voice` нет бэкенда в MVP), поэтому предложил в PR два варианта — ничего не делать (данные и так только на телефоне пользователя) или разовая миграция, стирающая всю историю чек-инов целиком (заодно и настоящие); рекомендовал первый.

#### O-260817-05 · Разметка ЗАПИСОК: только метаданные, только по согласию
- **Адресат:** ЗАПИСКИ
- **Принято:** 2026-08-17 от Илья, рабочая сессия (устав §5.4)
- **Приоритет:** P1
- **Статус:** исполнено
- **Как исполнять:** фаза Ф3 `charter/13_TRACKING_PLAN.md`, события `note_saved`, `note_searched`, `sync_completed`, `export_requested` по реестру. ЗАПИСКИ — локальное приложение, поэтому: отправка событий с очередью и работой офлайн, включается **только** явным согласием в настройках, по умолчанию выключена, выключатель находится там же, где остальные настройки приватности. Ни содержимого заметок, ни заголовков, ни текста поисковых запросов, ни путей к файлам — только факт, задержка и корзина длины. Тест, который собирает тело исходящего запроса на хранилище с заметками и падает, если в нём встретилась любая строка из этих заметок. Ветка `feature/analytics-metadata`, тесты, pull request. Не мержить.
- **Что сделано:** pull request [`compas-psy/zapiski#2`](https://github.com/compas-psy/zapiski/pull/2), ветка `feature/analytics-metadata`, не смёржен. **Развилка по базе ветки, решена и объяснена в PR, не молча:** в репозитории `zapiski` нет ветки `main` вообще (`git ls-remote --heads` — только три `claude/...`/`feature/...` ветки) — ветвился от фактического мейнлайна, `claude/cloud-deployment-cross-platform-9nenw7` (обычная линейная история, `HEAD` по умолчанию). Реализация: чистый модуль-редактор `packages/core/src/analytics/schema.ts` (`buildAnalyticsEvent`) пропускает только поля, объявленные в `analytics/schema/events.yaml` для конкретного события, всё остальное отбрасывает молча — структурная защита правила «содержание не измеряется никогда»; офлайн-очередь `AnalyticsQueue` на диске (тот же приём, что у существующей `ChangeQueue`). Согласие: в БД и на сервере (`users.analytics_opt_in`, `POST /auth/analytics-consent`) уже была заготовка от более ранней сессии, но нигде не подключённая — доведено до конца: переключатель в настройках аккаунта, рядом с рекламным согласием, но отдельным текстом и тумблером (не связаны молча); по умолчанию выключено; отзыв стирает и офлайн-очередь. Четыре триггера подключены в местах реального сохранения/поиска/синка/экспорта. Сервер: `POST /api/v1/analytics/events` за двумя независимыми замками на каждый запрос — флагом `ANALYTICS_ENABLED` (по умолчанию `false`) и согласием конкретного пользователя, оба отсутствующих неотличимы снаружи (`404`); Zod `.strict()`-схемы роняют весь батч `400`-м при лишнем поле вместо тихого отбрасывания. Миграция `0005_analytics_events.sql` только добавляющая. 35 новых тестов, все зелёные, `pnpm -r test`/`pnpm -r typecheck` чисты на всём монорепо (core 285→299, app 438→447, server 225→237, включая интеграционный тест на настоящем Postgres) — в т. ч. требуемый распоряжением тест: реальное тело исходящего HTTP-запроса после сохранения заметки с текстом «Клиент рассказал про развод…» не содержит этой строки. **Не сделано, названо в PR, а не скрыто:** фоновая уборка сырых событий старше 180 дней (`12_ANALYTICS.md §4`) не реализована; включать `ANALYTICS_ENABLED=true` на бою нет смысла, пока вход через магик-линк/Яндекс ID не доведён до продакшна (`docs/product/scope.md`) — аккаунтов, к которым привязывать события, ещё нет, это отдельная и более ранняя проблема продукта, не входящая в это распоряжение. Попутно замечено (не тронуто): `docs/dev/modules/server.md` до этого PR ошибочно говорил «три миграции», хотя их уже пять, и `0004_consents.sql` не была документирована вовсе — не моя правка, отдельная находка.

#### O-260817-06 · Разметка МОМЕНТОВ: установка, первая практика, удержание
- **Адресат:** МОМЕНТЫ
- **Принято:** 2026-08-17 от Илья, рабочая сессия (устав §5.4)
- **Приоритет:** P1 · выполнять **после** `O-260817-04`
- **Статус:** исполнено
- **Как исполнять:** фаза Ф4 `charter/13_TRACKING_PLAN.md`, события `app_installed`, `practice_started`, `practice_finished`, `crossed_to_product`. Установка всегда считается в паре с первой практикой в первый день: установки без удержания — расход, а не рост. Согласие на аналитику спрашивается один раз, отдельно от чек-ина самочувствия и другими словами; до согласия события не отправляются вовсе. `practice_finished.completion_pct` — редакторская метрика о контенте, не продуктовая. Ветка `feature/analytics-momenty`, тесты, pull request. Не мержить. Не начинать, пока `O-260817-04` не закрыт.
- **Что сделано:** pull request [`compas-psy/compas-voice#2`](https://github.com/compas-psy/compas-voice/pull/2), ветка `feature/analytics-momenty`, не смёржен. **Три развилки, решены и объяснены в PR, не молча:** (1) ссылка распоряжения «фаза Ф4 `13_TRACKING_PLAN.md`» не совпадает с содержанием — этот файл целиком про биллинг Т-Банка и в нём нет секции Ф4; по смыслу перечисленных событий это `12_ANALYTICS.md §6` фаза **Ф2** (D0-воронка МОМЕНТОВ), реализовывал по явным правилам из текста самого распоряжения, а не по несуществующей секции; (2) у МОМЕНТОВ нет бэкенда вообще (`docs/PRIVACY-DPO.md`, `CLAUDE.md`: «без бэкенда и без аккаунтов», в gradle нет ни одной сетевой зависимости) — события строятся, проходят строгий allowlist-реестр и копятся в локальной DataStore-очереди (до 500, стирается с «Очистить мои данные»), но никуда не отправляются: отправлять некуда, пока у продукта нет собственного `/ingest`; (3) `crossed_to_product` — в приложении нет ни одной кросс-промо точки перехода в другой продукт СИМПАС, событие и схема готовы, вызова нет нигде — добавлять UI-триггер значило бы придумывать продуктовое решение, которого нет ни в `docs/PRODUCT.md`, ни в распоряжении. Реализация: `analytics/schema/events.yaml` + `AnalyticsSchema.kt` — строгий allowlist полей (необъявленное отбрасывается молча, структурная защита правила «содержание не измеряется никогда»); `AnalyticsRecorder.kt` — до согласия ни один вызов не имеет побочного эффекта; одноразовый диалог согласия после онбординга + тумблер в «Я → Настройки» (по умолчанию выключено, текст и место отдельные от чек-ина самочувствия); `installedAtEpochMs` пишется локально при первом старте всегда (не аналитическое событие, никуда не уходит), чтобы `app_installed` при позднем согласии ушёл с реальным временем установки — это и есть связка «установка + первая практика в тот же день». Хуки `practice_started`/`practice_finished` в `KompasRoot`, `completion_pct` из позиции/длительности плеера, клампится 0–100. `docs/PRIVACY-DPO.md` обновлён (реестр данных + абзац про новую очередь и что Data Safety форму нужно будет пересмотреть отдельно вместе с транспортом). 9 новых тестов (allowlist на пяти сценариях + gating на всех четырёх событиях сразу), все зелёные; 8 прежних `PracticeCatalogTest` не изменены и тоже зелёные (`gradle testDebugUnitTest` — 17/17), `gradle assembleDebug` чист. **Не сделано, названо в PR, не скрыто:** реальная отправка (нет бэкенда), триггер `crossed_to_product` (нет UI-точки), пересмотр Google Play Data Safety (не требуется, пока нет передачи).

#### O-260817-04 · решение
- **Адресат:** БОРД
- **Принято:** 2026-08-17 08:21 UTC от Илья
- **Приоритет:** обычный
- **Статус:** принято
- **Распоряжение:**
  > решение
  > H-260816-01 · Публиковать ли промостраницу ЗАПИСОК · 10 мин · блокирует B-260816-01 -она уже опубликована!
  > H-260817-01 · Выдать токен записи в products/ · 10 мин · блокирует все кодовые P0 разом - выдан

#### O-260817-02 · Реализовать сквозную аналитическую разметку, фазы Ф0 и Ф1
- **Адресат:** CDO
- **Принято:** 2026-08-17 от Илья, рабочая сессия (устав §5.4)
- **Приоритет:** P0 · срочно
- **Статус:** исполнено
- **Распоряжение:**
  > Приступить к реализации аналитической сквозной разметки.
- **Как исполнять:** по `charter/13_TRACKING_PLAN.md`. Ф0 — приём вебхуков Т-Банка `NotificationPayment` с проверкой Token, таблица платежей с полем `terminal` (сайт и приложение — разные TerminalKey), своя таблица подписок для MRR, сверка по выписке T-API. Ф1 — приёмник `POST /ingest`, валидация по `analytics/schema/events.yaml`, таблицы `events` и `events_rejected`, событие `consent_updated` и правило «до согласия не пишем идентификаторы устройства». Только расширяющие миграции, только новые таблицы, за флагом, выключенным по умолчанию. `RebillId` и пароль терминала в аналитику не попадают никогда. Ветка `feature/analytics-f0-f1` в `products/cmpas.ru`, тесты, pull request. Не мержить и не выкатывать.
- **Что сделано:** pull request [`compas-psy/cmpas.ru#75`](https://github.com/compas-psy/cmpas.ru/pull/75), ветка `feature/analytics-f0-f1`, не смёржен. Ф0: вебхук `NotificationPayment` теперь определяет терминал по `TerminalKey` и проверяет `Token` паролем именно этого терминала (второй терминал — опциональные `TINKOFF_APP_TERMINAL_KEY`/`PASSWORD`, без них поведение как раньше); `Payment.terminal` (аддитивно, default `site`); новая таблица `Subscription` для MRR, пишется при успешном платеже за флагом `ANALYTICS_TRACKING_ENABLED` (default `false`); сверка по T-API и разовый импорт реестра — diff-логика с тестами плюс два ручных CLI-скрипта, ни один не подключён к автоматическому расписанию. Ф1: `POST /ingest` за флагом `ANALYTICS_INGEST_ENABLED` (default `false` → 404), `analytics/schema/events.yaml` с биллинговыми событиями и `identity_linked`/`consent_updated`, таблицы `events`/`events_rejected`; `device_id` не пишется, пока `User.analyticsConsentAt` не установлено; `rebillId` и пароль терминала структурно не могут попасть в `events` — реестр их не объявляет ни для одного события. В репозитории не было тестового фреймворка — добавлен `vitest`, 40 юнит-тестов (без обращения к реальной БД, Prisma-клиент замокан) зелёные; миграция дополнительно накатана на чистый Postgres в Docker и сверена через `prisma migrate diff` (пустой diff); `next build` проходит. Не сделано и явно не входило в эту итерацию: автоматические переходы `Subscription` между `trial`/`grace`/`churned` (только `active` при оплате), доразметка `plan` после разового импорта реестра (нет такой колонки у Т-Банка, ставится `unknown`), отдельная кросс-продуктовая таблица `account_id` (пока это `User.id` продукта). Не мержил, не выкатывал, не менял прод-конфигурацию.

#### O-260817-03 · Реализовать механики A и B записи на сессию в ПРАКТИКЕ
- **Адресат:** ПРАКТИКА
- **Принято:** 2026-08-17 от Илья, рабочая сессия (устав §5.4)
- **Приоритет:** P0 · срочно
- **Статус:** в работе
- **Распоряжение:**
  > Приступить к реализации CJM ПРАКТИКИ.
- **Как исполнять:** по `product/practice/CJM_booking_v1.md` и handoff. Механика A «тихая запись»: заголовок события в календаре выбирает клиент, канал напоминаний на выбор, СМС только с явного согласия, из текста напоминаний убраны имя специалиста и слово «психолог» — это закрывает и находку `B-260816-02`. Механика B «подбор времени»: один вопрос о предпочтении и три подобранных времени вместо сетки, полный календарь ссылкой; при пустом расписании — предложение листа ожидания, пустая сетка не показывается никогда. Плюс состояние S1-R: возврат по ссылке в течение 30 дней, память только о предпочтении, кнопка «не запоминать меня на этом устройстве», ни одного сообщения вдогонку. Обе механики за фича-флагами, выключенными по умолчанию. События воронки — по реестру разметки. Ветка `feature/booking-a-b`, тесты, pull request. Не мержить и не выкатывать.
- **Что сделано:** pull request [`compas-psy/cmpas.ru#76`](https://github.com/compas-psy/cmpas.ru/pull/76), ветка `feature/booking-a-b`, не смёржен. Механика A: текст 24-часового напоминания больше не называет специалиста и не говорит «психолог» — правка безусловная, не за флагом (закрывает `B-260816-02`, решение CPO — это красная линия приватности, не гипотеза для постепенной раскатки); новый флаг `PsychologistSettings.privateRemindersEnabled` (default `false`) — при включении ссылка подтверждения/отмены из напоминания перестаёт работать после конца сессии. Механика B: флаг `PsychologistSettings.timeSuggestEnabled` (default `false`) — экран с вопросом о предпочтении и 2–3 подобранными временами вместо сетки, «показать все времена» открывает прежний календарь без изменений; пустое расписание предлагает лист ожидания — новая таблица `WaitlistEntry`, только сбор контакта, без автоподбора при отмене (это, по `CJM §4`, отдельная механика после C). 19 юнит-тестов зелёные, миграция сверена `prisma migrate diff` на чистом Postgres (пустой diff), `next build`/`next lint` чисты на изменённых файлах. **Не сделано, распознано как развилка, а не решено молча:** состояние **S1-R** (возврат по ссылке 30 дней, память о предпочтении, кнопка «не запоминать меня») не описано ни в `CJM_booking_v1.md`, ни в бэклоге — есть только в тексте этого распоряжения; нужна отдельная спецификация PO-ПРАКТИКИ, иначе реализация — это агент придумывает UX за продукт. СМС-канал напоминаний и клиентский заголовок календарного события не реализованы — в коде нет ни SMS-провайдера, ни клиентской календарной интеграции вообще, сам `CJM §1.3` называет это функцией с нуля, а не переключателем.

#### O-260817-01 · Изменить устав §6.1 и 07_REPOS §2: агентам разрешено писать код продуктовых р…
- **Адресат:** CPO
- **Исполнено 17.08.2026 в рабочей сессии:** устав §6.1 и `07_REPOS §2` изменены, добавлен §5.4 о каналах задач, `04_TEMPLATES §10` о формате отчёта, `issues: write` в оба воркфлоу, промпты ритма обновлены. Решения записаны как `D-260817-01..03`.
- **Принято:** 2026-08-17 07:31 UTC от Илья
- **Приоритет:** P0 · срочно
- **Статус:** в работе
- **Распоряжение:**
  > Изменить устав §6.1 и 07_REPOS §2: агентам разрешено писать код продуктовых репозиториев в отдельной ветке feature/<id-задачи> и открывать pull request. Мёрж и деплой остаются действием учредителя, прямые коммиты в main продуктовых репозиториев запрещены. Перед пушем агент обязан прогнать тесты продукта; PR с падающими тестами не открывается. Технически: репозитории в products/ клонировать с PRODUCTS_READ_TOKEN (у него теперь Contents и Pull requests на запись), пушить ветку этим же токеном, PR открывать через gh с GH_TOKEN=PRODUCTS_READ_TOKEN. Добавить issues: write в permissions обоих воркфлоу ритма. Обновить .runner/prompt-daily.md и .runner/prompt-board.md, записать решением в 12_DECISIONS.
- **Что сделано:** борд N1 (17.08.2026) рассмотрел распоряжение и разделил его на две части. Текстовая правка устав §6.1 и `07_REPOS §2` — решена бордом сегодня же (`D-260817-01`, CHARTER), с двумя уточнениями от CTO и InfoSec: если у продукта нет тестов на затронутый путь — агент обязан написать минимальный тест, а не пропускать проверку; каждый push сопровождается secret-сканом. Техническая активация (права токена, `issues: write` в воркфлоу) не может быть выполнена сессией — заведена `H-260817-01`. До активации агенты по-прежнему не имеют права писать в `products/`; первым PR после активации, по решению `D-260817-02`, будет устранение автодеплоя из `products/cmpas.ru/CLAUDE.md` (`B-260817-04`), а не фичи.

#### O-260816-06 · Внедрить обработку обратной связи беты по claude/product/ZAPISKI_feedback_bet…
- **Адресат:** ЗАПИСКИ
- **Принято:** 2026-08-16 19:53 UTC от Илья
- **Приоритет:** обычный
- **Статус:** в работе (второй цикл, ещё не третий — не требует эскалации по правилу «два цикла»)
- **Распоряжение:**
  > Внедрить обработку обратной связи беты по claude/product/ZAPISKI_feedback_beta.md §4: ежедневно забирать новые обращения, разбирать на четыре судьбы, дедуплицировать со счётчиком упоминаний, заводить карточки в бэклоге с идентификаторами обращений, добавить в дек блок «Голос беты» с топ-3 темами недели. Тексты обращений в репозиторий не класть. SLA ответа — два рабочих дня, просрочка в сводку. События формы добавить в analytics/schema/events.yaml.
- **Что сделано:** документ `claude/product/ZAPISKI_feedback_beta.md`, на §4 которого ссылается распоряжение, не найден нигде в дереве `compas-ops` (проверено сплошным поиском) — исполнить как есть нельзя, это не «упёрлось в границу» устава §6.1 (не деньги/бренд/релиз/цена/ПДн), а обычная нехватка данных (`CLAUDE.md §5.1`). При этом в `zapiski` уже есть незамерженная ветка `feature/beta-feedback` с частичной TDD-реализацией той же фичи, заведён `B-260817-07`, чтобы её не потерять. Нужно от учредителя: прислать текст документа или указать, где он лежит. Если документ не появится третьим циклом подряд, распоряжение либо исполняется по восстановленной из ветки логике, либо уходит в `/human` с объяснением.

#### O-260816-04 · Сверить CJM записи с кодом cmpas.ru и реализовать механики A (тихая запись) и…
- **Адресат:** ПРАКТИКА
- **Принято:** 2026-08-16 17:55 UTC от Илья
- **Приоритет:** обычный
- **Статус:** в работе
- **Распоряжение:**
  > Сверить CJM записи с кодом cmpas.ru и реализовать механики A (тихая запись) и B (подбор времени вместо сетки) по product/practice/HANDOFF. За фича-флагами, выключенными по умолчанию, с тестами, расширяющими миграциями. Механику C начать после A и B, механику D не включать до визы Legal. Ветка и PR, деплой не делать.
- **Что сделано:** CJM (`product/practice/CJM_booking_v1.md` v1.1) перепроверен на HEAD `6e96e1b` — без изменений с прошлой сверки. Ночной цикл не пишет код и не открывает ветки/PR в продуктовых репозиториях (`07_REPOS §2`, `01_ROLES_PO §1.4`) — это делает «разработка» отдельным треком. Подготовлены инженерные спецификации, готовые к реализации: `B-260817-01` (механика A) и `B-260817-02` (механика B), обе с фичефлагом, тестами и миграцией, как требует распоряжение. Механика C не специфицирована (по распоряжению — только после A и B). Механика D не специфицирована и не начата (ждёт визы Legal). Попутно найден риск: `products/cmpas.ru/CLAUDE.md` инструктирует автодеплой после каждой задачи, что противоречит «деплой не делать» — заведён `B-260817-04` (P0).

#### O-260816-05 · Разметка Ф0 и Ф1, но под Т-Банк, а не ЮKassa: приём вебхуков NotificationPaym…
- **Адресат:** CPO
- **Принято:** 2026-08-16 17:55 UTC от Илья
- **Приоритет:** обычный
- **Статус:** в работе
- **Распоряжение:**
  > Разметка Ф0 и Ф1, но под Т-Банк, а не ЮKassa: приём вебхуков NotificationPayment с проверкой Token, таблица платежей с полем terminal (сайт и приложение — разные TerminalKey и пароли), разовый импорт реестров, ежедневная сверка по выписке T-API, своя таблица подписок для MRR. Плюс приёмник POST /ingest с валидацией по analytics/schema/events.yaml. RebillId и пароль терминала в аналитику не попадают никогда. Обновить charter/13_TRACKING_PLAN.md под Т-Банк. Ветка и PR, выкат после подтверждённого бэкапа.
- **Что сделано:** код `cmpas.ru` уже использует Т-Банк, не ЮKassa (`prisma/schema.prisma:707`) — распоряжение по факту совпадает с реальностью, `03_SOURCES §1.2` будет поправлен отдельной задачей. Написан `charter/13_TRACKING_PLAN.md` — полная спецификация под все восемь пунктов распоряжения. Заведена инженерная задача `B-260817-06` для разработки+CFO. Реализация (вебхук-приёмник, миграции, приёмник `/ingest`) — код в продуктовом репозитории, ночной цикл его не пишет; выкат прямо требует человека (устав §6.1, «деплой не делать» без подтверждённого бэкапа) и не будет исполнен автоматически ни на каком цикле.

#### O-260816-03 · Завести задачу: отражать бэклог в GitHub Issues. Сначала создать метки P0, P1…
- **Адресат:** CPO
- **Принято:** 2026-08-16 17:18 UTC от Илья
- **Приоритет:** обычный
- **Статус:** в работе
- **Распоряжение:**
  > Завести задачу: отражать бэклог в GitHub Issues. Сначала создать метки P0, P1, P2, P3, практика, записки, моменты, платформа. Правда — state/10_BACKLOG.md, Issues — производная, синхронизация только в одну сторону. На каждую открытую строку бэклога открытый issue с тем же идентификатором в заголовке и метками приоритета и продукта; закрытая строка — закрытый issue. Мои комментарии в issue за сутки переносить в state/15_ORDERS.md как распоряжения и отвечать в том же issue. Обратную синхронизацию не делать. Добавить .zapiski/ в .gitignore.
- **Что сделано:** задача заведена — `B-260817-05` с полной спецификацией (метки, правило зеркала, синхронизация комментариев). `.zapiski/` добавлен в `.gitignore` компас-ops. Метки и issues не созданы: `gh auth status` в этой сессии — не авторизован, а по `H-260813-02` (выполнено) даже штатный токен раннера имеет только `Contents: Read-only`, без прав на Issues — нужен отдельный токен, который эта сессия выдать себе не может (устав §6.1, доступы выдаёт человек). Не заведена как отдельная `/human`-задача — очередь на лимите 5/5, зафиксирована как кандидат первым при освобождении слота.


---

## Исполненные за последние 30 дней

#### O-260816-02 · мое распоряжение нужно выполнить сейчас, а не ночью
- **Адресат:** ПРАКТИКА
- **Принято:** 2026-08-16 11:16 UTC от Илья
- **Приоритет:** обычный
- **Статус:** исполнено
- **Распоряжение:**
  > мое распоряжение нужно выполнить сейчас, а не ночью
- **Что сделано:** внеплановый цикл запущен и доведён до конца в тот же день, 2026-08-16, не дожидаясь расписания 05:00 МСК — исполнение `O-260816-01` ниже.

#### O-260816-01 · нужен идеальный cjm записи на сессию, уникальный для рынка. Это должна быть к…
- **Адресат:** ПРАКТИКА
- **Принято:** 2026-08-16 10:35 UTC от Илья
- **Приоритет:** P0 · срочно
- **Статус:** исполнено
- **Распоряжение:**
  > нужен идеальный cjm записи на сессию, уникальный для рынка. Это должна быть киллер-фича, которой нет на рынке, но она должна быть максимально удобной. Не говори, что у тебя нет доступа к репозиториям, так как я в GitHub доступ к репозиториям прописал
- **Что сделано:** доступ к `compas-psy/cmpas.ru` подтверждён рабочим (закрывает `H-260813-02` в части ПРАКТИКИ). `product/practice/CJM_booking_v1.md` доведён до версии 1.1: снято прежнее ограничение «код недоступен», три киллер-механики (§1.1–1.3) сверены построчно с кодом — что подтвердилось, что нет, с точными файлами и строками (`src/app/bot/book/[psychologistId]/page.tsx`, `src/lib/cron/reminders.ts:79`, `prisma/schema.prisma:707`, `src/lib/client-cancellation.ts`, `src/lib/calendar/google.ts:199`). Готовый кликабельный прототип — `product/practice/booking_prototype.html`. Документ идёт на борд 17.08 как и планировалось; два кода-подтверждённых уточнения (нет клиентского биллинга под удержание, дефолтный текст напоминания раскрывает слово «психолог») внесены в бэклог `B-260816-02`.
