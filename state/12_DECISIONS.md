# Журнал решений

Формат — `04_TEMPLATES §5`. Ведёт CEO-агент. Сюда же сжимаются логи, вышедшие за срок ретенции (устав §8).
Обновлено: 2026-08-24.

---

## 2026

#### D-260824-10 · Находка бухгалтера/CFO о договоре эквайринга Т-Банка становится формальной задачей очереди
- **Заседание:** N3 от 24.08.2026
- **Тип:** 2 (сама постановка задачи; подтверждение договора — тип 1, за учредителем)
- **Контекст:** на N2 бухгалтер поднял находку — нет подтверждения, что договор эквайринга с Т-Банком оформлен под уже идущее боевое использование (платежи ПРАКТИКИ реально проводятся с апреля, минимум шесть висят статусом «pending» без сверки). Находка четыре дня жила сноской в `state/11_HUMAN_QUEUE.md`, не формальной задачей. CFO на N3 назвал её единственным пунктом сегодняшней повестки, где решение не требует данных, которых нет, — только да/нет от учредителя.
- **Решение:** заводится `H-260824-01` по требованиям устава §6.4. Деньги — категория, для которой устав §6.2 делает `/human` автоматическим; очередь и так превышает лимит пятью открытыми задачами по уже принятому прецеденту (`H-260818-02` на N2) — новая задача добавляется тем же основанием, не вытесняя остальные.
- **Отклонённая альтернатива:** оставить сноской до освобождения слота в очереди — отклонена: пока платежи идут боевым образом без подтверждённой договорной базы, цена бездействия не снижается от того, что задача не оформлена формально.
- **Владелец:** учредитель · **Срок:** сегодня, 24.08.2026 · **Критерий готовности:** ответ да/нет зафиксирован в `12_DECISIONS`.
- **Кто возражал:** нет
- **Статус:** принято

#### D-260824-09 · `H-260816-01` уточнена повторно: рекомендация борда — снять промостраницу ЗАПИСОК сегодня, не чинить
- **Заседание:** N3 от 24.08.2026
- **Тип:** 2 (рекомендация; снятие страницы — деплой, тип 1 действие исключительно учредителя по факту исполнения)
- **Контекст:** CMO и Legal независимо перепроверили страницу и код перед N3: `products/zapiski/apps/web/public/promo/index.html` по-прежнему содержит «Мы не можем прочитать ваши заметки. Технически.» и цену 299/249 ₽; при этом код (`server/src/config/env.ts`) сейчас отдаёт 299/**224** ₽ — то есть цель, к которой предполагалась синхронизация страницы, за три недели ни разу не была стабильной точкой, потому что учредитель ещё не подтвердил итоговую цену (`D-260817-04`, тип 1, остаётся открытым). Семь дней попыток «синхронизировать текст с кодом» не помогли, потому что сам код не устоялся.
- **Решение:** борд меняет рекомендацию с «снять либо срочно исправить» на однозначную «снять сегодня». Возврат страницы — только после того, как учредитель подтвердит итоговую цену и Legal/CMO завизируют текст без фразы о нечитаемости.
- **Отклонённая альтернатива:** продолжать чинить текст/цену на живой странице — отклонена: чинить нечего синхронизировать с устойчивым состоянием, а фраза о нечитаемости — самостоятельное нарушение независимо от цены.
- **Владелец:** CMO (текст) + учредитель (снятие, деплой) · **Срок:** сегодня, 24.08.2026 · **Критерий готовности:** страница недоступна публично либо не содержит фразы о нечитаемости и не содержит неподтверждённой цены.
- **Кто возражал:** нет
- **Статус:** принято

#### D-260824-08 · Новые находки ночи усиливают `H-260818-01`: автодеплой на push жив, токен лежит открытым текстом на дисках агентских сред
- **Заседание:** N3 от 24.08.2026
- **Тип:** 2 (обратимое — фиксация находок)
- **Контекст:** CTO проверил `products/cmpas.ru/.github/workflows/deploy-docker.yml` — `job: deploy` по-прежнему триггерится на `push: branches: ["main"]` (строка 129), то есть при отсутствии branch protection любой прямой пуш в `main` (а их сейчас минимум два независимых источника, см. `B-260824-01`) автоматически укатывается на прод без человека. Независимо InfoSec и CTO обнаружили: `PRODUCTS_READ_TOKEN` физически лежит в открытом виде в `.git/config` (в URL `origin`) в локальных клонах `products/cmpas.ru` и `products/compas-voice` — тот же личный admin-PAT, который уже требует ротации по `H-260818-01`, оказался ещё и на диске в открытом виде в каждой среде, где кто-либо клонирует эти репозитории. Текст токена в находке не приводится и никуда не логировался (`CLAUDE.md §5.4`).
- **Решение:** отдельных новых `/human`-задач не заводится — обе находки входят в `H-260818-01` дополнительным доказательством и уточнением объёма работы: ротация токена должна сопровождаться заменой способа его передачи в среды (переменные окружения/credential-helper, не URL remote'а), а решение по branch protection должно учитывать, что автодеплой на push — самостоятельный канал риска, не только мёрж PR.
- **Отклонённая альтернатива:** заводить `B-260822-01` как единственное покрытие риска автодеплоя — отклонена: `B-260822-01` про то, что CI-скрипты сами пушат в `main`; находка про `deploy-docker.yml` — про то, что происходит дальше с любым таким пушем (мгновенный прод), это разные звенья одной цепи.
- **Владелец:** CTO + InfoSec · **Срок:** вместе с `H-260818-01` · **Критерий готовности:** `deploy-docker.yml` не деплоит на голый push без ревью; токен передаётся в среды не через URL remote'а.
- **Кто возражал:** нет
- **Статус:** принято

#### D-260824-07 · `B-260818-04` подтверждена дважды независимым зелёным прогоном тестов — оговорка снята
- **Заседание:** N3 от 24.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** `D-260824-02` (ночной цикл) закрыла находку по коду и тексту PR с явной оговоркой «зелёный прогон тестов не подтверждён в этой сессии». На N3 CTO и CDO независимо друг от друга установили зависимости и прогнали `npx vitest run tests/analytics-ingest.test.ts` в `products/cmpas.ru` — оба получили 28/28 зелёных.
- **Решение:** оговорка `D-260824-02` снимается. `B-260818-04` закрыта устойчиво, на собственном прогоне, не только на чтении кода.
- **Владелец:** CDO + CTO · **Срок:** — · **Критерий готовности:** выполнено этим решением
- **Кто возражал:** нет
- **Статус:** принято

#### D-260824-06 · Промпт трека «реализация» расширен читать P0-пункты бэклога — исполнено этим же заседанием
- **Заседание:** N3 от 24.08.2026
- **Тип:** 2 (обратимое — правка `.runner/prompt-implement.md`, файл compas-ops, не продукта)
- **Контекст:** `D-260823-01` (ночной цикл 23.08) диагностировала: `prompt-implement.md` читает только `state/15_ORDERS.md`, никогда `state/10_BACKLOG.md` — назначенные треку «реализация» P0-пункты бэклога структурно вне области его чтения. Подтверждено фактом 23.08: 20 коммитов в ЗАПИСКИ не тронули ни одной из пяти открытых P0-находок продукта. На N3 CTO принял решение как техническое, не архитектурное — правка на несколько строк.
- **Решение:** `.runner/prompt-implement.md` дополнен: после распоряжений агент открывает `state/10_BACKLOG.md`, берёт пункты со статусом «предложена»/«в работе» и владельцем «трек «реализация»» по приоритету P0 раньше P1, исполняет тем же порядком, что распоряжения, отмечает результат в `10_BACKLOG.md` вместо `15_ORDERS.md`. Правка внесена и закоммичена этим же заседанием (не отдельным PR — файл в `compas-ops`, не в `products/`).
- **Отклонённая альтернатива:** конвертировать P0-пункты бэклога в распоряжения вручную каждый цикл — отклонена: это работало бы, но CPO/CEO тратили бы часть каждого цикла на ручное дублирование того, что уже есть в одном источнике правды (бэклоге).
- **Владелец:** CTO · **Срок:** выполнено 24.08.2026 · **Критерий готовности:** следующий прогон `implement.yml` берёт хотя бы один P0-пункт бэклога с владельцем «трек «реализация»» без ручного дублирования в `15_ORDERS.md` — проверить на следующем цикле.
- **Кто возражал:** нет
- **Статус:** принято

#### D-260824-05 · `B-260824-02` (мост аналитики ЗАПИСКИ→ПРАКТИКА): ретроактивная виза InfoSec и CDO без содержательных возражений
- **Заседание:** N3 от 24.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** мост смёржен без записанного ревью InfoSec до мёржа, вопреки собственному правилу продукта (заголовок `events.yaml` в `zapiski` требует ревью InfoSec на изменение состава/маршрута событий). InfoSec и CDO прочитали код моста (`practiceBridge.ts`, схемы, тесты) перед N3.
- **Решение:** содержательных возражений нет — переданные поля (`length_bucket`, `encrypted`, `query_length_bucket`, `results_count`) не несут текста заметок (проверено тестом `.strict()`-схемы), объём обоснован продуктовой метрикой (контр-метрика NSM ЗАПИСОК), согласие fail-closed с обеих сторон. Вето не накладывается, откат не требуется. Процессное нарушение (мёрж до ревью) фиксируется как факт; правило на будущее подтверждается без исключений: следующее изменение состава событий этого моста идёт к InfoSec до мёржа.
- **Отклонённая альтернатива:** откатить мост до получения формального ревью задним числом — отклонена: содержание уже проверено и соответствует правилам, откат исключительно ради процедуры не снижает риска, только теряет уже смёрженную работу.
- **Владелец:** InfoSec + CDO · **Срок:** — · **Критерий готовности:** выполнено этим решением
- **Кто возражал:** нет
- **Статус:** принято

#### D-260824-04 · `B-260813-10` (tracking plan ПРАКТИКИ): второй пропущенный срок PO — борд даёт явный чек-лист приёмки вместо очередного переноса
- **Заседание:** N3 от 24.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** PO-ПРАКТИКА взял явное обязательство написать документ к 24.08 (по итогам ночного цикла) — проверено на N3: файла в `product/practice/` по-прежнему нет. Это уже второй сорванный срок после возврата CTO на N1. CDO указал, что документ МОМЕНТОВ (принят этим же заседанием, см. ниже) прошёл приёмку с первого раза именно потому, что в нём было соблюдено четыре конкретных условия.
- **Решение:** борд фиксирует те же четыре условия как обязательный чек-лист приёмки для `B-260813-10`, чтобы не повторился третий сорванный срок без объяснения: (1) у каждого события — явный вопрос, на который оно отвечает, не только название; (2) ни одного свободнотекстового или дублирующего поля; (3) любой порог активации помечен гипотезой с планом пересчёта на первой реальной когорте, не выдан за факт; (4) явно указано, что уже реализовано в коде, а что — предложение документа. Новый срок — следующий ночной цикл, 25.08.2026.
- **Отклонённая альтернатива:** снова перенести без явного критерия — отклонена: это уже второй перенос подряд без изменения условий, третий по той же схеме не даст иного результата.
- **Владелец:** PO-ПРАКТИКА (документ) → CTO (приёмка) · **Срок:** 25.08.2026 · **Критерий готовности:** документ принят CTO без возврата на доработку.
- **Кто возражал:** нет
- **Статус:** принято

#### D-260824-03 · `product/moments/D0_TRACKING_PLAN.md` принят CDO — `B-260813-11` закрыта
- **Заседание:** N3 от 24.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** черновик написан PO-МОМЕНТЫ 21.08 (`D-260821-03`), ждал приёмки CTO/CDO три цикла. CDO прочитал документ целиком перед N3.
- **Решение:** документ принят без правок. Девять событий покрывают воронку без лишнего, каждое — со своим вопросом, порог активации «24 часа» явно помечен гипотезой с обязательным пересчётом на первых 200 когортах перед повторным решением борда — учитывает прошлое вето CDO дословно. Отдельно поручается PO-МОМЕНТЫ: строка «Ключевые события» в `05_METRICS §2.3` перечисляет `practice_abandoned`, которого документ сознательно не заводит — привести карточку продукта в соответствие с принятым планом.
- **Владелец:** CDO (приёмка, выполнено) + PO-МОМЕНТЫ (правка `05_METRICS §2.3`) · **Срок:** правка карточки — 25.08.2026 · **Критерий готовности:** выполнено по приёмке; карточка продукта поправлена.
- **Кто возражал:** нет
- **Статус:** принято

#### D-260824-01 · Найден агент, а не только CI-бот, пишущий напрямую в `main` `cmpas.ru` — эскалировано как отдельная, более серьёзная находка
- **Заседание:** решение CPO вне борда, ночной цикл 24.08.2026
- **Тип:** 2 (обратимое — фиксация находки; расследование деплой-цикла и решение о механизме — за CTO)
- **Контекст:** бриф PO-ПРАКТИКА нашёл в свежей истории `cmpas.ru` пять коммитов после мержа `#112` с автором `Claude <noreply@anthropic.com>`, чередующихся с коммитами `simpas-doctor`. CPO проверил напрямую (`git show -s --format='%an <%ae> | parents: %P'` на каждом): все девять коммитов (пять `Claude`, четыре `simpas-doctor`) имеют по одному родителю — прямые пуши, не мёржи. Это не CI-скрипт вроде `db-doctor.yml` (`B-260822-01`), а сам агент, действующий под инструментальной личностью Claude, пишущий в `main` в обход PR.
- **Решение:** заведена `B-260824-01`, P0, вне очереди устава §9. Находка передана как отдельная строка в `H-260818-01` (не растворена в уже существующей формулировке) — она меняет характер риска: барьер между действием агента и продакшеном отсутствует не только процедурно, но и практически прямо сейчас. Первый пункт повестки борда 24.08, наравне с уже открытыми `H-260818-01`/`H-260818-02`.
- **Отклонённая альтернатива:** посчитать это тем же самым, что `B-260822-01`, и не заводить отдельную запись — отклонена: разный субъект (агент, не CI-скрипт) означает разный путь исправления (нельзя просто поправить `.yml`-файл, нужно понять сам деплой-цикл, который это порождает).
- **Владелец:** CTO (расследовать механизм) + InfoSec · **Срок:** борд 24.08.2026 · **Критерий готовности:** механизм объяснён; ни один коммит агента/бота не появляется в `main` без PR.
- **Кто возражал:** нет
- **Статус:** принято

#### D-260824-02 · `B-260818-04` закрыта по коду и тексту PR; зелёный прогон тестов не подтверждён в этой сессии
- **Заседание:** решение CPO вне борда, ночной цикл 24.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** PO-МОМЕНТЫ и CPO независимо подтвердили: `cmpas.ru#112` переводит `identity_linked`/`consent_updated` на список продуктов `[practice, zapiski, moments]` (`analytics/schema/events.yaml:47-58`, проверено CPO напрямую), `compas-voice#4`/`#5` включили транспорт аналитики и поправили два реальных дефекта конверта. PO-МОМЕНТЫ не смогла прогнать `tests/analytics-ingest.test.ts` в своей среде (нет сети на установку зависимостей).
- **Решение:** `B-260818-04` закрывается статусом «сделана» на основании кода и текста PR — тот же стандарт доказательства, что уже применялся ранее к аналогичным находкам этого журнала (напр. `D-260819-02`). Явно фиксируется: закрытие не сопровождается собственным зелёным прогоном тестов в этой сессии; CTO подтверждает прогон на своей стороне при первой возможности, находка не переоткрывается только на этом основании.
- **Отклонённая альтернатива:** держать находку открытой до личного прогона тестов ночным циклом — отклонена: у ночного цикла нет сетевого доступа для установки зависимостей продуктовых репозиториев, требовать этого от каждого закрытия сделало бы почти все находки вечно «предложенными».
- **Владелец:** CDO + CTO · **Срок:** — · **Критерий готовности:** выполнено этим решением; CTO подтверждает тесты по возможности
- **Кто возражал:** нет
- **Статус:** принято

#### D-260823-01 · Найден корень «процессного риска»: трек «реализация» не запускался с 18.08, и даже при запуске не видит бэклог
- **Заседание:** решение CPO вне борда, ночной цикл 23.08.2026
- **Тип:** 2 (обратимое — диагностика и разовый триггер; правка `implement.yml`/`prompt-implement.md` — решает CTO)
- **Контекст:** четыре цикла подряд (19–22.08) CPO эскалировал «трек «реализация» не исполняет назначенные P0 в срок» как необъяснённый процессный риск и просто переназначал сроки (`D-260820-07`→`D-260821-02`, `D-260820-08`→`D-260821-04`). Этим циклом проверено напрямую: `.github/workflows/implement.yml` не имеет расписания (`cron`) — запускается только вручную (`workflow_dispatch`) или пушем в `.runner/implement`. `log/runner/` содержит `2026-08-18_implement.log` как последний прогон; логов за 19–22.08 нет ни одного. `.runner/implement` по-прежнему содержит запись «запуск 2026-08-18 · O-260818-01 и O-260818-02» — с тех пор никто не менял её содержимое, то есть пуш-триггер ни разу не сработал за пять ночей. `tg-orders.yml` (бот распоряжений, опрос раз в 10 минут) тоже не диспатчит `implement.yml` — он только пишет в `state/15_ORDERS.md`. Отдельно: `.runner/prompt-implement.md` инструктирует агента реализации читать только `state/15_ORDERS.md` (распоряжения учредителя со статусом «принято»/«в работе» и указанной веткой) — про `state/10_BACKLOG.md` там нет ни слова. Значит пункты бэклога (`B-260819-02`, `B-260818-04`, `B-260822-01`, `B-260819-01`, `B-260813-01`, `B-260813-08` и другие), которым ночные циклы и борд назначали владельцем «трек «реализация»», структурно не входят в область его чтения — даже если бы он запускался каждую ночь.
- **Решение:** зафиксировано два раздельных технических дефекта, не одна загадка. (1) У `implement.yml` нет расписания — технический факт, правка не требует решения типа 1. (2) Область чтения implement-агента (только заказы) не совпадает с практикой назначения ему пунктов бэклога — тоже не решение типа 1, но нужен явный выбор CTO: расширить `prompt-implement.md` на P0-пункты бэклога, назначенные треку «реализация», либо описать иной путь их исполнения (например, конвертацию в заказы). Этим циклом `.runner/implement` обновлён новой записью — пуш этого коммита запускает прогон implement.yml сегодня же, прерывая пятидневный простой; это не решает пункт (2).
- **Отклонённая альтернатива:** снова переназначить сроки P0-пунктам бэклога без объяснения причины — отклонена: это уже происходило четыре раза подряд и не сработало, потому что назначение адресовалось механизму, который физически не запускался.
- **Владелец:** CTO (правка `implement.yml`/`prompt-implement.md`) · **Срок:** борд 24.08.2026 · **Критерий готовности:** `implement.yml` получает расписание либо иной регулярный триггер; `prompt-implement.md` читает P0-пункты бэклога, назначенные треку «реализация», либо явно описан альтернативный путь их исполнения.
- **Кто возражал:** нет
- **Статус:** принято

#### I-260818-01 · Приём боевых платежей отказывал 18.08: недоверенный корень сертификата Минцифры и неверная стадия сборки образа
- **Обнаружен:** 18.08.2026, диагностика инцидента в `products/cmpas.ru` в тот же день · **Затронуто:** приём платежей ПРАКТИКИ на проде, окно 18.08 (устранено тем же днём)
- **Персональные данные:** нет
- **Что сделано сейчас:** оба симптома устранены 18.08 отдельными PR: `cmpas.ru#107` («Доверие корню Минцифры») добавил корневой сертификат Минцифры в доверенный набор сервера — без него TLS-соединение к `securepay.tinkoff.ru` отклонялось как самоподписанное; `cmpas.ru#104` («Закрепить стадию сборки образа приложения») исправил стадию сборки Docker-образа — контейнер запускал сборщик диагностики вместо Next.js, из-за чего выкладка падала и откатывалась.
- **Что нужно от человека:** ничего — оба фикса уже на `HEAD`, инцидент закрыт технически. Постмортем оформлен постфактум этим циклом (третий цикл подряд без записи, `B-260819-03`).
- **Причина:** установлена 18.08.2026, в день инцидента (см. коммиты `00b90fe`/#107, `ab5b6b6`/#104).
- **Что меняем, чтобы не повторилось:** `B-260817-04`/`B-260818-05` (гигиена CI/сборки образа, сверка окружения раннера и сервера) — уже в бэклоге, отдельная новая задача не заводится, эта находка их не расширяет.
- **Статус:** закрыт (постмортем оформлен с опозданием в три цикла)

#### D-260821-01 · Порог D-260820-06 сработал: H-260818-01 и H-260818-02 не закрыты к циклу 21.08 — отдельная эскалация ⚠️ первой строкой
- **Заседание:** решение CPO вне борда, ночной цикл 21.08.2026
- **Тип:** 2 (обратимое — сама фиксация; закрытие задач — тип 1, за учредителем)
- **Контекст:** `D-260820-06` (борд N2, 20.08) назначил обеим задачам единый срок — «если к следующему циклу (ночной, 21.08 05:00 МСК) ни одна не закрыта, статус переходит в отдельную эскалацию ⚠️ первой строкой сводки». Проверено этим циклом: `PRODUCTS_READ_TOKEN` не ротирован, branch protection не подтверждена, приватный ключ не отозван — `state/11_HUMAN_QUEUE.md` не менялся с 20.08.
- **Решение:** условие наступило. Обе задачи выносятся отдельным сообщением ⚠️ до сводки этого цикла, как и предписано. Новых `/human`-задач не заводится — обе уже в очереди с полным обоснованием.
- **Владелец:** учредитель · **Срок:** сегодня, 21.08.2026 · **Критерий готовности:** токен ротирован, branch protection включена на трёх `products/`, ключ отозван.
- **Кто возражал:** нет
- **Статус:** принято

#### D-260821-02 · D-260820-07 исполняется: подтверждения цены/триала/провайдера ЗАПИСОК в 12_DECISIONS нет — откат коммитов назначен треку «реализация»
- **Заседание:** решение CPO вне борда, ночной цикл 21.08.2026
- **Тип:** 2 (сам откат — обратимое действие, восстанавливающее прежнее известное состояние)
- **Контекст:** `D-260820-07` дала учредителю один цикл на подтверждение «решения от 18.08» дословно в этом журнале, до 21.08 05:00 МСК. Журнал прочитан целиком этим циклом — записи с датой 20 или 21.08 по цене/триалу/провайдеру ЗАПИСОК нет. PO-ЗАПИСКИ независимо подтвердила это тем же поиском.
- **Решение:** по условию `D-260820-07` коммиты `22174b2` (цена 199/149→299/224 ₽) и `91a5459` (Т-Банк третьим провайдером) откатываются треком «реализация» как самовольные изменения. Задача ставится в план дня как P0. Это не решение о цене — цена остаётся открытым вопросом учредителя (`D-260817-04`).
- **Отклонённая альтернатива:** дать ещё один цикл на подтверждение — отклонена: `D-260820-07` не предусматривала продления, это был бы второй перенос того же вопроса.
- **Владелец:** CPO (назначение) + трек «реализация» (исполнение) · **Срок:** сегодня, 21.08.2026 · **Критерий готовности:** коммиты откачены, цена/триал/провайдер возвращены к последнему подтверждённому коду состоянию (199/149 ₽, без Т-Банка).
- **Кто возражал:** нет
- **Статус:** принято

#### D-260821-03 · B-260813-11 (D0-воронка МОМЕНТОВ) — черновик написан PO этим циклом, передаётся на приёмку CTO
- **Заседание:** решение CPO вне борда, ночной цикл 21.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** `B-260813-11` дважды возвращалась CTO на доработку из-за отсутствия документа. Решением CPO от 20.08 PO-МОМЕНТЫ взяла написание черновика на себя следующим циклом.
- **Решение:** черновик `product/moments/D0_TRACKING_PLAN.md` написан и закоммичен этим циклом — девять событий, офлайн-буфер и досылка, определение активации с явной пометкой «гипотеза, требует перепроверки на первых 200 когортах» (вето CDO, `05_METRICS §3.4`). Статус `B-260813-11` меняется с «предложена — четвёртый день без спецификации» на «предложена — черновик готов, ждёт приёмки CTO».
- **Владелец:** CTO (приёмка) · **Срок:** ближайший борд, 24.08.2026 · **Критерий готовности:** CTO принимает документ или возвращает с конкретными правками.
- **Кто возражал:** нет
- **Статус:** принято

#### D-260821-04 · B-260818-04 не закрыта в назначенный срок (20.08) — переназначается треку «реализация» без ожидания борда
- **Заседание:** решение CPO вне борда, ночной цикл 21.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** `D-260820-08` (борд N2) подняла `B-260818-04` до P0 со сроком «сегодня, 20.08», владелец CDO+CTO. Проверено этим циклом напрямую: `products/cmpas.ru/analytics/schema/events.yaml:40-51` по-прежнему закрепляет `identity_linked`/`consent_updated` только за `product: practice`. Срок пропущен без исполнения и без объяснения в бэклоге.
- **Решение:** правка технически проста (реестр допускает несколько продуктов на одно событие или не проверяет `product` для этих двух событий) и не требует решения типа 1 — переназначается треку «реализация» как P0 сегодня, не дожидаясь борда 24.08.
- **Отклонённая альтернатива:** ждать борда 24.08 для повторного назначения — отклонена: задача уже назначена и ничего нового по существу борд не решает, только теряется четыре дня простоя готового кода МОМЕНТОВ.
- **Владелец:** трек «реализация» (правка) · CDO подтверждает корректность на ближайшем борде · **Срок:** сегодня, 21.08.2026 · **Критерий готовности:** оба теста `tests/analytics-ingest.test.ts` зелёные, `analyticsTransportEnabled` можно включать.
- **Кто возражал:** нет
- **Статус:** принято

#### D-260821-05 · Сборка PDF дека починена: причина была не в бренде браузера, а в типе сборки
- **Заседание:** решение CPO вне борда, ночной цикл 21.08.2026
- **Тип:** 2 (обратимое — код инструмента compas-ops, не продукта)
- **Контекст:** `D-260820-09` поручила этому циклу один явный эксперимент по `B-260815-03` (PDF не собирался шесть+ циклов подряд, 13–20.08) с фиксацией результата независимо от исхода. Прежний диагноз в `tools/deck_to_pdf.py` («Chromium печатает, google-chrome — нет») не подтвердился: полная сборка Chromium из кэша Playwright (`chrome-linux64/chrome`, версия 151.0.7922.x — та же, что у `/usr/bin/chromium`) зависла на печати ровно так же, как раньше зависал google-chrome, до таймаута, без файла на выходе. Отдельная headless-only сборка Playwright (`chromium_headless_shell`, бинарь `chrome-headless-shell`) напечатала тот же дек за секунды, без единой строки про dbus/UPower/GCM в логе. Настоящая причина — тип сборки (полный браузер тянет профиль, синхронизацию и push-регистрацию даже с `--disable-sync`/`--disable-background-networking`), а не бренд.
- **Решение:** `tools/deck_to_pdf.py` теперь ищет headless-shell сборку Playwright первой (новый список `BROWSER_GLOBS_HEADLESS_SHELL`, актуальные пути `chrome-headless-shell-linux64/chrome-headless-shell`), и только если её нет — переходит к прежнему порядку (PATH `chromium` → полная сборка Playwright → `google-chrome` как крайний фолбэк). Проверено на этом цикле: PDF дека `daily_2026-08-21.md` собран за секунды (5 страниц, 135.5 КБ) и доставлен учредителю вслед за HTML-фолбэком.
- **Отклонённая альтернатива:** продолжать перебор флагов на полнобраузерных сборках — отклонена: пять с лишним предыдущих попыток (13–20.08) перебирали именно флаги, а не тип сборки, и не продвинулись; сегодняшний эксперимент проверял другую переменную и сразу дал результат.
- **Владелец:** COO · **Срок:** выполнено 21.08.2026 · **Критерий готовности:** PDF собран и отправлен — выполнено; `B-260815-03` закрывается.
- **Кто возражал:** нет
- **Статус:** принято

#### D-260820-04 · O-260817-04 признано исполненным с опозданием; H-260816-01 переформулирована из «публиковать ли» в «снять либо срочно исправить уже опубликованную страницу»
- **Заседание:** N2 от 20.08.2026
- **Тип:** 2 (обратимое — переформулировка задачи и подготовка PR; само снятие/обновление в проде — тип 1, за учредителем)
- **Контекст:** распоряжение `O-260817-04`, адресованное борду (17.08.2026 08:21 UTC), три дня содержит прямой ответ учредителя на пункт деки N1: «H-260816-01 · Публиковать ли промостраницу ЗАПИСОК... — она уже опубликована!». Ни одно решение N1 (`D-260817-01..05`) не отразило это подтверждение; `H-260816-01` в очереди продолжала формулироваться как «решить, публиковать ли», хотя учредитель уже сообщил, что страница живая. Находка сделана независимо ролями CEO, CMO, Legal и InfoSec при подготовке к N2 — редкое совпадение, усиливающее её достоверность.
- **Решение:** `O-260817-04` закрывается статусом «исполнено» задним числом, с явным признанием, что рамка вопроса три дня оставалась неверной. `H-260816-01` переписывается: с «решить, публиковать ли» на «снять либо срочно исправить уже опубликованную страницу с недостоверным текстом о приватности и расходящейся ценой» — категория та же (публикация от лица бренда / юридическое), статус приравнен к активному инциденту, срок — сегодня. Трек «реализация» получает поручение сегодня же открыть PR, убирающий фразу «Мы не можем прочитать ваши заметки. Технически.» и приводящий цену к единственному подтверждённому коду значению (199/149 ₽) на всех живых поверхностях (промостраница, письмо входа, экран входа приложения) — это не решает вопрос об итоговой цене портфеля (990/1990 ₽ в документах против 199/149 ₽ в коде, остаётся за учредителем по `D-260817-04` от N1), но убирает с публичной поверхности третье, никем не подтверждённое число (299/249 ₽). Само обновление или снятие страницы в проде — деплой, действие исключительно учредителя.
- **Отклонённая альтернатива:** оставить `H-260816-01` в прежней формулировке «публиковать ли» — отклонена: реальность обогнала вопрос три дня назад, устаревшая формулировка вводит учредителя в заблуждение о характере решения.
- **Владелец:** CEO (переформулировка очереди) + трек «реализация» (PR по тексту/цене) + учредитель (тип 1, публикация/деплой) · **Срок:** сегодня, 20.08.2026 · **Критерий готовности:** `H-260816-01` переписана; PR с исправлением текста и цены открыт.
- **Кто возражал:** нет
- **Статус:** принято

#### D-260820-05 · Pull request #91 (устранение автодеплоя `cmpas.ru`) не закрывается до включения branch protection на `main`
- **Заседание:** N2 от 20.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** правка, убирающая требование автодеплоя из `CLAUDE.md`, уже присутствует на `HEAD` `cmpas.ru`, но попала туда не через PR #91, а тем же путём прямого пуша бота мимо ревью, который и есть предмет инцидента `H-260818-01`. Закрытие PR #91 как «неактуального» без подтверждённого технического барьера зафиксировало бы обход ревью как норму, а не устранённый риск.
- **Решение:** PR #91 остаётся открытым до включения branch protection с обязательным ревью на `main` `cmpas.ru` (часть `H-260818-01`, п.4). После включения CTO подтверждает и закрывает PR #91 с явной ссылкой на это решение.
- **Владелец:** CTO · **Срок:** по факту закрытия `H-260818-01` · **Критерий готовности:** branch protection включена; PR #91 закрыт с объяснением.
- **Кто возражал:** нет
- **Статус:** принято

#### D-260820-06 · `H-260818-01` и `H-260818-02` — единый неразделимый приоритет сегодня, без исключений по частям
- **Заседание:** N2 от 20.08.2026
- **Тип:** 2 (решение о приоритете; сами задачи — тип 1, решает учредитель)
- **Контекст:** InfoSec и CTO независимо настаивают: ротация токена без branch protection не даёт защиты, а отзыв SSH-ключа не может ждать решения по истории `main`. Обе задачи открыты 1 и 2 суток соответственно, обе неоднократно эскалировались.
- **Решение:** борд подтверждает обе задачи первым и вторым пунктом повестки на сегодня, без исключений и без разбивки на части. Если к следующему циклу (ночной, 21.08 05:00 МСК) ни одна не закрыта — статус переходит в отдельную эскалацию ⚠️ первой строкой сводки, а не остаётся пунктом общего списка.
- **Владелец:** учредитель · **Срок:** сегодня, 20.08.2026 · **Критерий готовности:** токен ротирован и branch protection включена на трёх `products/`; ключ отозван.
- **Кто возражал:** нет
- **Статус:** принято

#### D-260820-07 · `B-260819-02`: цена/триал/провайдер ЗАПИСОК откатываются автоматически, если решение не подтверждено к следующему циклу
- **Заседание:** N2 от 20.08.2026
- **Тип:** 2 (сам откат — обратимое действие агента, восстанавливающее прежнее известное состояние, не новое ценовое решение)
- **Контекст:** код ЗАПИСОК дважды за двое суток менял цену/триал/провайдера со ссылкой на устное «решение учредителя от 18.08», не записанное в `12_DECISIONS`. Это второй подобный инцидент подряд (после расхождения в `B-260813-14`). CPO и CFO сходятся: рост поверхности риска (третий платёжный провайдер) идёт прямо против решения `D-260817-04` («собственная модель биллинга ЗАПИСОК — техдолг, подлежит устранению», а не расширению).
- **Решение:** учредителю даётся один цикл на подтверждение «решения от 18.08» дословно в `12_DECISIONS` (до ночного цикла 21.08 05:00 МСК). Если подтверждения нет — трек «реализация» откатывает коммиты `22174b2` (цена/триал) и `91a5459` (третий провайдер) как самовольные изменения, а не решение борда о новой цене.
- **Отклонённая альтернатива:** оставить как есть до отдельного обсуждения — отклонена: это уже второй цикл расхождения без записи, откладывать далее значит молчаливо принять правило «код меняет цену сам, решение находится потом».
- **Владелец:** CPO (контроль срока) + трек «реализация» (откат при отсутствии подтверждения) · **Срок:** 21.08.2026, ночной цикл · **Критерий готовности:** запись в `12_DECISIONS` существует и совпадает с кодом, либо код откачен.
- **Кто возражал:** нет
- **Статус:** принято

#### D-260820-08 · `B-260818-04` поднята до P0: собственная находка CDO третьи сутки блокирует уже оплаченный код МОМЕНТОВ
- **Заседание:** N2 от 20.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** CDO подтвердил на борде — реестр `identity_linked`/`consent_updated` закреплён только за `product: practice`, из-за чего уже смёрженный транспорт аналитики МОМЕНТОВ (`compas-voice#3`) простаивает под выключенным флагом третьи сутки подряд. CDO сам предложил закрыть находку одним PR.
- **Решение:** `B-260818-04` поднимается с P1 до P0, владелец CDO совместно с CTO, срок — сегодня, трек «реализация».
- **Владелец:** CDO + CTO · **Срок:** 20.08.2026 · **Критерий готовности:** оба падающих теста `tests/analytics-ingest.test.ts` зелёные; флаг `analyticsTransportEnabled` можно включать без риска отклонения событий.
- **Кто возражал:** нет
- **Статус:** принято

#### D-260820-09 · Сборка PDF дека (`B-260815-03`) поднимается до P1 после шести циклов подряд без результата
- **Заседание:** N2 от 20.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** COO — доставка PDF дека не работает шестой цикл подряд (13–20.08); доставка не теряется (откат на HTML), но это прямо противоречит причине, по которой формат PDF был выбран (`D-260813-04`: телефон не листает HTML-вложение предпросмотром).
- **Решение:** `B-260815-03` поднимается с P2 до P1. Следующему циклу поручается один явный эксперимент — другой набор флагов запуска headless-браузера либо другой движок рендера — с фиксацией результата независимо от исхода: если не получилось, явно написать, что именно попробовано и почему не сработало, а не повторять седьмой раз подряд тот же неудачный запуск без изменений.
- **Владелец:** COO · **Срок:** следующий цикл, 21.08.2026 · **Критерий готовности:** PDF собран и отправлен, либо задокументирован новый диагноз, отличный от прежнего.
- **Кто возражал:** нет
- **Статус:** принято

#### D-260820-01 · Порог «тишина данных» пробил восьмые сутки — вопрос остаётся на повестке борда 20.08, не снимается
- **Заседание:** решение CPO вне борда, ночной цикл 20.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** `D-260819-01` (19.08) уже зафиксировала переход порога `05_METRICS §4.5` с P1 на P0 на седьмые сутки и назначила срок «борд 20.08». Ночной цикл 20.08 подтверждает: `13_DATA_INBOX.md` по-прежнему пуст, восьмые сутки подряд (13.08–20.08 включительно). Ночной цикл выполняется раньше борда того же дня (05:00 против 10:00 МСК) — на момент этой записи борд ещё не заседал.
- **Решение:** риск остаётся первым пунктом повестки борда сегодня, как и было назначено вчера. Новой `/human`-задачи не заводится — `H-260813-03` уже покрывает блокер и уже помечена P0 с просроченным сроком.
- **Отклонённая альтернатива:** считать вопрос закрытым констатацией «выносится на борд» без подтверждения на следующий день — отклонена: правило `05_METRICS §4.5` не имеет исключения «уже эскалировано вчера», порог остаётся пробитым, пока данных нет.
- **Владелец:** CEO-агент (контроль вынесения на борд) · **Срок:** борд 20.08.2026, сегодня · **Критерий готовности:** борд явно разобрал риск и либо получил данные, либо назвал следующий шаг с датой.
- **Кто возражал:** нет
- **Статус:** принято

#### D-260820-02 · Закрыть B-260817-07: ветка `feature/beta-feedback` подтверждена слитой, работа не потеряна
- **Заседание:** решение CPO вне борда, ночной цикл 20.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** `B-260817-07` держался открытым с 17.08, потому что предыдущие циклы не могли построчно подтвердить, что merge-коммит `a98026b` (17.08 15:15 UTC) в `zapiski` — это то же дерево, что и незамерженная ветка `feature/beta-feedback`, а не одноимённая по смыслу независимая работа. Ночной цикл 20.08 (бриф PO-ЗАПИСКИ) сравнил напрямую: второй родитель merge-коммита (`77bf91c`) содержит те же файлы (`packages/core/src/feedback/{events,report,prompt}.ts`, `server/src/routes/feedback.ts`), миграция на один номер новее (`0006` вместо `0005`) — объяснимо разрешением конфликта при слиянии, не новой работой.
- **Решение:** `B-260817-07` закрывается статусом «сделана». Работа ветки не потеряна — она уже часть рабочей ветки по умолчанию `zapiski`.
- **Отклонённая альтернатива:** держать задачу открытой до отдельного явного подтверждения CTO на борде — отклонена: построчное сравнение — это ровно то доказательство, которого не хватало, откладывать дальше означало бы требовать подтверждения ради подтверждения.
- **Владелец:** CPO · **Срок:** — · **Критерий готовности:** выполнено этим решением
- **Кто возражал:** нет
- **Статус:** принято

#### D-260820-03 · O-260816-06 разблокирована: документ спецификации найден в репозитории
- **Заседание:** решение CPO вне борда, ночной цикл 20.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** распоряжение `O-260816-06` (адресат ЗАПИСКИ) три цикла подряд числилось заблокированным отсутствием документа `claude/product/ZAPISKI_feedback_beta.md`. Бриф PO-ЗАПИСКИ 20.08 нашёл документ по другому пути — `product/zapiski/FEEDBACK_beta.md`, добавленный в `compas-ops` предыдущим ночным циклом (`838dcd0`, «Ночной цикл 2026-08-19»); содержание совпадает дословно с описанием §4 распоряжения.
- **Решение:** распоряжение больше не заблокировано нехваткой данных, эскалация в `/human` по правилу «третий цикл» не требуется — правило существует для случаев, когда исполнить нечем, а не когда исполнение технически возможно другим треком. Статус `O-260816-06` меняется на «в работе — документ на руках», реализация передаётся треку «реализация».
- **Отклонённая альтернатива:** заводить `/human`-задачу по правилу «третий цикл», раз формальный срок наступил — отклонена: правило существует, чтобы не держать распоряжение в подвешенном состоянии бесконечно из-за нехватки данных; данные уже есть, эскалировать нечего.
- **Владелец:** CPO · **Срок:** — · **Критерий готовности:** выполнено этим решением
- **Кто возражал:** нет
- **Статус:** принято

#### D-260819-01 · Порог «тишина данных» пробит на семь суток — риск выносится на борд 20.08
- **Заседание:** решение CPO вне борда, ночной цикл 19.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** `13_DATA_INBOX.md` не получал ни одной строки с 13.08.2026 — сегодня седьмые сутки подряд. `05_METRICS §4.5` требует на седьмые сутки перехода приоритета с P1 на P0 и вынесения на ближайший борд как риск управления вслепую, а не просто продления срока существующей `/human`-задачи.
- **Решение:** `H-260813-03` пересчитана в очереди как P0; в план дня 19.08 внесена явная эскалация; борд 20.08 обязан рассмотреть это отдельным пунктом, а не строкой в текучке. Новой `/human`-задачи не заводится — существующая уже покрывает блокер, дублировать её значило бы просто занять шестой-седьмой слот очереди тем же вопросом.
- **Отклонённая альтернатива:** оставить приоритет P1 до явного решения борда — отклонена: правило `05_METRICS §4.5` не требует решения борда, чтобы сработать, порог считается пробитым автоматически на седьмые сутки.
- **Владелец:** CEO-агент (вынос на борд) · **Срок:** борд 20.08.2026 · **Критерий готовности:** борд 20.08 явно разобрал риск и либо получил данные, либо назвал следующий шаг.
- **Кто возражал:** нет
- **Статус:** принято

#### D-260819-02 · Реестр приёмника `cmpas.ru` уже знает события МОМЕНТОВ — находка `B-260818-03` закрыта, `B-260818-04` остаётся отдельно
- **Заседание:** решение CPO вне борда, ночной цикл 19.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** бриф PO-МОМЕНТЫ 19.08 повторил вчерашнюю находку `B-260818-03` («реестр не знает событий МОМЕНТОВ»), не заметив, что `compas-psy/cmpas.ru#94` (исполняет `O-260817-17`) уже смёржен 18.08 13:06 МСК и добавил `product: moments` в `events.yaml`. CPO проверил напрямую `grep` по актуальному `HEAD` — события на месте.
- **Решение:** `B-260818-03` закрывается статусом «сделана». Вторая, не пересекающаяся часть исходной находки — `identity_linked`/`consent_updated` закреплены только за `product: practice`, что по-прежнему отвергает анонимные события устройств МОМЕНТОВ/ЗАПИСОК — остаётся открытой как `B-260818-04`, без изменений.
- **Отклонённая альтернатива:** закрыть обе находки разом, раз PR один — отклонена: `B-260818-04` — независимая проблема в другой части того же файла, PR `#94` её не касался.
- **Владелец:** CPO · **Срок:** — · **Критерий готовности:** выполнено этим решением
- **Кто возражал:** нет
- **Статус:** принято

#### D-260818-01 · Ночной цикл не извлекает секреты из git-конфигурации вручную
- **Заседание:** решение CPO вне борда, ночной цикл 18.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** `PRODUCTS_READ_TOKEN` штатно доступен как переменная окружения GH_TOKEN только треку «реализация» (`implement.yml`); ночному циклу (`daily.yml`) он не передаётся — по замыслу ночной цикл не пишет код. Сегодня ночью ночной цикл всё же выполнил один код-фикс (`cmpas.ru#91`, устранение автодеплоя) через извлечение токена из URL `git remote`, прежде чем этот риск был замечен. Второй такой же по типу задаче (восстановление ветки `main` в `zapiski`) было отказано — исполнитель верно сослался на `CLAUDE.md §5.4`: токен, извлечённый в команды и попавший в переписку/лог агента, считается скомпрометированным независимо от безобидности самой операции.
- **Решение:** ночной цикл впредь не извлекает токены из `git remote`/конфигурации для собственных `git`/`gh`-команд. Код-правки в `products/` — либо через трек «реализация» (`implement.yml`, токен там передаётся штатно), либо решением человека. Сегодняшнее использование токена для `cmpas.ru#91` считается разовым инцидентом, а не прецедентом; сам токен считается потенциально скомпрометированным по букве `§5.4` — заведена `H-260818-01` на ротацию.
- **Отклонённая альтернатива:** оставить токен как есть, раз утечка «внутренняя» (осталась в логах агента, не ушла третьей стороне) — отклонена: правило `§5.4` не делает исключения по адресату утечки, только по факту появления в переписке.
- **Владелец:** COO · **Срок:** до ротации токена (см. `H-260818-01`) · **Критерий готовности:** ни один последующий цикл не извлекает токен вручную; трек «реализация» становится единственным каналом кода в `products/`.
- **Кто возражал:** нет
- **Статус:** принято

#### D-260818-02 · Закрыть H-260817-01: право записи в products/ подтверждено рабочим
- **Заседание:** решение CPO вне борда, ночной цикл 18.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** критерий готовности `B-260817-08` («первый тестовый PR агента успешно открывается») выполнен и перевыполнен — за 17.08 в трёх репозиториях `products/` открыто и частично смёржено больше десятка PR, а сегодня ночью тем же токеном открыт `cmpas.ru#91`. Права подтверждены напрямую через GitHub API.
- **Решение:** `H-260817-01` закрывается статусом «выполнена», слот в очереди `/human` (лимит пять) освобождён и немедленно занят новой находкой той же ночи — `H-260818-01` (ротация токена).
- **Владелец:** CEO-агент · **Срок:** — · **Критерий готовности:** выполнено этим решением
- **Кто возражал:** нет
- **Статус:** принято

#### D-260817-01 · Задача из рабочей сессии исполняется немедленно, а не переводится в распоряжение боту
- **Заседание:** решение учредителя от 17.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** учредитель ставил задачи в сессии, а сессия возвращала ему текст, который он должен был сам отправить боту, чтобы задача попала в ночной цикл. Это удваивало действия человека и откладывало результат на сутки.
- **Решение:** введён устав §5.4. Задача из сессии исполняется в этой же сессии тем, к кому обращена, и фиксируется коммитом в тот же день. Бот остаётся асинхронным каналом. Правило требует действующего ключа записи у сессии; отсутствие ключа — инцидент.
- **Владелец:** COO · **Критерий готовности:** ни одна задача из сессии не ушла в бот без прямой просьбы учредителя.
- **Статус:** принято

#### D-260817-02 · Агентам разрешено писать код продуктов в ветке с pull request
- **Заседание:** решение учредителя от 17.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** устав §6.1 и `07_REPOS §2` запрещали агентам писать код продуктов. Из-за этого «реализация» превращалась в спецификацию: механики записи ПРАКТИКИ дважды доводились до готового описания и ни разу до кода.
- **Решение:** агенты пишут код в ветке `feature/<id>` и открывают pull request; тесты перед пушем обязательны; мёрж и деплой остаются исключительным действием учредителя; прямые коммиты в `main` продуктовых репозиториев запрещены; миграции только расширяющие.
- **Отклонённая альтернатива:** отдать реализацию отдельным чатам разработки — отклонена: тогда продуктовый контекст и приоритет живут в одном месте, а код в другом, и они расходятся.
- **Владелец:** CTO · **Критерий готовности:** первый pull request от агента с зелёными тестами.
- **Статус:** принято

#### D-260817-03 · Отчёты пишутся по формату из пяти блоков простым языком
- **Заседание:** решение учредителя от 17.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** сводки читались как внутренняя переписка: идентификаторы задач, жаргон, отчёт о процессе вместо выводов. Учредителю приходилось расшифровывать, вместо того чтобы решать.
- **Решение:** `04_TEMPLATES §10` — СУТЬ, НАХОДКИ таблицей с критичностью, СДЕЛАНО, НУЖНО ОТ ВАС, ПРИЛОЖЕНИЕ. Жаргон только в приложении, одно предложение — один факт, каждая цифра с источником.
- **Владелец:** COO · **Критерий готовности:** учредитель понимает отчёт без уточняющих вопросов.
- **Статус:** принято

#### D-260817-01 · Разрешить агентам писать код продуктовых репозиториев в ветке и открывать PR — правка устав §6.1 и 07_REPOS §2 [CHARTER]
- **Заседание:** N1 от 17.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** распоряжение учредителя `O-260817-01` (P0, срочно) требует разрешить агентам писать код `products/` в ветке `feature/<id-задачи>` и открывать PR, оставив мёрж и деплой за учредителем. PO четвёртый цикл подряд готовят спецификации (`B-260817-01`, `B-260817-02`), которые некому реализовать. CTO указал, что правило «тесты обязательны» неисполнимо буквально: у `cmpas.ru` тестов нет вообще, у `compas-voice` — почти нет. InfoSec не наложил вето при условии secret-скана перед push.
- **Решение:** устав §6.1, абзац о том, что агенты «не выкатывают в прод и не меняют боевую конфигурацию», дополняется: «агент разработки открывает pull request в ветке `feature/<id-задачи>` с прогнанными тестами; мёрж и деплой остаются действием учредителя». `charter/07_REPOS §2` дополняется разделом «Пишут в продуктовые репозитории»: ветка `feature/<id-задачи>`, тесты продукта обязаны пройти локально перед пушем — если у продукта нет теста на затронутый путь, агент обязан написать минимальный тест, а не пропускать проверку; каждый push проходит secret-скан; PR — только через `gh`, без automerge, прямой пуш в `main` невозможен правами токена. Техническая активация (расширение `PRODUCTS_READ_TOKEN` до Contents + Pull requests: write, `issues: write` в permissions `daily.yml`/`board.yml`) не выполнена — выдача токена не может быть сделана агентом (устав §6.1, доступы выдаёт человек), заведена `H-260817-01`.
- **Отклонённая альтернатива:** исполнить распоряжение технически сразу — отклонена: сессия физически не имеет прав писать в `products/`, только устав можно изменить без токена.
- **Владелец:** CTO · **Срок:** правка устава — выполнено сегодня; техническая активация — по факту выдачи токена, целевой срок 19.08.2026 · **Критерий готовности:** устав §6.1 и `07_REPOS §2` содержат новый текст (выполнено); первый PR агента в `products/` проходит по новым правилам.
- **Кто возражал:** нет вето; CTO предупредил о неисполнимости буквального требования тестов для `cmpas.ru` — учтено формулировкой «минимальный тест на затронутый путь».
- **Статус:** принято · техническая активация в работе

#### D-260817-02 · Первый PR с правом записи в products/ — устранить автодеплой из CLAUDE.md ПРАКТИКИ, затем восстановить main в ЗАПИСКАХ
- **Заседание:** N1 от 17.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** `products/cmpas.ru/CLAUDE.md:5` прямо инструктирует «после каждой задачи всегда делать деплой: смёрджить PR в main самостоятельно» — прямое нарушение устава §6.1, подтверждено CTO построчно (`deploy-docker.yml` реально запускает деплой на push в `main`). В `zapiski` нет ветки `main` — открывать PR по `D-260817-01` физически некуда.
- **Решение:** как только `PRODUCTS_READ_TOKEN` активирован (`H-260817-01`), первый PR любого агента-разработчика — правка `products/cmpas.ru/CLAUDE.md` (снять требование автодеплоя, заменить на «ветка + PR, деплой — по распоряжению учредителя»), закрывает `B-260817-04`. Второй шаг — создать ветку `main` в `zapiski` от текущего HEAD (`c529b14`) без новых коммитов и включить процесс ревью, закрывает `B-260815-02`. Только после этого — фичи (`B-260817-01`, `B-260817-02`).
- **Отклонённая альтернатива:** начинать сразу с фич — отклонена: агент с правом пуша, следующий текущей инструкции `CLAUDE.md` буквально, автоматически смержит и задеплоит в прод без человека — риск неавторизованного релиза выше пользы от фичи, сделанной на день раньше.
- **Владелец:** CTO · **Срок:** первый цикл после активации токена · **Критерий готовности:** `products/cmpas.ru/CLAUDE.md` не требует автодеплоя; ветка `main` существует в `zapiski`.
- **Кто возражал:** —
- **Статус:** принято

#### D-260817-03 · Подтвердить: МОМЕНТЫ остаются бесплатными, платный экран и цена не публикуются
- **Заседание:** N1 от 17.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** `B-260816-03` — пейволл 399 ₽/мес существует в коде `compas-voice` (флаг выключен) и на статической странице `products/cmpas.ru/sites/moments.cmpas.ru` с отдельным деплоем; CMO лично подтвердил текст и цену на строках 66, 176, 180. Это расходится с действующей моделью портфеля (`05_METRICS §2.3`: «МОМЕНТЫ не монетизируются напрямую»).
- **Решение:** действующая модель подтверждена без изменений — МОМЕНТЫ бесплатны, вход в экосистему. Флаг гейтинга остаётся выключенным. Страница `sites/moments.cmpas.ru` с ценой 399 ₽/мес не публикуется до отдельного решения борда с участием CFO о пересмотре модели портфеля.
- **Отклонённая альтернатива:** включить пейволл сейчас, раз код уже готов — отклонена: это смена стратегии портфеля (устав §1, единый биллинг), не обсуждалась и не была решением, а просто осталась в коде.
- **Владелец:** CPO · **Срок:** — (действует до отдельного пересмотра) · **Критерий готовности:** флаг гейтинга выключен, страница с ценой недоступна публично либо не содержит цены.
- **Кто возражал:** —
- **Статус:** принято

#### D-260817-04 · ЗАПИСКИ обязаны прийти к единому аккаунту и биллингу портфеля; итоговая цена — решение учредителя
- **Заседание:** N1 от 17.08.2026
- **Тип:** 2 (обратимое, кроме итоговой цены — тип 1)
- **Контекст:** `B-260813-14` — в коде ЗАПИСКИ работают как отдельный продукт со своим биллингом ЮKassa (199/149 ₽, `BILLING_ENABLED=false`), в документах — 990/1990 ₽ как часть тарифов ПРАКТИКИ, на промостранице — третья цена, 299/249 ₽. Устав §1 уже фиксирует принцип: «три продукта делят одного пользователя, один аккаунт... один биллинг» — это не новый вопрос, а действующее правило, которому код ЗАПИСОК не соответствует.
- **Решение:** борд подтверждает применимость устава §1 к ЗАПИСКАМ явно: собственная модель биллинга ЗАПИСОК — технический долг, подлежит устранению при реализации биллинг-трекинга (`B-260817-06`). Итоговая цена (документы называют 990/1990 ₽, но это не проверено учредителем как решение) — тип 1, решает учредитель; вопрос уже покрыт `H-260816-01` (промостраница не публикуется, пока цена не подтверждена).
- **Отклонённая альтернатива:** оставить три параллельные модели до отдельного продуктового решения — отклонена: устав уже отвечает на архитектурный вопрос, откладывать нечего, открыт только вопрос цены.
- **Владелец:** CPO + CTO · **Срок:** при реализации `B-260817-06` · **Критерий готовности:** один `account_id`, один биллинг на весь портфель для ЗАПИСОК.
- **Кто возражал:** —
- **Статус:** принято

#### D-260817-05 · Приватность МОМЕНТОВ: чек-ин самочувствия возвращается к записи только по касанию
- **Заседание:** N1 от 17.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** `B-260817-03` (P0) — с коммита `fc068b6` приложение пишет чек-ин самочувствия (0–10) без действия пользователя; InfoSec подтвердил лично в коде (`AftercareScreen.kt:44,76`); три документа продукта (`PRODUCT.md`, `QA-TESTPLAN.md`, `PRIVACY-DPO.md`) прямо запрещают текущее поведение, `PRIVACY-DPO.md` сам называет это потенциально данными о здоровье. Legal наложил вето на дефолтное поведение до визы InfoSec; InfoSec визу не даёт — обоснования для дефолта «5» нет.
- **Решение:** дефолт возвращается к записи только по касанию, как в `PRODUCT.md`. Расширение сбора задним числом не визируется. Исполнение — первой волной PR после активации токена (`H-260817-01`), приоритет выше любых фич, наравне с `B-260817-04`.
- **Отклонённая альтернатива:** завизировать расширение сбора задним числом — отклонена и InfoSec, и Legal: нет обоснования, зачем нужен именно этот дефолт, а визирование постфактум закрепляет нарушение процесса (`01_ROLES_PO §1.5` — фичи, расширяющие сбор данных, идут к InfoSec до мерджа, не после).
- **Владелец:** разработка + InfoSec · **Срок:** первая волна PR после активации токена · **Критерий готовности:** дефолт чек-ина совпадает с `PRODUCT.md`.
- **Кто возражал:** —
- **Статус:** принято

#### D-260815-01 · Переименовать компанию и операционную систему в СИМПАС
- **Заседание:** решение учредителя от 15.08.2026
- **Тип:** 1 (учредитель)
- **Контекст:** устав и документы были написаны под именем ОКОЁМ, живая проба этого имени не проводилась. Учредитель принял имя СИМПАС для бизнеса и операционной системы.
- **Решение:** СИМПАС — имя компании и операционной системы, применено во всех документах устава. Публичные имена продуктов (ПРАКТИКА, ЗАПИСКИ, МОМЕНТЫ) не меняются этим решением.
- **Возражение агента, отклонённое учредителем:** «СИМПАС» и «КОМПАС» различаются одной буквой, сходство знаков оценивается на слух и по смыслу, правообладатель «КОМПАС» уже прекращал охрану сходных обозначений в классах 09 и 42. Учредитель принял риск на себя явно и закрыл обсуждение. К вопросу не возвращаться.
- **Владелец:** CEO · **Срок:** — · **Критерий готовности:** имя ОКОЁМ отсутствует в документах устава.
- **Статус:** принято

#### D-260815-02 · Перенести оба ритма на раннер GitHub Actions
- **Заседание:** решение вне борда от 15.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** `D-260813-03` назначил памятью репозиторий, но расписания остались в облаке и продолжили писать в проект claude.ai: ночной цикл 14.08 отработал впустую, его результата нет в репозитории, деки лежат недоставленными. Причина структурная: облачная сессия читает GitHub, но не пишет в него, и ей негде хранить секреты.
- **Решение:** ночной цикл (`daily.yml`, 02:00 UTC) и борд (`board.yml`, 07:00 UTC пн и чт) выполняются на раннере GitHub Actions. Раннер даёт одновременно сеть, право записи в репозиторий и хранилище секретов — ни одна другая среда не даёт всех трёх. Доставка выполняется изнутри цикла через `tools/deliver.py`, PDF собирается на раннере (chromium ставится шагом воркфлоу). Прогон, не изменивший ни одного файла, считается неуспешным и падает с ошибкой. Облачные расписания Cowork выключаются.
- **Отклонённая альтернатива:** оставить ритм в Cowork и дать сессии токен записи в репозиторий — отклонена: токен пришлось бы хранить в тексте расписания.
- **Владелец:** COO · **Срок:** 18.08.2026 · **Критерий готовности:** три ночных цикла подряд отработали на раннере, коммит и доставка прошли.
- **Статус:** принято · уточняет `D-260813-03`, отменяет часть `D-260813-08` в части сборки PDF

#### D-260815-03 · Ввести роль CDO и строить аналитику с нуля
- **Заседание:** решение учредителя от 15.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** разметки событий нет ни в одном продукте, ни один из 39 порогов алертов не проверяем, подключён единственный источник, не покрывающий продуктовых метрик. Ответственного за это не было: CPO отвечает за приоритет, CTO за ёмкость, а за существование данных — никто.
- **Решение:** ввести CDO десятой ролью борда с вето на задачу без описанного способа измерения. План работ — `charter/12_ANALYTICS.md`: сбор с сервера, единый `account_id`, PostgreSQL и Metabase на своей инфраструктуре, шесть фаз от биллинга до алертов, обязательный ежедневный отчёт качества данных. Правило: метрика существует, только если у неё есть SQL в репозитории.
- **Отклонённая альтернатива:** внешние SaaS-аналитики — отклонены: выгрузка поведенческих данных психологов на чужую инфраструктуру несовместима с мандатом InfoSec.
- **Владелец:** CDO · **Срок:** Ф0 — 7 дней после доступа к биллингу · **Критерий готовности:** деньги считаются из данных, а не оцениваются.
- **Статус:** принято

#### D-260815-04 · Ограничить набор скилов тремя и записать причины отказов
- **Заседание:** решение вне борда от 15.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** запрошено восемь скилов. Часть из них дублирует друг друга по процессу, часть увеличивает расход контекста вместо экономии, один требует выноса операционной памяти на внешний сервис.
- **Решение:** ставим `markitdown`, `graphify`, `security-guidance`; `superpowers` и `impeccable` уже установлены. Отклонены: `supermemory` (внешний SaaS, дублирует репозиторий как память), `Spec Kit` (конфликтует с планированием superpowers, рассчитан на команду), массовая установка из каталога awesome-claude-skills (раздувает контекст каждой сессии). Разграничение зон — `charter/11_SKILLS.md`.
- **Владелец:** CTO · **Срок:** — · **Критерий готовности:** три скила установлены на раннере и в Claude Code, проверка пройдена.
- **Статус:** принято

#### D-260815-05 · Разовое исключение: токен записи передан агенту через переписку
- **Заседание:** решение учредителя от 15.08.2026
- **Тип:** 1 (учредитель)
- **Контекст:** `CLAUDE.md §5.4` запрещает передачу секретов через переписку. Настройка ритма требовала коммита в репозиторий, а у облачной сессии права записи нет; учредитель потребовал выполнить работу без своего участия.
- **Решение:** выпущен тонкий токен GitHub только на репозиторий `compas-ops`, права Contents и Workflows, срок 7 дней. Использован для одного набора коммитов 15.08.2026. Отзывается учредителем сразу после завершения работ.
- **Владелец:** InfoSec · **Срок:** 15.08.2026 · **Критерий готовности:** токен отозван на странице токенов GitHub.
- **Статус:** принято · исключение, прецедентом не является

#### D-260813-01 · Развернуть агентскую орг-структуру с ночным циклом и бордом C-1
- **Заседание:** учредительное решение от 13.08.2026 (до первого борда)
- **Тип:** 1 (учредитель)
- **Контекст:** три продукта — ПРАКТИКА, ЗАПИСКИ, МОМЕНТЫ — развивались без общего приоритета и без регулярного контроля исполнения. Данные, обращения пользователей и планы существовали, но не сходились в одном месте и не превращались в дневные решения.
- **Решение:** ввести product-owner агентов по каждому продукту с единым CPO, ночной цикл в 05:00 МСК и борд C-1 по понедельникам и четвергам в 10:00 МСК; всё, что нельзя автоматизировать, оформлять как `/human` задачи учредителю по требованиям устава §6.4; результаты борда доставлять слайдами в Telegram.
- **Отклонённая альтернатива:** отдельный агент-ассистент без орг-структуры — отклонена, потому что не даёт ни контроля исполнения, ни единого приоритета между продуктами, а именно они и были проблемой.
- **Владелец:** CEO-агент · **Срок:** первый борд 17.08.2026 · **Критерий готовности:** три ночных цикла и один борд прошли, артефакты дошли до учредителя.
- **Кто возражал:** —
- **Статус:** принято

#### D-260813-02 · Хранить операционную память в проекте, а репозиторий держать зеркалом
- **Заседание:** учредительное решение от 13.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** каждый запуск по расписанию стартует в чистом контейнере — локальные файлы не переживают сессию. Доступ к репозиториям в момент решения не подтверждён, доступ к проекту есть всегда.
- **Решение:** первичное хранилище — документы проекта `claude/ops/`; `compas-ops` зеркалит их и даёт историю; при расхождении прав проект.
- **Отклонённая альтернатива:** хранить всё только в git — отклонена, потому что при отказе доступа к репозиторию система теряет память целиком, а не историю.
- **Владелец:** COO · **Срок:** пересмотреть после закрытия `H-260813-02` · **Критерий готовности:** три ночных цикла подряд без потери состояния.
- **Статус:** принято

#### D-260813-03 · Перенести операционную память в репозиторий, проект оставить историей
- **Заседание:** решение вне борда от 13.08.2026, принято при передаче ритма из Cowork
- **Тип:** 2 (обратимое)
- **Контекст:** `D-260813-02` назначил первичным хранилищем документы проекта claude.ai, потому что доступ к репозиторию не был подтверждён. Причина отказа выяснилась: репозиторий `compas-ops` был пуст — ноль коммитов и ноль веток, отсюда `409` на любой запрос содержимого и невозможность записи. Это не отказ прав, а отсутствие точки, от которой можно оттолкнуться. С первым коммитом ограничение снято, а два живых хранилища гарантированно разъезжаются: `HANDOFF §7` отводил им неделю.
- **Решение:** первичное хранилище — репозиторий `compas-psy/compas-ops`, каталоги `charter/`, `state/`, `log/`. Проект claude.ai перестаёт быть памятью. Промпты обоих запусков переписаны на чтение и запись репозитория (`08_SCHEDULES` версия 2). Копии исходников скриптов из `06_DELIVERY §8` убраны: носитель кода теперь один — `tools/`.
- **Отклонённая альтернатива:** оставить память в проекте и синхронизировать её с репозиторием после каждой правки — отклонена: синхронизация вручную держится на дисциплине и отказывает первой же занятой ночью, а расхождение памяти обнаруживается не в момент поломки, а через несколько циклов, когда на разошедшихся данных уже приняты решения.
- **Владелец:** COO · **Срок:** 20.08.2026 · **Критерий готовности:** три ночных цикла подряд отработали, читая и записывая только репозиторий.
- **Кто возражал:** —
- **Статус:** принято · отменяет `D-260813-02`

#### D-260813-04 · Присылать текст и готовую презентацию в обоих ритмах
- **Заседание:** решение вне борда от 13.08.2026, по требованию учредителя
- **Тип:** 2 (обратимое)
- **Контекст:** ночной цикл присылал только сводку в 15 строк, дек был лишь у борда. Сводка отвечает на вопрос «что случилось», но не удерживает доказательства: через два дня решение, принятое ночью, нечем проверить. Отдельно выяснилось, что HTML-дек, уходивший файлом, телефон не показывает предпросмотром — учредителю приходилось скачивать вложение и искать, чем его открыть.
- **Решение:** оба ритма присылают текст и презентацию. Формат презентации — PDF: Telegram листает его прямо в переписке. HTML идёт следом только для борда, где дек потом открывают на большом экране. Порядок фиксирован: сначала текст, потом файл. Собрано в одну команду `tools/deliver.py`, чтобы шаг нельзя было пропустить; дек ночного цикла — 4–6 слайдов по шаблону `04_TEMPLATES §6.1`.
- **Отклонённая альтернатива:** присылать дек картинками по слайду — отклонена: шесть картинок засоряют переписку сильнее, чем один файл, и в них нельзя искать текстом.
- **Владелец:** COO · **Срок:** первый цикл после включения канала · **Критерий готовности:** учредитель получил текст и PDF в ночном цикле и в борде.
- **Кто возражал:** —
- **Статус:** принято

#### D-260813-05 · Доставлять через GitHub Actions, не полагаясь на сеть среды
- **Заседание:** решение вне борда от 13.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** `api.telegram.org` недостижим из облачной среды агента — сеть ограничена списком доменов, и запрос к Bot API получает `403` от прокси, не доходя до Telegram. Проверено прямым вызовом. Симптом неотличим от отозванного токена, если смотреть только на факт «бот молчит», и это увело диагностику в сторону смены токена.
- **Решение:** основной маршрут доставки — воркфлоу `tg-relay.yml`: агент коммитит артефакты, отправляет раннер GitHub, у которого интернет открыт. Токен живёт в секретах репозитория и в среду агента не попадает вовсе. Прямая отправка из среды остаётся допустимой, если домен разрешён, — агент пробует её первой и переходит к воркфлоу сам. Диагностика различает случаи: `tools/tg_doctor.py`.
- **Отклонённая альтернатива:** разрешить домен в сетевых настройках среды и отправлять только напрямую — отклонена как единственный маршрут: он требует хранить токен в переменных среды, которые не являются секретом и видны всем пользователям среды, и ломается при любом изменении сетевой политики. Как дополнительный маршрут разрешён.
- **Владелец:** COO · **Срок:** 14.08.2026 · **Критерий готовности:** «Telegram · диагностика» доставила сообщение и PDF.
- **Кто возражал:** —
- **Статус:** принято

#### D-260813-06 · Принимать распоряжения учредителя через Telegram-бота
- **Заседание:** решение вне борда от 13.08.2026, по требованию учредителя
- **Тип:** 2 (обратимое)
- **Контекст:** учредитель мог влиять на систему только через задачи в очереди, которые заводили сами агенты, и через правку документов. Прямого способа сказать «сделайте вот это» не было: указание в переписке сессии умирало вместе с сессией.
- **Решение:** бот принимает команды `/борд`, `/цпо`, `/практика`, `/записки`, `/моменты`; сообщение становится распоряжением в `state/15_ORDERS.md`, и это первое, что читает любой запуск — раньше бэклога. Слово «срочно» первым в тексте даёт P0 и вытесняет план дня. Приём идёт опросом из GitHub Actions каждые десять минут (`tg-orders.yml`), потому что вебхук требует постоянного адреса, которого у системы нет. Распоряжение не отменяет устав §6.1: упирающееся в границу получает статус «упёрлось в границу» и возвращается задачей учредителю с вариантами.
- **Отклонённая альтернатива:** принимать распоряжения свободным текстом без команды — отклонена: бот публичен по имени, и без явной команды и проверки чата распоряжением станет любое сообщение постороннего.
- **Владелец:** COO · **Срок:** 14.08.2026 · **Критерий готовности:** распоряжение, отданное из Telegram, исполнено ближайшим циклом и помечено «исполнено».
- **Кто возражал:** —
- **Статус:** принято

#### D-260813-07 · Подчинить деки стандарту содержания и проверять его машиной
- **Заседание:** решение вне борда от 13.08.2026, по требованию учредителя
- **Тип:** 2 (обратимое)
- **Контекст:** дек нулевого цикла был декоративен там, где должен был быть доказательным: заголовки называли темы («Метрики», «Риски»), обсуждение ролей пересказывалось фактом встречи, а не содержанием. По такому деку нельзя принять решение, не задав пять уточняющих вопросов.
- **Решение:** введён `10_DECK_STYLE` — пирамида с ответом первым, заголовки-тезисы с глаголом и числом, каждое утверждение с источником или пометкой «данных нет», каждое предложение с владельцем, сроком, критерием готовности и ценой бездействия, последний слайд с решениями и командой бота для ответа. Язык: короткие фразы, активный залог, запрет оборотов-пустышек. Соблюдение проверяет `tools/deck_lint.py`, его запускает `deliver.py` до отправки.
- **Отклонённая альтернатива:** ограничиться указанием «пиши хорошо» в промпте — отклонена: требование без проверки исполняется ровно до первой занятой ночи. Автоматическая переписка текста тоже отклонена: дек, исправленный автозаменой слов, остаётся деком без мысли.
- **Владелец:** CEO · **Срок:** первый борд 17.08.2026 · **Критерий готовности:** дек борда проходит `deck_lint` без ошибок и содержит числа с источниками.
- **Кто возражал:** —
- **Статус:** принято

#### D-260813-08 · Собирать презентацию у агента, а не на раннере GitHub
- **Заседание:** решение вне борда от 13.08.2026
- **Тип:** 2 (обратимое)
- **Контекст:** пять прогонов подряд сборка PDF на раннере GitHub упиралась в таймаут. И `google-chrome`, и `chromium` из PATH запускаются, пишут в лог про отсутствие dbus и неудачную регистрацию в GCM, замолкают и файла не создают. Тот же дек тем же кодом Chromium в средах агентов печатает за секунду. Перебор флагов, смена способа ожидания и перенос лога из пайпа в файл ничего не изменили.
- **Решение:** PDF собирает тот, у кого работает браузер, — агент у себя. Раннер только отправляет готовый файл: `deliver.py --pdf`, вход `pdf` у `tg-relay.yml`, `outbox/*.pdf` исключён из `.gitignore`. Браузер как зависимость CI убран совсем.
- **Отклонённая альтернатива:** продолжать чинить браузер на раннере — отклонена: пять попыток по минуте каждая не дали ни одного признака приближения к решению, а цена проблемы (дек уходит HTML вместо PDF) ниже цены дальнейшего перебора.
- **Владелец:** COO · **Срок:** выполнено 13.08.2026 · **Критерий готовности:** диагностика доставила текст и PDF за девять секунд.
- **Кто возражал:** —
- **Статус:** выполнено

---

## Архив сжатых логов

_пусто_
