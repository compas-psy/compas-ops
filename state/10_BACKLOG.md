# Портфельный бэклог

Ведёт CPO. Правила приоритизации — устав §9, формат — `04_TEMPLATES §3`.
Обновлено: 2026-08-21, ночной цикл.

**P0: 15 · P1: 11 · P2: 5 · P3: 0** (`B-260826-01` поднята с P1 до P0 этим циклом — см. запись 27.08 ниже)
Обновлено: 2026-08-24, борд N3. CTO подтвердил `B-260824-01` живьём (`git log`) и нашёл следующее звено той же цепи — `deploy-docker.yml` деплоит на любой push, не только на PR-мёрж (`B-260824-03`, новая). `B-260818-04` закрыта устойчиво — подтверждена дважды независимым зелёным прогоном тестов (CTO и CDO), не только чтением кода. `B-260813-11` (D0-воронка МОМЕНТОВ) принята CDO без правок и закрыта. `B-260813-10` (tracking plan ПРАКТИКИ) — второй сорванный срок PO, борд дал явный чек-лист приёмки и новый срок 25.08. Промпт `.runner/prompt-implement.md` расширен на этом же заседании читать P0-бэклог трека «реализация» (`D-260823-01`/`D-260824-06`), не только распоряжения.
Обновлено: 2026-08-25, ночной цикл. Ни один продуктовый репозиторий не сдвинулся за сутки (`cmpas.ru` на `f92fa99`, `zapiski` на `cb03996`, `compas-voice` на `3502275`) — трек «реализация» не запускался: `log/runner/` не содержит прогона за 24→25.08, `.runner/implement` не обновлялся с 23.08, а `implement.yml` по-прежнему не имеет `cron` (проверено `grep`) — вчерашняя правка `D-260824-06` закрыла только чтение бэклога промптом, саму проблему расписания (первую половину диагноза `D-260823-01`) CTO не тронул. Все P0-пункты ниже — без движения. Новое: тот же паттерн прямых пушей в `main`/автопубликации на push, что уже P0 в `cmpas.ru`, подтверждён и в `compas-voice` (`B-260824-03` расширена). `B-260813-10` (tracking plan ПРАКТИКИ) — третий подряд сорванный срок, включая явный чек-лист борда вчера. `charter/05_METRICS.md §2.3` приведена в соответствие с принятым `D0_TRACKING_PLAN.md` (поручение `D-260824-03`, исполнено PO-МОМЕНТЫ).
Обновлено: 2026-08-26, ночной цикл. **P1: 11 → 12** (новая `B-260826-01`). Третья ночь подряд (24→25→26.08) без единого коммита ни в одном из трёх продуктов — `cmpas.ru` на `f92fa99`, `zapiski` на `cb03996`, `compas-voice` на `3502275`, все три независимо подтверждены свежим `git fetch`. `implement.yml` по-прежнему без `cron` (`grep` пусто), `.runner/implement` не менялся с 23.08 — корневая причина (`B-260823-01`) не устранена. Новое: (1) разбивка авторов прямых коммитов в `main` `compas-voice` показала личность «Eliah Martynov» (9 коммитов) наравне с `Claude`/`compas-psy`/`simpas-agent` — заведено `B-260826-01`, нужно подтверждение CTO/InfoSec, человек это или агент; (2) PO-ПРАКТИКА при рутинной проверке `git remote -v` в своей среде увидел токен `PRODUCTS_READ_TOKEN` в открытом виде в URL — токен не воспроизведён повторно, URL сразу очищен, инцидент усиливает `H-260818-01`, отдельной `/human`-задачи не создаёт. `B-260813-10` (tracking plan ПРАКТИКИ) — четвёртый сорванный срок подряд.
Обновлено: 2026-08-27, ночной цикл. **Диагноз `B-260823-01` требует пересмотра, не подтверждения**: за ночь 26.08 все три продукта реально сдвинулись — `cmpas.ru` +13 коммитов, `zapiski` +7, `compas-voice` +1 (`git log <прежний HEAD>..HEAD` в каждом репозитории этим циклом) — трек «реализация» физически работал. Но ни `log/runner/2026-08-26_implement.log`, ни `.runner/implement` (последняя запись по-прежнему «запуск 2026-08-23») этого не отражают, и `implement.yml` всё ещё без `cron`. Значит эти 21 коммит появились не через путь, который `compas-ops` умеет логировать, — исполнение идёт неаудируемым отсюда механизмом; это не отменяет находку про отсутствие расписания, а добавляет более серьёзную. Все 21 коммит — прямые пуши (один родитель), без единого PR, во всех трёх продуктах. В `zapiski` это привело к содержательному нарушению явного решения борда `D-260824-09` («снять промостраницу сегодня, не чинить») — вместо снятия текст переписан на живой странице, а сама страница расширена на корень домена, см. `H-260816-01`. В `compas-voice` очередной такой коммит (`c1d3df8`, автор «Eliah Martynov», десятый по счёту) задел `android/**`, что по конфигурации `android-build.yml` должно было автоматически опубликовать релиз без единого шага ревью — конкретное, не только теоретическое проявление `B-260824-03`; commit message этого коммита ссылается на несуществующий `B-260823` — усиливает подозрение `B-260826-01`, которая поднимается с P1 до P0. `B-260813-10` — пятый сорванный срок подряд.

---

#### B-260818-05 · Убрать приватный ключ и мусорные файлы из `HEAD` `cmpas.ru`; найти, что заменило историю `main`
- **Продукт:** платформа (ПРАКТИКА, репозиторий) · **Приоритет:** P0 (утечка секрета в публичный репозиторий, вне очереди устава §9)
- **Источник:** найдено в этом прогоне при подготовке к исполнению распоряжений в `products/cmpas.ru`; см. `H-260818-02` для полной находки и решения, которые нужны от учредителя.
- **Текущее поведение:** `main` содержит закоммиченный приватный SSH-ключ (`antigravity_key`/`.pub`) и мусорные артефакты чужой рабочей директории (`_tmp_migrate.sql`, `fix_schema.sql`, `migration.sql`, `how f8c8291`, файл с обрубленным именем) — все с корневого коммита `bfdc2eeb`, который является корнем всей видимой истории `main` (20 коммитов, история до него не читается).
- **Желаемое поведение:** ветка/PR этого прогона убирает перечисленные файлы из `HEAD` (не переписывая историю — это отдельное необратимое решение за учредителем, `H-260818-02` п.3). Отдельно от кода: CTO/InfoSec выясняют, каким процессом заменена история `main` вокруг 2026-08-17 13:30–13:40 UTC под личностью `simpas-doctor`, и закрывают возможность повторной замены (права, кто может force-push в `main`, ревью на защищённой ветке — пересекается с `B-260818-02`/`H-260818-01`, тот же класс риска: процесс, оставляющий след, неотличимый от действия человека).
- **Критерий готовности:** файлы отсутствуют в `HEAD`; учредитель принял решение по п.3 `H-260818-02`; CTO подтвердил или опроверг причину замены истории.
- **Метрика проверки:** нет — безопасность, не метрика
- **Владелец:** разработка (этот прогон, часть) + CTO/InfoSec (расследование)
- **Эффект на соседние продукты:** нет напрямую, но тот же класс риска (ботовская личность = неотличимое от человека действие) уже отмечен для `zapiski` (`B-260818-02`) — платформенный паттерн, не изолированный инцидент
- **Обновлено 19.08 (ночной цикл):** файловая часть закрыта — `compas-psy/cmpas.ru#106` «Убрать утёкший приватный ключ и мусор из HEAD» смёржен 18.08 15:44 МСК; `git ls-files` на актуальном `HEAD` подтверждает, что `antigravity_key`/`.pub` и мусорные файлы (`_tmp_migrate.sql`, `fix_schema.sql`, `migration.sql`) отсутствуют — проверено этим циклом напрямую. Расследование причины замены истории `main` и решение учредителя по `H-260818-02` (отзыв ключа, аудит-лог, переписывание истории) не закрыты — это отдельная, не завершённая часть.
- **Статус:** в работе — код-часть сделана, ждёт учредителя и CTO/InfoSec по расследованию

---

## P0 — сегодня

#### B-260824-01 · Агент под личностью Claude пишет напрямую в `main` `cmpas.ru`, чередуясь с диагностическим ботом — не только workflow обходит PR, но и сам агент
- **Продукт:** платформа (ПРАКТИКА, репозиторий `cmpas.ru`) · **Приоритет:** P0 (свежее, активное нарушение устава §6.1, вне очереди устава §9 — серьёзнее уже известного `B-260822-01`, потому что это не CI-скрипт, а сам агент)
- **Источник:** найдено брифом PO-ПРАКТИКА, подтверждено CPO напрямую этим циклом (`git show -s --format='%an <%ae> \| parents: %P'` по каждому коммиту).
- **Текущее поведение:** после обычного PR-мержа `cmpas.ru#112` (`3ab0e29`) в `main` легло ещё пять коммитов автора `Claude <noreply@anthropic.com>` (`f92fa99`, `f104c8a`, `3f63d1c`, `72c8458`, `dd4b4e6`) вперемешку с четырьмя коммитами `simpas-doctor <bot@users.noreply.github.com>` (`ec57600`, `2c20b72`, `ecbbc9c`, `6df7a0d`, «Диагностика базы»). Каждый коммит имеет ровно одного родителя — не мёрж, прямой пуш. Похоже на автоматический цикл «диагностика → фикс агентом → снова диагностика», выполняющийся прямо на `main` без единого PR. Устав §6.1 прямо запрещает это независимо от того, кто пишет — CI-бот или сам агент реализации.
- **Желаемое поведение:** любой автоматический фикс во время деплоя идёт через PR с ревью; если исключение осознанно допускается (как временная мера для диагностики) — оно явно описано и утверждено учредителем, а не существует по факту работы пайплайна.
- **Критерий готовности:** ни один коммит с автором `Claude`/`simpas-doctor`/аналогичной ботовской личностью не появляется в `main` без предшествующего PR; branch protection (часть `H-260818-01`) закрывает это структурно, а не на честном слове.
- **Метрика проверки:** нет — процессная безопасность
- **Владелец:** CTO (расследовать деплой-цикл, который это порождает) + InfoSec
- **Эффект на соседние продукты:** да — тот же класс риска, что `H-260818-01`/`H-260818-02`/`B-260822-01`, и он активен прямо сейчас, не только исторически; усиливает срочность решения по обоим
- **Обновлено 24.08 (борд N3):** подтверждено CTO напрямую (`git log`/`git show` в живом коде) — 20 коммитов подряд в `zapiski` без единого мёржа, 9 прямых коммитов в `cmpas.ru` тем же паттерном. Механизм пуша не найден ни в одном workflow-файле — значит настроен вне репозитория (права токена/приложения), не правкой промпта. См. также `B-260824-03` — автодеплой на любой push, не только на мёрж, следующее звено той же цепи.
- **Обновлено 27.08 (ночной цикл):** ещё 13 прямых коммитов в `cmpas.ru` за ночь 26.08 (`f92fa99..HEAD`, чередование `Claude`/`simpas-doctor`, все с одним родителем), плюс 7 в `zapiski` и 1 в `compas-voice` тем же паттерном — риск активен во всех трёх продуктах одновременно, не только в `cmpas.ru`. Источник по-прежнему не установлен: ни `log/runner/`, ни `.runner/implement` не фиксируют прогона, который мог их произвести (пересмотренный диагноз `B-260823-01`). Не гипотеза — эти коммиты уже вызвали содержательные последствия: нарушение решения борда в `zapiski` (`H-260816-01`) и вероятную автопубликацию релиза в `compas-voice` (`B-260824-03`).
- **Статус:** предложена — самая свежая и самая серьёзная находка, активна во всех трёх продуктах, первый пункт повестки следующего борда

#### B-260824-03 · `deploy-docker.yml` в `cmpas.ru` деплоит на любой push в `main`, не только на PR-мёрж — усиливает `B-260824-01`
- **Продукт:** платформа (ПРАКТИКА, репозиторий `cmpas.ru`) · **Приоритет:** P0 (следующее звено цепи `B-260824-01`/`B-260822-01`, вне очереди устава §9)
- **Источник:** проверено CTO на борде N3 напрямую — `products/cmpas.ru/.github/workflows/deploy-docker.yml:129`, `job: deploy`, `if: github.event_name == 'push'`.
- **Текущее поведение:** пока branch protection на `main` не включена, любой прямой пуш в `main` (минимум два независимых источника — `B-260824-01`, `B-260822-01`) автоматически укатывается на прод без единого человеческого шага между коммитом и продакшеном.
- **Желаемое поведение:** деплой триггерится на мёрж проверенного PR, а не на голое push-событие, либо job требует дополнительного условия (пройденный review-статус), которое делает случайный/несанкционированный push безопасным по умолчанию.
- **Критерий готовности:** прямой push в `main` в обход PR не запускает деплой на прод.
- **Метрика проверки:** нет — процессная безопасность
- **Владелец:** CTO
- **Эффект на соседние продукты:** да, подтверждено — тот же паттерн найден в `compas-voice` (обновление 25.08). `zapiski` пока не проверялся.
- **Обновлено 25.08 (ночной цикл):** PO-МОМЕНТЫ нашла тот же паттерн в `compas-voice` — `git log` по умолчательной ветке показывает 10 прямых коммитов автора `Claude` (по одному родителю у каждого, не мёржи), и `.github/workflows/android-build.yml` публикует релиз (`gh release create/upload`) на любой push в `main` без условия о пройденном ревью, тот же механизм, что `deploy-docker.yml` в `cmpas.ru`. Один и тот же `PRODUCTS_READ_TOKEN` (admin-права, `H-260818-01`) используется во всех трёх `products/` — риск не изолирован одним репозиторием, ротация и branch protection должны закрывать все три разом, а не по одному циклу.
- **Обновлено 26.08 (ночной цикл):** подтверждено без изменений — `android-build.yml:258` по-прежнему безусловно публикует релиз на push в `main`, ни branch protection, ни `CODEOWNERS` не найдено. Полная разбивка авторов прямых коммитов `main` `compas-voice` (31 всего): `compas-psy` 11, `Claude` 10, `Eliah Martynov` 9, `simpas-agent` 1 — см. новую находку `B-260826-01` про личность «Eliah Martynov». `zapiski` по-прежнему не проверялся на этот же паттерн.
- **Обновлено 27.08 (ночной цикл):** находка стала конкретной, не только теоретической — коммит `c1d3df8` (26.08, автор «Eliah Martynov») задел `android/**`; по действующему триггеру `android-build.yml` (`push branches: [main] paths: android/**`) это должно было автоматически опубликовать/обновить релиз без единого шага ревью. PO-МОМЕНТЫ не смогла подтвердить сам факт публикации — `gh auth status` не залогинен в среде ночного цикла, нужен доступ CTO к `gh run list`/GitHub Releases для проверки.
- **Статус:** предложена — область расширена на `compas-voice`, найдено и там, и это уже не гипотеза, а вероятный факт неревьюированной публикации; `zapiski` по-прежнему не проверялся

#### B-260823-01 · Трек «реализация» не запускался пять ночей подряд — implement.yml без расписания, промпт не читает бэклог
- **Продукт:** платформа (compas-ops, процесс) · **Приоритет:** P0 (объясняет и снимает груз с четырёх других открытых P0-находок, вне очереди устава §9)
- **Источник:** проверено ночным циклом 23.08 напрямую — `.github/workflows/implement.yml`, `log/runner/`, `.runner/implement`, `.github/workflows/tg-orders.yml`, `.runner/prompt-implement.md`. Полный разбор — `D-260823-01`.
- **Текущее поведение:** `implement.yml` запускается только `workflow_dispatch` или пушем в `.runner/implement` — расписания нет. Последний реально выполненный прогон — `log/runner/2026-08-18_implement.log`; логов за 19–22.08 нет ни одного, `.runner/implement` не менялся с 18.08. `tg-orders.yml` (бот распоряжений) диспатч `implement.yml` не делает. Даже при запуске `prompt-implement.md` читает только `state/15_ORDERS.md`, не `state/10_BACKLOG.md` — пункты бэклога, назначенные «треку «реализация»» решениями CPO/борда, физически вне области его чтения.
- **Желаемое поведение:** `implement.yml` получает регулярный триггер (расписание либо иной надёжный механизм), и/или `prompt-implement.md` расширяется читать P0-пункты бэклога с владельцем «трек «реализация»», либо явно описан альтернативный путь их исполнения.
- **Критерий готовности:** implement-агент запускается без ручного вмешательства не реже раза в сутки; пункт бэклога с владельцем «трек «реализация»» имеет подтверждённый путь до исполнения.
- **Метрика проверки:** нет — процессная надёжность
- **Владелец:** CTO
- **Эффект на соседние продукты:** да — платформенный, объясняет простой P0-исправлений во всех трёх продуктах одновременно (ЗАПИСКИ: `B-260819-02`, `B-260819-01`, `B-260813-01`, `B-260813-08`; платформа/МОМЕНТЫ: `B-260818-04`; ПРАКТИКА: `B-260822-01`, `B-260817-04`)
- **Что сделано этим циклом:** `.runner/implement` обновлён новой записью, чтобы пуш этого коммита запустил прогон сегодня — прерывает пятидневный простой, не решает пункт про чтение бэклога.
- **Обновлено 24.08 (ночной цикл):** диагноз подтверждён ровно наполовину. Расписание/триггер действительно заработал — `implement.yml` реально прогнался 23.08 (20 коммитов в `zapiski`, минимум 9 в `cmpas.ru`, коммиты в `compas-voice`). Но вторая часть диагноза «промпт не читает бэклог» тоже подтвердилась фактом, а не предположением: ни один из 20 коммитов `zapiski` не тронул ни один из пяти файлов, относящихся к открытым P0-находкам ЗАПИСОК (`B-260819-02`, `B-260819-01`, `B-260813-01`, `B-260813-08`, `H-260816-01`) — весь объём ушёл на Android-сборку, деплой и мост аналитики. Первая часть закрывается, вторая остаётся открытой на CTO.
- **Обновлено 24.08 (борд N3, `D-260824-06`):** CTO закрыл только вторую половину — расширил `.runner/prompt-implement.md` читать P0-бэклог. Первая половина («у `implement.yml` нет расписания») не тронута: файл по-прежнему без `cron`, только `workflow_dispatch` и push по пути `.runner/implement`.
- **Обновлено 25.08 (ночной цикл):** первая половина диагноза оказалась не решена, а лишь замаскирована разовым ручным пушем 23.08 — подтверждено этим циклом напрямую (`grep cron|schedule .github/workflows/implement.yml` → пусто). Ни один из трёх продуктовых репозиториев не сдвинулся за 24→25.08, `log/runner/` не содержит прогона за эти сутки, `.runner/implement` не обновлялся с 23.08 — implement.yml не запускался вовсе, регулярного триггера как не было, так и нет. Критерий готовности `D-260823-01` («получает расписание либо иной надёжный механизм») не выполнен, хотя решение борда `D-260824-06` закрыло задачу как «выполнено» по второй половине — первая половина ошибочно считалась решённой заодно. Это не гипотеза, а факт: без ручного пуша в `.runner/implement` трек «реализация» не работает ни одну ночь.
- **Обновлено 26.08 (ночной цикл):** третья ночь простоя подряд (24→25→26.08), подтверждено независимо всеми тремя PO — `implement.yml` по-прежнему без `cron`/`schedule`, `.runner/implement` не менялся с 23.08. Разовый ручной пуш 23.08 остаётся единственным прогоном за девять суток.
- **Статус:** в работе — реопенится: чтение бэклога исправлено, но регулярного триггера как не было, так и нет; первый пункт повестки следующего борда

#### B-260822-01 · `db-doctor.yml`/`db-baseline.yml`/`ru-ca.yml` пишут напрямую в `main`, минуя PR — три workflow тем же паттерном
- **Продукт:** платформа (ПРАКТИКА, репозиторий `cmpas.ru`) · **Приоритет:** P0 (материал к открытому инциденту `H-260818-01`/`H-260818-02`, вне очереди устава §9)
- **Источник:** бриф PO-ПРАКТИКА, ночной цикл 22.08 — впервые разобран механизм, стоящий за уже известными прямыми коммитами бота `simpas-doctor` в `main` (в т.ч. `47260c6`, `133c474`, `27719c8`, `346ed14`, `b2250a8`). Расширено 24.08 — тот же паттерн найден ещё в двух workflow, подтверждено CPO напрямую `grep`.
- **Текущее поведение:** `products/cmpas.ru/.github/workflows/db-doctor.yml:64-70`, `db-baseline.yml:67-68`, `ru-ca.yml:65-66` — идентичный паттерн `git commit` → `git push origin HEAD:main || echo "нечего пушить"`. Это не гипотетический риск, а работающий механизм минимум в трёх CI-скриптах, каждый пишет прямо в `main`, минуя PR и ревью — прямое нарушение устава §6.1. Ошибка push молча проглатывается (`|| echo`) во всех трёх, поэтому включение branch protection (часть `H-260818-01`) без правки этих файлов либо будет постоянно и незаметно отказывать, либо (если workflow оставят исключением из защиты) обнулит смысл самой защиты. Отдельно, более серьёзно — см. `B-260824-01`: помимо этих трёх CI-скриптов, сам агент реализации (автор коммита `Claude`) тоже пишет в `main` напрямую в том же окне 23.08.
- **Желаемое поведение:** все три workflow открывают pull request с диагностическим отчётом вместо прямого коммита в `main` (либо пишут отчёт в issue/комментарий, если PR избыточен для чисто информационного файла); ошибка push не проглатывается молча ни в одном из трёх.
- **Критерий готовности:** ни один запуск `db-doctor.yml`/`db-baseline.yml`/`ru-ca.yml` не коммитит напрямую в `main`; при включённой branch protection ни один не ломается молча.
- **Метрика проверки:** нет — процессная безопасность
- **Владелец:** разработка (трек «реализация»), синхронизировать с `H-260818-01` (branch protection) до или одновременно
- **Эффект на соседние продукты:** нет напрямую, но тот же паттерн (ботовская личность = коммит в `main` без ревью) стоит проверить в `zapiski`/`compas-voice` — до сих пор проверялся только `cmpas.ru`
- **Обновлено 23.08 (ночной цикл):** без движения — строки 64-70 `db-doctor.yml` не тронуты, подтверждено PO-ПРАКТИКА независимо. Причина простоя теперь объяснена: `B-260823-01` (трек «реализация» не запускался пять ночей).
- **Обновлено 24.08 (ночной цикл):** масштаб находки вырос — `db-baseline.yml` и `ru-ca.yml` несут тот же паттерн (`grep` этим циклом), плюс новая, отдельная и более серьёзная находка `B-260824-01` (сам агент, не только CI-скрипт, пишет в `main` напрямую).
- **Статус:** предложена — область находки расширена, не проверена ещё в `zapiski`/`compas-voice`

#### B-260819-01 · CI-проверка безопасности ЗАПИСОК не запускается ни на одном реальном пуше
- **Продукт:** ЗАПИСКИ · **Приоритет:** P0 (безопасность процесса, вне очереди устава §9 — все находки SEC-001/приватности идут без единой автоматической проверки)
- **Источник:** бриф PO-ЗАПИСКИ, ночной цикл 19.08 — независимо подтверждено CPO (`grep` `on:`-блока файла).
- **Текущее поведение:** `products/zapiski/.github/workflows/security.yml` триггерится только на `branches: ["main"]`, а такой ветки в репозитории нет — рабочая ветка по умолчанию `claude/cloud-deployment-cross-platform-9nenw7` (тот же корень, что у `B-260815-02`). Соседние воркфлоу (`provision-zapiski.yml`, `deploy-zapiski.yml`) это уже обошли явным перечислением веток; у `security.yml` такой правки нет — похоже на недосмотр. Итог: сканер секретов, тест SEC-001 и проверка «запрещённых путей» не выполнились ни разу за все 18.08 (20 коммитов).
- **Желаемое поведение:** `security.yml` триггерится на реальной рабочей ветке (по образцу `provision-zapiski.yml`), и security-проверки реально идут на каждом пуше.
- **Критерий готовности:** после следующего пуша в рабочую ветку в GitHub Actions появляется прогон workflow «Безопасность».
- **Метрика проверки:** нет — процессная безопасность
- **Владелец:** разработка (трек «реализация»)
- **Эффект на соседние продукты:** нет
- **Обновлено 20.08 (ночной цикл):** без движения второй цикл подряд — `.github/workflows/security.yml:14-16` по-прежнему триггерится на несуществующую ветку `main`, подтверждено PO-ЗАПИСКИ независимо от вчерашней проверки. За эти сутки в репозиторий легло 31 новый коммит (рекордный объём), ни один не прошёл ни одной автоматической security-проверки — цена бездействия растёт быстрее, чем раньше.
- **Обновлено 23.08 (ночной цикл):** без движения, подтверждено PO-ЗАПИСКИ. Причина простоя объяснена `B-260823-01` — не перегрузка, а трек «реализация» не запускался вовсе.
- **Статус:** предложена — S-размер, простой объяснён `B-260823-01`, не переоткрывать как загадку

#### B-260819-02 · Цена, триал и платёжный провайдер ЗАПИСОК меняются в коде без записи в 12_DECISIONS
- **Продукт:** ЗАПИСКИ · **Приоритет:** P0 (поднято 22.08 — второй пропущенный срок отката плюс расширившееся самовольное изменение биллинга; исходное обоснование P1 см. в истории ниже)
- **Источник:** бриф PO-ЗАПИСКИ, ночной цикл 19.08 — коммит `44e1d2d` «Триал облака в MVP: 30 дней до 1 сентября, дальше 14» (18.08). **Расширено 20.08:** коммит `22174b2` (19.08 14:36) меняет саму цену — `PRICE_MONTHLY_RUB` 199→299 ₽, `PRICE_YEARLY_MONTHLY_RUB` 149→224 ₽ (`server/src/config/env.ts:141,145,148-149`), ссылаясь на «Решения учредителя от 18.08.2026»; коммит `91a5459` тем же днём добавляет Т-Банк третьим платёжным провайдером (после ЮKassa и Google Play) в собственный биллинг ЗАПИСОК (`server/src/config/env.ts`, за флагом `BILLING_ENABLED=false`).
- **Текущее поведение:** ни одна из этих трёх правок (триал, цена, третий провайдер) не имеет записи в `state/12_DECISIONS.md` — прочитан целиком, такой записи нет. Промостраница частично догнала код (299 ₽/мес совпадает), но годовая ставка расходится (249 ₽ на промо против 224 ₽ в коде) — уже шестое по счёту расхождение той же природы (см. историю в `B-260813-14`).
- **Желаемое поведение:** CPO/CEO сверяют с учредителем на борде, было ли решение реально принято в указанном виде, и фиксируют его в `12_DECISIONS` дословно — либо код откатывается как самовольное изменение. Отдельно: третий платёжный провайдер в собственном биллинге ЗАПИСОК движется прямо против направления `D-260817-04` («собственная модель биллинга ЗАПИСОК — технический долг, подлежит устранению») — это тоже требует явного слова борда, а не молчаливого продолжения разработки в противоположную сторону.
- **Критерий готовности:** запись в `12_DECISIONS` существует и совпадает с тем, что реально в коде (цена, триал, провайдер), либо код возвращён к прежнему поведению.
- **Метрика проверки:** нет
- **Владелец:** CPO + CEO
- **Эффект на соседние продукты:** да — пятый и шестой параметры добавляются к нерешённому конфликту цен ЗАПИСОК (`B-260813-14`): триал (было 14 дней везде, теперь код говорит 30 до 01.09), цена (199/149 → 299/224 в коде, 299/249 на промо, 990/1990 в документах — три разных числа одновременно), плюс рост, а не сокращение количества платёжных интеграций.
- **Обновлено 21.08 (ночной цикл):** срок из `D-260820-07` наступил — `12_DECISIONS.md` прочитан целиком, подтверждения «решения от 18.08» нет. По условию решения откат назначен треку «реализация» как P0 сегодня (`D-260821-02`); это не решение о цене — цена остаётся вопросом учредителя (`D-260817-04`).
- **Обновлено 22.08 (ночной цикл):** назначенный срок (21.08) пропущен — откат не выполнен. Хуже: расхождение выросло, а не застыло — коммит `6afde85` (19.08 15:13, найден только этим циклом) полностью удалил ЮKassa и Google Play из кода, оставив Т-Банк единственным платёжным провайдером ЗАПИСОК; это больше, чем «третий провайдер добавлен», как было описано раньше. Второй раз подряд назначение треку «реализация» не исполняется — см. эскалацию в плане дня 22.08.
- **Обновлено 23.08 (ночной цикл):** без движения, ноль коммитов в `zapiski` за сутки (подтверждено PO-ЗАПИСКИ). Причина найдена — `B-260823-01`: трек «реализация» не запускался пять ночей подряд, назначение физически не могло исполниться.
- **Обновлено 27.08 (ночной цикл):** восьмой пропущенный срок подряд. `PRICE_MONTHLY_RUB=299`/`PRICE_YEARLY_MONTHLY_RUB=224` и Т-Банк как провайдер — без изменений в `server/src/config/env.ts`, подтверждено PO-ЗАПИСКИ: ни один из 7 новых коммитов 26.08 не тронул `env.ts`. Записи в `12_DECISIONS` по-прежнему нет.
- **Статус:** в работе — откат коммитов `22174b2`/`91a5459`/`6afde85` не исполнен восьмой цикл подряд; простой объяснён `B-260823-01`, не переоткрывать как загадку

#### B-260819-03 · Оформить постмортем инцидента с приёмом платежей 18.08 (сертификат Минцифры + стадия сборки)
- **Продукт:** платформа (ПРАКТИКА) · **Приоритет:** P1 (инцидент уже устранён, но не задокументирован — тот же паттерн, что и авария 17.08, для которой тоже нет `I-...`)
- **Источник:** бриф PO-ПРАКТИКА, ночной цикл 19.08 — найден по коммитам `git show 00b90fe` (#107, «Доверие корню Минцифры») и `git show ab5b6b6` (#104, «Закрепить стадию сборки образа приложения»).
- **Текущее поведение:** 18.08 сервер отвергал TLS-соединение к `securepay.tinkoff.ru` как самоподписанное, потому что сертификат подписан корнем Минцифры, которого нет в стандартном наборе Debian; отдельно неверная стадия сборки Docker-образа заставляла контейнер запускать сборщик диагностики вместо Next.js, выкладка падала и откатывалась. Оба симптома устранены тем же днём (#107, #104), но ни один не оформлен как `I-...` по шаблону `04_TEMPLATES §8` — причина и признак «чтобы не повторилось» нигде не зафиксированы структурно.
- **Желаемое поведение:** запись `I-260818-01` с разбором причины, что сделано, что изменить, чтобы не повторилось.
- **Критерий готовности:** запись существует в формате `04_TEMPLATES §8`.
- **Метрика проверки:** нет
- **Владелец:** CTO
- **Эффект на соседние продукты:** нет — но без доверенного корня сертификата приём боевых платежей был бы невозможен ни для одного продукта портфеля, если бы деньги шли через тот же сервер
- **Обновлено 20.08 (ночной цикл):** без движения второй цикл подряд — запись `I-260818-01` по-прежнему не найдена сплошным поиском по `state/`+`log/`.
- **Обновлено 21.08 (ночной цикл):** закрыто — запись `I-260818-01` оформлена по шаблону `04_TEMPLATES §8` в `state/12_DECISIONS.md` (третий цикл простоя, но данные и так были известны из этой же карточки).
- **Статус:** сделана

#### B-260818-01 · Красная линия приватности напоминаний вернулась на `main` ПРАКТИКИ
- **Продукт:** ПРАКТИКА · **Приоритет:** P0 (приватность клиента, красная линия `01_ROLES_PO §2`, вне очереди устава §9)
- **Источник:** бриф PO-ПРАКТИКА, ночной цикл 18.08 — независимо перепроверено CPO на `origin/main` (`git show origin/main:src/lib/cron/reminders.ts`).
- **Текущее поведение (было):** 24-часовое напоминание клиенту снова содержало слово «психолог» и имя специалиста (`src/lib/cron/reminders.ts:79`).
- **Обновлено 19.08 (ночной цикл):** закрыто. `reminders.ts` больше не строит текст инлайн — вызывает `build24hReminderText()` из нового `src/lib/cron/reminder-text.ts`; текст не содержит «психолог»/имени специалиста, покрыт `tests/reminder-text.test.ts`. Проверено дважды — брифом PO-ПРАКТИКА и напрямую `grep` этим циклом (ноль совпадений `психолог`/`psychologistName` в обоих файлах).
- **Критерий готовности:** `reminders.ts` не содержит слова «психолог» и имени специалиста в тексте 24-часового напоминания. — выполнено.
- **Метрика проверки:** нет — приватность, не метрика
- **Владелец:** разработка (трек «реализация»), вне очереди
- **Эффект на соседние продукты:** нет
- **Статус:** сделана

#### B-260818-02 · Слияние PR в ЗАПИСКАХ похоже на действие агента, а не учредителя — расследовать
- **Продукт:** платформа (устав §6.1) · **Приоритет:** P0 (нарушение границы человек/агент, вне очереди устава §9)
- **Источник:** бриф PO-ЗАПИСКИ, ночной цикл 18.08 — независимо перепроверено CPO через GitHub API.
- **Текущее поведение:** `compas-psy/zapiski#1` закрыт как `merged` 17.08 15:22 UTC, поле `merged_by` показывает `compas-psy`, но коммит слияния подписан `Claude <noreply@anthropic.com>`, а текст сообщения написан от первого лица агента, разбирающего находку заказчика постфактум («Проверено после слияния: typecheck... 1700 тестов зелёные»). `PRODUCTS_READ_TOKEN`, которым пользуются агенты, оказался личным PAT аккаунта `compas-psy` — с этим токеном действие агента и клик учредителя в интерфейсе GitHub оставляют одинаковый след и неразличимы постфактум.
- **Желаемое поведение:** учредитель подтверждает или опровергает, что лично мержил `zapiski#1`; независимо от ответа — токен ротирован (`H-260818-01`) и на ветку по умолчанию каждого продуктового репозитория включена защита с обязательным ревью, чтобы мёрж токеном агента стал технически невозможен, а не только запрещён инструкцией.
- **Критерий готовности:** ответ учредителя зафиксирован в `12_DECISIONS`; branch protection включена на всех трёх `products/`.
- **Метрика проверки:** нет — процессная безопасность
- **Владелец:** InfoSec + CTO · зависит от `H-260818-01`
- **Эффект на соседние продукты:** да — тот же токен используется во всех трёх репозиториях, тот же риск применим к каждому
- **Статус:** предложена

#### B-260817-08 · Активировать право агентов писать в products/ (токен + правка воркфлоу)
- **Продукт:** платформа · **Приоритет:** P0 (единственный блокер всех остальных P0-исправлений в коде)
- **Источник:** борд N1, решение `D-260817-01` по распоряжению `O-260816-01`... `O-260817-01`
- **Критерий готовности:** первый тестовый PR агента в любой из `products/` репозиториев успешно открывается.
- **Обновлено 18.08 (ночной цикл):** критерий выполнен и перевыполнен — за 17.08 через токен уже открыто больше десятка PR в трёх репозиториях (см. `15_ORDERS.md`), сегодня ночью тем же способом открыт `cmpas.ru#91`. Права подтверждены напрямую через GitHub API (`permissions.push: true`, `admin: true` на всех трёх `products/`). Побочная находка при подтверждении — токен оказался личным PAT аккаунта `compas-psy`, а не токеном с урезанными правами; заведена `H-260818-01` на ротацию с branch protection.
- **Владелец:** COO + CTO
- **Эффект на соседние продукты:** да — разблокировало все три продукта разом
- **Статус:** сделана

#### B-260817-03 · Вернуть чек-ин самочувствия МОМЕНТОВ к записи только по касанию
- **Продукт:** МОМЕНТЫ · **Приоритет:** P0 (приватность, потенциально данные о здоровье — красная линия устава §10, вне очереди устава §9) · **сделана**
- **Источник:** бриф PO-МОМЕНТЫ, ночной цикл 17.08 — полная история `compas-voice` прочитана впервые. Коммит `fc068b6` (доработки по фидбэку, до 17.07) изменил `AftercareScreen.kt` и `StartSheet.kt`: чек-ин напряжения (0–10) теперь пишется по умолчанию, `null` — только при явном «Пропустить». Раньше писался только при касании слайдера.
- **Текущее поведение:** приложение записывает значение самочувствия без действия пользователя на каждом запуске практики через шит и итоговый экран — уже месяц, незамеченно.
- **Желаемое поведение:** три документа продукта прямо противоречат этому и не были обновлены при мерже: `docs/PRODUCT.md:149–155` («записываем значение, только если было касание — проще и честнее»), `docs/QA-TESTPLAN.md:51` («чек-ин не записан без касания»), `docs/PRIVACY-DPO.md:22,28` («может считаться данными о здоровье / психическом состоянии»). Либо поведение возвращается к документированному, либо расширение сбора данных проходит через InfoSec явно, до, а не после мерджа (`01_ROLES_PO §1.5`).
- **Критерий готовности:** дефолт чек-ина совпадает с `PRODUCT.md`.
- **Метрика проверки:** нет — вопрос приватности, не метрики
- **Владелец:** разработка + InfoSec
- **Эффект на соседние продукты:** нет
- **Обновлено 17.08 (борд N1):** решение принято — `D-260817-05`, задним числом визировать текущий дефолт не будем (InfoSec и Legal согласны, обоснования для дефолта «5» нет). Исполнение — первая волна PR после `B-260817-08` наравне с `B-260817-04`.
- **Обновлено 18.08 (ночной цикл):** `compas-psy/compas-voice#1` смёржен учредителем 17.08 11:32 UTC, проверено построчно на `origin/main` (`AftercareScreen.kt`/`StartSheet.kt` пишут значение только при касании, три документа продукта совпадают с кодом). Закрыто.
- **Статус:** сделана

#### B-260817-04 · Инструкция репозитория ПРАКТИКИ предписывает автодеплой после каждой задачи — вопреки уставу §6.1
- **Продукт:** платформа (ПРАКТИКА) · **Приоритет:** P0 (риск неавторизованного релиза, вне очереди устава §9)
- **Источник:** независимо обнаружено CPO при чтении `products/cmpas.ru/CLAUDE.md` и брифом PO-ПРАКТИКА в этом же цикле — раздел «Правила работы агента»: «После каждой задачи всегда делать деплой: смёрджить PR в `main` самостоятельно — это запустит GitHub Actions (деплой сервера + Android-билд)».
- **Текущее поведение:** любой агент разработки, открывший этот репозиторий и следующий его CLAUDE.md буквально, автоматически мержит в `main` и деплоит — без исключений и без человека. Прямо противоречит уставу §6.1 («не выкатывают в прод») и явному пункту распоряжения `O-260816-04` («деплой не делать»).
- **Желаемое поведение:** инструкция репозитория требует ветку и PR, деплой — только по явному распоряжению человека или отдельной задаче `/human`.
- **Критерий готовности:** `products/cmpas.ru/CLAUDE.md` не содержит требования автомерджа/автодеплоя после задачи.
- **Метрика проверки:** нет — процессный риск, не метрика
- **Владелец:** CTO
- **Эффект на соседние продукты:** да — тот же паттерн стоит проверить в CLAUDE.md `zapiski` и `compas-voice`, ночной цикл этого не делал
- **Обновлено 17.08 (борд N1):** `D-260817-02` — это первый PR, который откроет любой агент-разработчик сразу после `B-260817-08`, раньше любых фич: агент с правом пуша, следующий текущей инструкции буквально, автоматически смержит и задеплоит без человека.
- **Обновлено 18.08 (ночной цикл):** решение борда не было исполнено первым — за 17.08 в `products/` открылось больше десятка PR раньше этого. Хуже: PO-ПРАКТИКА независимо нашёл, что инструкция уже успела сработать — авария 17.08 (`adeb588`/#84 «Выкладка останавливается, если база не совпадает со схемой») прямо связана с автомержем без проверки миграций. Ночной цикл открыл `cmpas.ru#91` — правит ровно эту строку `CLAUDE.md:5` на «PR открывает агент, мёрж и деплой — только учредитель», не смёржен.
- **Обновлено 20.08 (ночной цикл):** содержание уже на `HEAD` совпадает с веткой-исправлением (`git diff origin/main origin/feature/B-260817-04-no-autodeploy -- CLAUDE.md` — пусто, требования автодеплоя в файле нет), но это произошло не через мёрж PR #91: файл менялся диагностическим коммитом 18.08 10:06 UTC, тем же окном, где боты `simpas-doctor`/`simpas-agent` сделали минимум 15 прямых коммитов в `main`, минуя PR (см. эскалацию по `H-260818-01`/`H-260818-02` в `20_DAILY_LOG` 20.08). Функционально риск снят, но механизм, которым он снят, — тот же самый паттерн прямого пуша в `main` мимо ревью, который сейчас разбирается как инцидент. CTO нужно подтвердить: PR #91 закрыть как избыточный (содержание уже применено) и зафиксировать, как файл попал на `HEAD`, а не просто закрыть задачу по факту зелёного диффа.
- **Обновлено 23.08 (ночной цикл):** без движения, седьмой цикл без решения CTO, подтверждено PO-ПРАКТИКА. Теперь есть прямая улика к механизму — `B-260822-01` (`db-doctor.yml` штатно пушит в `main`).
- **Статус:** в работе — седьмой цикл ждёт решения CTO по PR #91

#### B-260816-01 · Не публиковать промостраницу ЗАПИСОК, пока она обещает то, что код не выполняет
- **Продукт:** ЗАПИСКИ · **Приоритет:** P0 (юридический риск на новой публичной поверхности, вне очереди — устав §9)
- **Источник:** бриф PO-ЗАПИСКИ, внеплановый цикл 16.08 — `apps/web/public/promo/index.html` (772 строки), коммиты `15fb302`, `c6a692f`, `306aea8`, `18c7e9b` (16.08, 11:18–12:00 UTC), постоянные адреса сборок (`/updates/latest/zapiski.apk`, `.exe`) уже добавлены рядом
- **Текущее поведение:** страница готова к раскатке и содержит безусловное `«Мы не можем прочитать ваши заметки. Технически.»` (строка 580) при открытом SEC-001 и падающем регрессионном тесте, плюс третью цену `ЗАПИСКИ+` — 299 ₽/мес (249 ₽ при годовой, 14 дней триала) — не совпадающую ни с кодом (199/149 ₽), ни с документами (990/1990 ₽). Неизвестно, задеплоена ли страница уже на `zapiski.cmpas.ru/promo` — CTO ночью не участвует, подтверждения нет.
- **Желаемое поведение:** страница не публикуется (или снимается, если уже публична) до закрытия `B-260813-08` (SEC-001) и решения по формулировке (`H-260813-05`) и по цене (`B-260813-14`).
- **Критерий готовности:** страница недоступна публично либо не содержит безусловных утверждений о невозможности прочтения и не содержит цены, расходящейся с решением борда.
- **Метрика проверки:** нет — красная линия продукта, не ждёт решения по приоритету
- **Владелец:** разработка + маркетинг
- **Эффект на соседние продукты:** нет
- **Статус:** предложена

#### B-260815-01 · CI-проверка: не пускать в сборку письмо входа с обещанием о невозможности прочитать заметки
- **Продукт:** ЗАПИСКИ · **Приоритет:** P0 (юридический риск и красная линия продукта, вне очереди — устав §9)
- **Источник:** бриф PO-ЗАПИСКИ 15.08.2026 — проверено на HEAD: `server/src/services/mailer.ts`, функция `renderMagicLink`, текстовая и HTML-версии письма входа по-прежнему содержат фразу «Заметки мы не читаем — технически не можем», хотя `docs/dev/security/AUDIT.md` в этом же коммите подтверждает обратное (SEC-001, статус «предложено, блокирует приёмку»). Юридические черновики (`server/legal/notes-0.9.md`, `privacy-0.9.md`, 14.08) уже переписаны честно — письмо осталось единственной непереписанной поверхностью. **Обновлено 16.08 (внеплановый цикл):** та же фраза найдена во второй, более публичной поверхности — `apps/web/public/promo/index.html:580`, см. `B-260816-01`.
- **Текущее поведение:** ничто не мешает собрать и задеплоить письмо или промостраницу с этой фразой, пока SEC-001 открыт.
- **Желаемое поведение:** сборка падает, если фраза о технической невозможности прочитать заметки встречается в исходниках письма входа или промостраницы, а SEC-001 не закрыт.
- **Критерий готовности:** тест/проверка в CI зелёная только при отсутствии фразы либо при закрытом SEC-001.
- **Метрика проверки:** нет — красная линия продукта (`01_ROLES_PO §1.3`), не ждёт решения учредителя по формулировке (в отличие от `B-260813-07`) — можно делать уже сегодня.
- **Владелец:** разработка
- **Эффект на соседние продукты:** нет
- **Статус:** в работе (перевод CPO 16.08). **Обновлено 16.08 (внеплановый цикл):** за сутки в CI и в `scripts/` кода проверки по-прежнему нет (`.github/workflows/security.yml` не менялся до находки), а поверхность для регресса выросла — добавить в область проверки `apps/web/public/promo/index.html`, не только письмо. **Обновлено 17.08:** третья поверхность найдена — `packages/app/src/i18n/ru.ts:141` и `en.ts:103` (`signIn.privacy`), рендерится на экране входа приложения (`packages/app/src/screens/SignInScreen.tsx:239`) — это уже используемая поверхность, не гипотетическая, как промостраница. Область проверки CI расширяется на оба i18n-файла.

#### B-260813-01 · Проверить публичный репозиторий ЗАПИСКИ на утечки
- **Продукт:** ЗАПИСКИ · **Приоритет:** P0 (правило устава §9: безопасность вне очереди)
- **Источник:** проверка доступов 13.08.2026, `07_REPOS §1`
- **Текущее поведение:** репозиторий `compas-psy/zapiski` публичен. Это репозиторий продукта, работающего с профессиональной тайной специалиста.
- **Желаемое поведение:** публичность является осознанным решением борда, а не следствием настроек по умолчанию; в истории коммитов нет секретов, ключей, тестовых персональных данных и внутренних документов.
- **Критерий готовности:** проведён поиск секретов по всей истории; составлен список найденного; борд принял явное решение о публичности; при обнаружении секретов — они отозваны, а не просто удалены из последнего коммита.
- **Метрика проверки:** нет — это гигиена, а не метрика
- **Владелец:** InfoSec совместно с CTO
- **Эффект на соседние продукты:** общие пакеты монорепо могут использоваться другими продуктами — проверить, не утекает ли через них конфигурация
- **Обновлено 17.08 (борд N1):** `D-260817-04` — секретов в истории не найдено (проверено сплошным поиском), окончательное решение о самой публичности остаётся за учредителем (`H-260813-04`), но борд уже решил: `docs/dev/security/AUDIT.md`, `THREAT-MODEL.md` и `docs/spec/ds/vendor/` убираются из публичного доступа первой же волной PR после `B-260817-08`, не дожидаясь решения о публичности целиком.
- **Обновлено 18.08 (ночной цикл):** не сделано, хотя токен активен уже сутки и в `zapiski`/`cmpas.ru`/`compas-voice` за это время открыто и смёржено больше десятка PR по другим темам — эта конкретная задача осталась без хода. Файлы физически на месте и публично доступны (`docs/dev/security/AUDIT.md` 96 КБ, `THREAT-MODEL.md` 24 КБ, `docs/spec/ds/vendor/`), проверено PO-ЗАПИСКИ сегодня ночью.
- **Обновлено 20.08 (ночной цикл):** без изменений — файлы по-прежнему в дереве и не в `.gitignore`, подтверждено PO-ЗАПИСКИ. Четвёртый день просрочки относительно решения борда `D-260817-04` от 17.08.
- **Обновлено 23.08 (ночной цикл):** без изменений, подтверждено PO-ЗАПИСКИ. Причина простоя объяснена `B-260823-01`.
- **Статус:** предложена — седьмой день просрочки, простой объяснён `B-260823-01`

#### B-260813-07 · Убрать из пользовательских текстов обещание, которое код не выполняет
- **Продукт:** ЗАПИСКИ · **Приоритет:** P0 (юридический риск, вне очереди)
- **Источник:** ночной цикл 13.08, бриф PO-ЗАПИСКИ; обновлено 15.08
- **Текущее поведение:** частично сделано. Юридические черновики (`notes-0.9.md`, `privacy-0.9.md`) переписаны честно 14.08 — обещание невозможности прочтения снято, шифрование названо «продуктовой рекомендацией», а не фактом. Письмо входа (`renderMagicLink`) на HEAD от 15.08 всё ещё содержит фразу «технически не можем» — самая заметная пользователю поверхность осталась непеределанной.
- **Желаемое поведение:** ни одного утверждения о невозможности прочтения в текстах, отдающихся пользователю, пока находка не закрыта.
- **Критерий готовности:** проверены письмо входа, черновики политики и соглашения, документация и все внешние тексты; формулировка утверждена учредителем.
- **Владелец:** разработка · зависит от `H-260813-05`
- **Обновлено 17.08:** третья живая поверхность — экран входа приложения (`packages/app/src/i18n/ru.ts:141`, `en.ts:103` → `SignInScreen.tsx:239`), не только письмо и промостраница. Эта поверхность уже используется пользователями сегодня, в отличие от непубличной промостраницы.
- **Статус:** в работе

#### B-260813-08 · Закрыть SEC-001: шифрование между локальным движком синхронизации и облаком
- **Продукт:** ЗАПИСКИ · **Приоритет:** P0 (безопасность, вне очереди)
- **Источник:** внутренний аудит репозитория, подтверждён ночным циклом 13.08, статус находки перепроверен 15.08 — без изменений («предложено, блокирует приёмку»)
- **Текущее поведение:** незашифрованная заметка уходит в облако открытым текстом; регрессионный тест на текущей ветке помечен как заведомо падающий; заголовок заметки используется как первичный ключ хранилища, то есть утекает как метаданные.
- **Желаемое поведение:** содержание и имена заметок не покидают устройство в открытом виде.
- **Критерий готовности:** регрессионный тест SEC-001 проходит; путь и заголовок не хранятся в открытом виде на сервере.
- **Метрика проверки:** нет — красная линия продукта (`01_ROLES_PO §3`)
- **Владелец:** разработка · ревью InfoSec
- **Эффект на соседние продукты:** определяет требования к хранению для всей платформы
- **Обновлено 23.08 (ночной цикл):** без движения, регрессионный тест SEC-001 по-прежнему заведомо падающий (`it.fails`), подтверждено PO-ЗАПИСКИ. Причина простоя объяснена `B-260823-01`.
- **Статус:** предложена — простой объяснён `B-260823-01`

---

## P1 — до недели

#### B-260824-02 · Мост аналитики ЗАПИСКИ → ПРАКТИКА (`practiceBridge.ts`) построен без записанного ревью InfoSec
- **Продукт:** платформа (аналитика, ЗАПИСКИ + ПРАКТИКА) · **Приоритет:** P1
- **Источник:** бриф PO-ЗАПИСКИ, ночной цикл 24.08 — коммиты трека «реализация» 23.08 (`dbff8b4`, `ab4d201`, `8e3b529`, `181d8be` и другие) вводят `practiceBridge.ts`, пересылающий метаданные заметок (в т.ч. `length_bucket`) из ЗАПИСОК в приёмник ПРАКТИКИ.
- **Текущее поведение:** мост расширяет объём данных, покидающих контур продукта ЗАПИСКИ (даже если это только метаданные, не содержание) — построен и смёржен без явной записи ревью InfoSec в `12_DECISIONS`, хотя `01_ROLES_PO §1.5` требует этого до, а не после мёржа.
- **Желаемое поведение:** InfoSec задним числом подтверждает или отклоняет соответствие моста правилу — что именно передаётся, откуда взято, нет ли риска реидентификации по `length_bucket` в паре с другими полями.
- **Критерий готовности:** запись InfoSec о соответствии/несоответствии зафиксирована в `12_DECISIONS`.
- **Метрика проверки:** нет — процесс приватности
- **Владелец:** InfoSec
- **Эффект на соседние продукты:** да — данные идут именно между двумя продуктами
- **Статус:** предложена

#### B-260818-04 · `identity_linked`/`consent_updated` в реестре закреплены за `practice` — ломает анонимную аналитику МОМЕНТОВ/ЗАПИСОК
- **Продукт:** платформа (аналитика, три продукта) · **Приоритет:** P0 (поднято бордом N2, `D-260820-08` — собственная находка CDO третьи сутки блокирует уже смёрженный код, CDO берёт исполнение на себя сегодня)
- **Источник:** найдено при прогоне тестов `products/cmpas.ru` во время исполнения `O-260818-02` (не относится к самой задаче — гигиена сервера, не аналитика). Не моя правка, воспроизведено на чистом `origin/main` (`c8e65bf`) отдельным `git worktree` до и независимо от любых изменений этой ветки.
- **Текущее поведение:** `npx vitest run tests/analytics-ingest.test.ts` — 2 из 15 падают на `main`: `device_id-only consent_updated(granted=true) is itself accepted...` и `a device that later gets an account is linked by identity_linked...`. Причина — `analytics/schema/events.yaml` (сведён `O-260817-17`, `cmpas.ru#94`) закрепляет `identity_linked` и `consent_updated` за `product: practice`; `validateEvent` (тем же PR) теперь отвергает событие, если `def.product !== raw.product`. Но `O-260817-13` (анонимный приём по `deviceId`, `cmpas.ru#93`) написан для МОМЕНТОВ и ЗАПИСОК — у обоих нет аккаунта, и тесты шлют оба события с `product: 'moments'`. Итог: устройство МОМЕНТОВ/ЗАПИСОК физически не может ни дать согласие (`consent_updated`), ни быть привязанным к аккаунту задним числом (`identity_linked`) — обе операции требуемые самой моделью анонимного приёма отвергаются приёмником как «чужой продукт».
- **Желаемое поведение:** `identity_linked`/`consent_updated` — кросс-продуктовые события (модель устройство→аккаунт одинакова для всех трёх продуктов), а не только `practice`. Либо реестр допускает несколько продуктов на одно событие, либо эти два события не проверяются по `product` вовсе.
- **Критерий готовности:** оба теста в `tests/analytics-ingest.test.ts` зелёные без ослабления проверки «чужой продукт отвергается» для остальных событий.
- **Метрика проверки:** нет — целостность приёмника, не метрика
- **Владелец:** CDO (реестр) + CTO
- **Эффект на соседние продукты:** да — блокирует анонимную аналитику МОМЕНТОВ и ЗАПИСОК целиком, пока не исправлено
- **Обновлено 19.08 (ночной цикл):** подтверждено без изменений — `events.yaml` по-прежнему закрепляет `identity_linked`/`consent_updated` за `product: practice` (`grep` этим циклом). `B-260818-03` (сам реестр событий МОМЕНТОВ) закрыт отдельно — эта находка не зависела от неё и не решилась заодно.
- **Обновлено 20.08 (ночной цикл):** подтверждено без изменений (`events.yaml:40-51`) — третьи сутки блокирует уже смёрженный транспорт МОМЕНТОВ (`compas-voice#3`, флаг `analyticsTransportEnabled=false` выключен именно из-за этого).
- **Обновлено 21.08 (ночной цикл):** борд N2 поднял до P0 со сроком «сегодня, 20.08» (`D-260820-08`), владелец CDO+CTO — срок пропущен, `events.yaml:40-51` не изменился, подтверждено `grep` этим циклом. Четвёртые сутки блокировки. Переназначено треку «реализация» без ожидания борда 24.08 — правка техническая, не решение типа 1 (`D-260821-04`).
- **Обновлено 22.08 (ночной цикл):** второй назначенный срок (20.08, затем 21.08) пропущен — `events.yaml:40-51` не изменился, `src/lib/analytics/schema.ts:75-76` по-прежнему отвергает событие при несовпадении продукта, подтверждено `grep` этим циклом независимо PO-МОМЕНТЫ. Пятые сутки блокировки уже смёрженного транспорта МОМЕНТОВ.
- **Обновлено 23.08 (ночной цикл):** без движения, шестые сутки блокировки, подтверждено PO-МОМЕНТЫ. Причина найдена — `B-260823-01`: трек «реализация» не запускался пять ночей, а даже при запуске не читает пункты бэклога вроде этого.
- **Обновлено 24.08 (ночной цикл):** закрыто. `cmpas.ru#112` (23.08 21:07 МСК) переводит `identity_linked`/`consent_updated` на список продуктов `[practice, zapiski, moments]` — подтверждено CPO напрямую (`analytics/schema/events.yaml:47-58`). `compas-voice#4`/`#5` тем же днём включили флаг `analyticsTransportEnabled` и поправили два реальных дефекта конверта (формат `ts`, отсутствующий `event_id`). **Не проверено вживую:** зелёный прогон `tests/analytics-ingest.test.ts` в этой сессии не выполнялся (нет сети на установку зависимостей у проверявшего агента) — закрытие основано на чтении кода и текста PR, не на собственном прогоне тестов; CTO стоит подтвердить прогон на своей стороне.
- **Обновлено 24.08 (борд N3):** оговорка снята — CTO и CDO независимо друг от друга прогнали `npx vitest run tests/analytics-ingest.test.ts` в `products/cmpas.ru`, оба получили 28/28 зелёных (`D-260824-07`). Закрытие подтверждено собственным прогоном, не только чтением кода.
- **Статус:** сделана — подтверждено дважды независимым прогоном тестов

#### B-260817-01 · Механика A «Тихая запись»: нейтральный текст напоминаний за фичефлагом
- **Продукт:** ПРАКТИКА · **Приоритет:** P1
- **Источник:** распоряжение учредителя `O-260816-04`, спецификация PO-ПРАКТИКА 17.08, `product/practice/CJM_booking_v1.md §1.3`
- **Текущее поведение:** `src/lib/cron/reminders.ts:79` (24-часовое напоминание) называет специальность и имя специалиста; 1-часовое (строка 134) уже нейтрально.
- **Желаемое поведение:** при включённом флаге `PsychologistSettings.privateRemindersEnabled` (новое поле, `@default(false)`) 24-часовое напоминание не содержит слова «психолог»/имени — только дату, время, формат, ссылку. Часть про нейтральный текст (без имени/специальности) включена по умолчанию отдельно от остального флага — см. решение CPO в `B-260816-02`.
- **Критерий готовности:** юнит-тесты (флаг off → текст как сейчас; флаг on → без имени/специальности; 1ч-текст не меняется) зелёные; аддитивная Prisma-миграция применяется без даунтайма.
- **Метрика проверки:** нет — приватность, не метрика
- **Владелец:** разработка
- **Эффект на соседние продукты:** нет
- **Статус:** предложена

#### B-260817-02 · Механика B «Подбор времени вместо сетки» за фичефлагом
- **Продукт:** ПРАКТИКА · **Приоритет:** P1
- **Источник:** распоряжение учредителя `O-260816-04`, спецификация PO-ПРАКТИКА 17.08, `product/practice/CJM_booking_v1.md`
- **Текущее поведение:** `src/app/bot/book/[psychologistId]/page.tsx` сразу показывает календарную сетку.
- **Желаемое поведение:** при включённом флаге `PsychologistSettings.timeSuggestEnabled` — экран с 2–3 пресетами времени и подбором ближайших подходящих слотов (новая функция `getSuggestedTimes`, переиспользует существующие `getAvailableDates`/`getAvailableTimesForDateStr`); ссылка «показать все» ведёт в текущую сетку без изменений.
- **Критерий готовности:** юнит-тесты на `getSuggestedTimes` по тегам предпочтений зелёные; regression-тест подтверждает, что при выключенном флаге поведение не меняется; флаг по умолчанию `false`.
- **Метрика проверки:** нет — гипотеза о снижении отвала непроверяема без разметки событий (`B-260813-03`)
- **Владелец:** разработка
- **Эффект на соседние продукты:** нет
- **Статус:** предложена

#### B-260818-03 · Реестр приёмника `cmpas.ru` не знает событий МОМЕНТОВ
- **Продукт:** платформа (`cmpas.ru`) · заказчик МОМЕНТЫ · **Приоритет:** P1
- **Источник:** обнаружено при исполнении `O-260817-14` (ветка `feature/analytics-transport`, `compas-psy/compas-voice#3`).
- **Текущее поведение (было):** `products/cmpas.ru/analytics/schema/events.yaml` перечислял только биллинговые события Т-Банка и `identity_linked`/`consent_updated`; событий МОМЕНТОВ не было вообще.
- **Обновлено 19.08 (ночной цикл):** закрыто. `compas-psy/cmpas.ru#94` «Единый реестр событий: приёмник знает все три продукта» смёржен 18.08 13:06 МСК (исполняет `O-260817-17`) — `events.yaml` теперь содержит `app_installed`, `practice_started`, `practice_finished`, `crossed_to_product` с `product: moments`, подтверждено `grep` этим циклом напрямую. PO-МОМЕНТЫ в брифе этой ночи не заметил мёрж и по инерции повторил старую находку — поправлено при merge. Оставшаяся часть исходной находки (кросс-продуктовые `identity_linked`/`consent_updated`) — отдельная, ещё не закрытая проблема, см. `B-260818-04`.
- **Критерий готовности:** событие МОМЕНТОВ с этими четырьмя именами и полями проходит `validateEvent`; событие с необъявленным полем по-прежнему отвергается. — выполнено.
- **Метрика проверки:** нет
- **Владелец:** разработка
- **Эффект на соседние продукты:** разблокирует доставку данных из `feature/analytics-transport` — приёмник больше не первая причина отказа; вторая (`B-260818-04`) остаётся.
- **Статус:** сделана

#### B-260817-06 · Реализовать биллинг-трекинг Т-Банк по `charter/13_TRACKING_PLAN.md`
- **Продукт:** платформа · **Приоритет:** P1
- **Источник:** распоряжение учредителя `O-260816-05` (адресат CPO); спецификация — новый документ `charter/13_TRACKING_PLAN.md`
- **Текущее поведение:** платежи в `cmpas.ru` уже идут через Т-Банк (`prisma/schema.prisma:707`, `tinkoffPaymentId`, `rebillId`), но нет вебхук-приёмника `NotificationPayment`, поля `terminal`, разовой сверки реестра, ежедневной сверки по T-API, отдельной таблицы подписок для MRR.
- **Желаемое поведение:** полный список — `charter/13_TRACKING_PLAN.md §1–6`. Коротко: приёмник вебхуков с проверкой `Token`, поле `terminal` в `Payment` (сайт/приложение — разные `TerminalKey`), разовый импорт, ежедневная сверка, таблица `Subscription`, события биллинга в будущий `analytics/schema/events.yaml`. `RebillId` и пароль терминала никогда не попадают в аналитику.
- **Критерий готовности:** см. `13_TRACKING_PLAN.md`; выкат — отдельный шаг после подтверждённого бэкапа, только по решению человека (устав §6.1).
- **Метрика проверки:** MRR, ARPU, конверсия триала, churn считаются из данных, а не оцениваются
- **Владелец:** разработка + CFO · зависит от `H-260813-03` (доступ к биллингу, Ф0 из `12_ANALYTICS.md §6`)
- **Эффект на соседние продукты:** да — правило «деньги живут на уровне аккаунта» (`05_METRICS §3.19`), MRR общий на портфель
- **Обновлено 19.08 (ночной цикл):** статус в бэклоге отставал от кода — бриф PO-ПРАКТИКА нашёл и CPO подтвердил `grep`: приёмник вебхуков (`src/lib/tinkoff.ts`, `src/app/api/payments/callback/route.ts`, `verifyNotificationToken`), поле `terminal` в `Payment`, таблица `Subscription`, скрипты сверки реестра (`scripts/reconcile-tinkoff.ts`, `import-tinkoff-registry.ts`) уже в коде. Выкат по-прежнему не делался (устав §6.1, ждёт подтверждённого бэкапа и решения человека) — код есть, факта включения на бою нет; сам приёмник без выгрузки биллинга (`H-260813-03`) числа для CFO не даёт, пока никто не смотрит на реальные вебхуки.
- **Статус:** в работе

#### B-260816-02 · Убрать имя специалиста и слово «психолог» из дефолтного текста напоминания клиенту
- **Продукт:** ПРАКТИКА · **Приоритет:** P1 · **Владелец:** разработка
- **Источник:** бриф PO-ПРАКТИКА, внеплановый цикл 16.08 — `src/lib/cron/reminders.ts:79`, шаблон напоминания за 24 часа: «...встреча с психологом ({psychologistName})»; напоминание за 1 час этого не делает.
- **Текущее поведение:** дефолтный текст напоминания раскрывает специальность и имя специалиста человеку, который мог бы не хотеть, чтобы это увидел кто-то ещё на его телефоне — уже сегодня, для реальных клиентов, а не только в целевом CJM.
- **Желаемое поведение:** нейтральный текст по умолчанию (дата, время, ссылка), как описано в `product/practice/CJM_booking_v1.md §1.3`.
- **Критерий готовности:** оба шаблона (24ч и 1ч) не содержат слова «психолог» и имени специалиста без явного согласия клиента.
- **Метрика проверки:** нет — вопрос приватности, не метрики
- **Эффект на соседние продукты:** нет
- **Merge-решение CPO 17.08:** инженерная спецификация `B-260817-01` реализует нужный текст, но за флагом `privateRemindersEnabled`, выключенным по умолчанию (так требует `O-260816-04` — контролируемая раскатка новых механик записи). Эта конкретная находка — красная линия приватности (`01_ROLES_PO §2`: «никаких уведомлений клиенту, которые специалист не одобрил»), а не гипотеза, которую нужно раскатывать постепенно. Решение: текст 24-часового напоминания без имени/специальности включается **по умолчанию**, независимо от остального флага `B-260817-01` — либо отдельным флагом с дефолтом `true`, либо вообще без флага (безусловная замена текста), решает разработка при реализации. Флаг `privateRemindersEnabled` остаётся выключенным по умолчанию только для остальной части механики A (собственно «тихая запись»).
- **Статус:** в работе — ждёт разработку с уточнённым выше критерием готовности

#### B-260813-07a · Разрешённые формулировки о приватности для внешних текстов
- **Продукт:** ЗАПИСКИ · **Приоритет:** P1 · **Владелец:** маркетинг + Legal
- **Критерий готовности:** письменно зафиксирован список разрешённых и запрещённых формулировок о шифровании и приватности до закрытия SEC-001.
- **Статус:** предложена

#### B-260813-10 · Tracking plan ПРАКТИКИ
- **Продукт:** ПРАКТИКА · **Приоритет:** P1 · **Владелец:** PO → CTO, ревью InfoSec
- **Суть:** девять событий воронки от регистрации до первой оплаты с обязательными свойствами и явным списком запрещённых полей.
- **Дополнено 16.08 (PO-ПРАКТИКА):** добавить в спецификацию признак когорты «сезон старта» (лето / не лето) на событии `signup` — первая когорта NSM стартует летом и без метки рискует прочитаться как провал вместо сезонного эффекта.
- **Критерий готовности:** ТЗ принято CTO без возврата на доработку; после внедрения строится воронка из пяти шагов.
- **Ответ CTO на борде N1:** возврат на доработку — предмета приёмки нет, нигде в репозитории не лежит документ с девятью событиями и списком запрещённых полей, только однострочные описания в бэклоге. Просьба к PO: оформить полноценный документ по образцу `charter/13_TRACKING_PLAN.md`.
- **Обновлено 23.08 (ночной цикл):** четвёртый цикл без документа, подтверждено PO-ПРАКТИКА — новая управленческая панель (PR #109) уже показывает пустой экран воронки записи как прямое следствие.
- **Обновлено 24.08 (ночной цикл):** пятый цикл без документа. Находка подтверждена ещё раз кодом, не только словами PO — `src/lib/panel/queries/funnel.ts:1-7` содержит комментарий разработки: «Воронка записи и источники привлечения зависят от событий, которых в реестре нет». PO-ПРАКТИКА берёт документ явным обязательством на сегодня.
- **Обновлено 24.08 (борд N3):** обязательство не выполнено — файла в `product/practice/` по-прежнему нет (шестой цикл без документа, второй сорванный срок после возврата CTO на N1). Борд дал явный чек-лист приёмки (`D-260824-04`, по образцу принятого документа МОМЕНТОВ): у каждого события — свой вопрос, ни одного свободнотекстового/дублирующего поля, пороги активации помечены гипотезой с планом пересчёта, явно указано реализованное в коде против предложения документа. Новый срок — 25.08.2026.
- **Обновлено 25.08 (ночной цикл):** третий сорванный срок подряд — проверено PO-ПРАКТИКА напрямую, `product/practice/` по-прежнему содержит только CJM-файлы, документа с девятью событиями и чек-листом борда нет. Это единственная P0/P1-находка в бэклоге, за которую отвечает не CTO/InfoSec/трек «реализация», а сам PO-продукта — процессный риск другой природы, чем простой implement-трека: работа не заблокирована внешней зависимостью. CPO поднимает как отдельный пункт повестки следующего борда, не переносит срок в четвёртый раз молча.
- **Обновлено 26.08 (ночной цикл):** четвёртый сорванный срок подряд — проверено PO-ПРАКТИКА напрямую: каталога `product/practice/` в репозитории по-прежнему не существует вовсе. Это единственная открытая P0/P1-находка портфеля, где причина простоя — не внешняя зависимость (трек «реализация», токен, ключ), а сам PO-продукта; переносить срок в пятый раз без разбора смысла не имеет.
- **Обновлено 27.08 (ночной цикл):** пятый сорванный срок подряд, подтверждено PO-ПРАКТИКА — `product/practice/` по-прежнему содержит только CJM-файлы и прототип, документа с чек-листом `D-260824-04` нет. PO-ПРАКТИКА взял написание документа явным обязательством на сегодня без внешних блокеров.
- **Статус:** предложена — пятый сорванный срок подряд; первый пункт повестки следующего борда наравне с `B-260823-01`

#### B-260813-11 · Спецификация D0-воронки МОМЕНТОВ
- **Продукт:** МОМЕНТЫ · **Приоритет:** P1 · **Владелец:** PO → CTO, ревью InfoSec
- **Суть:** девять событий с офлайн-буфером и досылкой; активация определена как первая завершённая практика в пределах 24 часов от первого запуска.
- **Критерий готовности:** спецификация принята CTO; воронка «первый запуск → первая практика» строится из данных.
- **Ответ CTO на борде N1:** тот же вердикт, что у `B-260813-10` — возврат на доработку, документа с девятью событиями нигде нет, только заголовки в `charter/12_ANALYTICS.md`. CDO дополнительно наложил вето на пороги активации как неизмеренные гипотезы (`05_METRICS §3.4`) — PO обязан перепроверить их на первых 200 когортах, а не закладывать как заданные.
- **Обновлено 20.08 (ночной цикл):** третий+ цикл подряд без движения. PO-МОМЕНТЫ берёт документ на себя следующим циклом по образцу `charter/13_TRACKING_PLAN.md` — не решение CTO, а обязательство PO написать черновик, чтобы было что приёмывать.
- **Обновлено 21.08 (ночной цикл):** черновик написан — `product/moments/D0_TRACKING_PLAN.md` (`D-260821-03`), девять событий, офлайн-буфер и досылка, активация помечена гипотезой с явной ссылкой на вето CDO. Ждёт приёмки CTO на ближайшем борде (24.08).
- **Обновлено 24.08 (борд N3):** принят CDO без правок (`D-260824-03`). Отдельное поручение PO-МОМЕНТЫ — привести строку «Ключевые события» в `05_METRICS §2.3` в соответствие с документом (документ сознательно не заводит `practice_abandoned`).
- **Статус:** сделана

#### B-260813-12 · Спецификация первой выгрузки биллинга
- **Продукт:** платформа · **Приоритет:** P1 · **Владелец:** PO + учредитель
- **Критерий готовности:** первый блок METRICS содержит не менее шести заполненных строк вместо одной-двух.
- **Статус:** предложена



#### B-260813-02 · Подключить первую волну источников данных
- **Продукт:** платформа · **Приоритет:** P1
- **Источник:** `03_SOURCES §5`
- **Текущее поведение:** ни один источник продуктовой аналитики не подключён; вся система работает на ручных выгрузках.
- **Желаемое поведение:** биллинг, Яндекс.Метрика и AppMetrica отдают данные в ночной цикл без участия человека.
- **Критерий готовности:** три источника читаются автоматически, ночной бриф помечен как «данные свежие» три дня подряд.
- **Владелец:** CTO · зависит от `H-260813-03`
- **Статус:** предложена

#### B-260813-03 · Определить и разметить события активации в трёх продуктах
- **Продукт:** платформа · **Приоритет:** P1
- **Источник:** `05_METRICS` — критерии активации заданы как гипотезы и не проверены на данных
- **Текущее поведение:** неизвестно, что именно делает пользователь в первую неделю и где отваливается.
- **Желаемое поведение:** размечены события воронки от регистрации до активации в каждом продукте, единый `account_id` связывает продукты.
- **Критерий готовности:** воронка строится из данных, а не из предположений; один человек в трёх продуктах считается один раз (`05_METRICS §3`).
- **Владелец:** CTO совместно с CPO
- **Статус:** предложена

#### B-260826-01 · Личность «Eliah Martynov» с прямыми коммитами в `main` `compas-voice` — подтвердить, человек это или агент
- **Продукт:** платформа · **Приоритет:** P0 (поднято с P1 27.08 — см. обновление ниже: новый коммит этой же личности содержит признак фабрикации, не только гипотезу о личности)
- **Источник:** бриф PO-МОМЕНТЫ, ночной цикл 26.08 — разбивка авторов прямых (non-merge) коммитов в `main` `compas-voice`: `compas-psy` 11, `Claude` 10, `Eliah Martynov` 9, `simpas-agent` 1 (всего 31).
- **Текущее поведение:** неизвестно, представляют ли коммиты от имени «Eliah Martynov» настоящие действия учредителя за клавиатурой (это разрешено и нормально) или агента, использующего человекоподобное имя автора git — что усугубляло бы уже открытый риск `B-260818-02`/`H-260818-01`: граница человек/агент и без того неразличима технически (один токен на всех), а человекоподобное имя автора коммита делает её неразличимой ещё и по логу.
- **Желаемое поведение:** CTO/InfoSec подтверждают происхождение этих коммитов — через аудит-лог GitHub организации (IP/User-Agent/сессия) либо прямым вопросом учредителю, было ли это его действие.
- **Критерий готовности:** ответ зафиксирован в `12_DECISIONS`.
- **Метрика проверки:** нет — процессная безопасность
- **Владелец:** CTO + InfoSec
- **Эффект на соседние продукты:** да — тот же вопрос стоит проверить в `cmpas.ru`/`zapiski`, если там встречаются коммиты за подписью, похожей на имя учредителя, но не подтверждённой как его собственная сессия.
- **Обновлено 27.08 (ночной цикл):** десятый коммит этой личности, `c1d3df8` (26.08, «Очередь аналитики: при переполнении не терять согласие»), содержательно корректен (тестовый регресс на потерю `consent_updated` из FIFO-очереди), но его commit message и тест ссылаются на тикет `B-260823` — такого тикета с этой темой не существует (`B-260823-01` в бэклоге — про `cron` `implement.yml`, не про аналитику; проверено `grep` по `state/10_BACKLOG.md` и по `products/compas-voice`). Правдоподобный, но непроверяемый номер задачи — именно то, о чём предупреждает `CLAUDE.md §5.1` про придуманные данные. Это не доказывает, что личность — агент, а не человек, но снимает презумпцию невиновности: реальный человек не сослался бы на несуществующий тикет в этом стиле. Поднято до P0.
- **Статус:** предложена

#### B-260813-04 · Зеркалить операционную память в compas-ops
- **Продукт:** платформа · **Приоритет:** P1
- **Источник:** `07_REPOS §4`
- **Текущее поведение:** состояние живёт только в проекте, без истории изменений и возможности отката.
- **Желаемое поведение:** каждый ночной цикл и каждый борд оставляют коммит в `compas-ops` через pull request.
- **Критерий готовности:** три ночных цикла подряд отражены в истории репозитория.
- **Владелец:** COO · зависит от `H-260813-02`
- **Статус:** предложена

---

## P2 — месяц

#### B-260817-05 · Отражать бэклог в GitHub Issues: метки, одностороннее зеркало, синхронизация комментариев в распоряжения
- **Продукт:** платформа · **Приоритет:** P2 · **Владелец:** COO + CTO
- **Источник:** распоряжение учредителя `O-260816-03`
- **Текущее поведение:** бэклог существует только как `state/10_BACKLOG.md`; в GitHub Issues компас-ops ничего не отражается.
- **Желаемое поведение:** метки `P0`, `P1`, `P2`, `P3`, `практика`, `записки`, `моменты`, `платформа` заведены; на каждую открытую строку бэклога — открытый issue с тем же идентификатором в заголовке и метками приоритета/продукта; закрытая строка бэклога закрывает issue. Правда всегда `state/10_BACKLOG.md`, issues — производная, синхронизация только в одну сторону (backlog → issues). Комментарии учредителя в issue за сутки переносятся в `state/15_ORDERS.md` как распоряжения, ответ — в том же issue.
- **Критерий готовности:** восемь меток созданы; скрипт/воркфлоу открывает issue на каждую P0–P2 строку бэклога и закрывает при смене статуса на «сделана»/«отклонена»; комментарии учредителя долетают до `15_ORDERS.md` в течение цикла.
- **Метрика проверки:** нет — операционная гигиена
- **Блокер:** сессия ночного цикла не авторизована в `gh` (`gh auth status` → not logged in), и даже с токеном текущий доступ раннера — только `Contents: Read-only` (см. остаточный пробел `H-260813-02` в `11_HUMAN_QUEUE`, «Выполнено»): для issues/labels нужен токен с правами `Issues: Write`. Реализация не начата до выдачи такого токена — `/human`-задача не заведена в этом цикле, очередь на лимите (5/5); кандидат первым при освобождении слота.
- **Эффект на соседние продукты:** нет
- **Статус:** предложена — задача заведена по распоряжению `O-260816-03`, `.zapiski/` добавлен в `.gitignore` этим же циклом

#### B-260817-07 · Не потерять ветку `feature/beta-feedback` в `zapiski` до получения документа спецификации
- **Продукт:** ЗАПИСКИ · **Приоритет:** P2 · **Владелец:** CTO
- **Источник:** бриф PO-ЗАПИСКИ 17.08 — распоряжение `O-260816-06` ссылается на `claude/product/ZAPISKI_feedback_beta.md`, которого нет нигде в `compas-ops`; при этом в `zapiski` уже существует незамерженная ветка `feature/beta-feedback` (коммит `aef9676`, 16.08 20:00 UTC) с начатой TDD-реализацией: `server/src/routes/feedback.ts`, `server/migrations/0005_feedback.sql`, `packages/core/src/feedback/{events,report}.ts`, `packages/app/src/state/feedback.ts`. Сообщение коммита прямо ссылается на спецификацию из документа, который в этом репозитории отсутствует — то есть кто-то уже видел этот документ не в `compas-ops`, а работа прервана «по просьбе заказчика».
- **Текущее поведение:** ветка существует, никуда не деплоится, не влияет на `main` (которого в `zapiski` тоже нет). Риск — что следующий агент, получив документ, начнёт реализацию с нуля, не зная о ветке.
- **Желаемое поведение:** на ближайшем борде CTO явно решает — продолжать ветку `feature/beta-feedback` как основу или начинать заново, как только документ станет доступен.
- **Критерий готовности:** решение зафиксировано в `12_DECISIONS`.
- **Метрика проверки:** нет
- **Эффект на соседние продукты:** нет
- **Обновлено 19.08 (ночной цикл):** похоже на самороспуск — PO-ЗАПИСКИ нашёл коммит `a98026b` («Обратная связь в продукте: слита ветка беты в ветку по умолчанию», 17.08 15:15 UTC), который выглядит как слияние именно этой ветки. Не закрываю без подтверждения: у PO нет доступа сравнить содержимое веток построчно с исходным `feature/beta-feedback` и подтвердить, что это то же самое дерево, а не одноимённая по смыслу, но независимая работа. Нужно решение CTO на ближайшем борде — как и было запрошено изначально, только теперь с готовым кандидатом на ответ.
- **Обновлено 20.08 (ночной цикл):** подтверждено построчно — второй родитель merge-коммита `a98026b` (`77bf91c`) содержит именно файлы ветки `feature/beta-feedback` (`packages/core/src/feedback/{events,report,prompt}.ts`, `server/src/routes/feedback.ts`, миграция `0006_feedback.sql`, на один номер новее упомянутой здесь `0005` — из-за разрешения конфликта при слиянии). Это то же дерево, не одноимённая независимая работа. Решение зафиксировано `D-260820-02`, задача закрывается.
- **Статус:** сделана

#### B-260816-03 · Решить статус платного экрана МОМЕНТОВ (399 ₽/мес) против модели «бесплатный вход в экосистему»
- **Продукт:** МОМЕНТЫ · **Приоритет:** P2 (решение борда) · **Владелец:** CPO
- **Источник:** бриф PO-МОМЕНТЫ, внеплановый цикл 16.08 — первый доступ к репозиторию `compas-voice` за четыре цикла. `android/app/src/main/java/ru/cmpas/voice/ui/paywall/PaywallScreen.kt` (~строка 102): «Потом 399 ₽ в месяц», триал 7 дней. `05_METRICS §2.3`: «МОМЕНТЫ не монетизируются напрямую в текущей модели». `01_ROLES_PO §4`: продукт бесплатный, экономика меряется переходами в другие продукты.
- **Текущее поведение:** пейволл в коде есть, но выключен флагом (`docs/ROADMAP.md`: гейтинг каталога — OFF), вреда пользователю сейчас нет; расхождение только между кодом и принятой моделью.
- **Обновлено 17.08 (бриф PO-ПРАКТИКА):** та же цена — 399 ₽/мес, 7 дней бесплатно — найдена ещё раз на статической странице `sites/moments.cmpas.ru/public/index.html` (строки 7, 66, 176, 180) внутри репозитория `cmpas.ru`, с собственным изолированным деплоем (`deploy-moments.yml`), не завязанным на основной Docker-деплой. Неизвестно, опубликована ли страница уже сейчас (доступа проверить нет). Тот же паттерн, что у промостраницы ЗАПИСОК (`B-260816-01`): публичный текст может обогнать решение борда. Не поставлено в очередь `/human` отдельно — очередь на лимите (5/5); эскалировано ⚠️ в сводке цикла на рассмотрение борда 17.08 напрямую.
- **Желаемое поведение:** явное решение борда — либо МОМЕНТЫ остаются бесплатными и флаг/экран/промостраница удаляются, либо модель портфеля пересматривается и это фиксируется в `05_METRICS §2.3`.
- **Критерий готовности:** решение зафиксировано в `12_DECISIONS`; до этого флаг гейтинга не включается и промостраница с ценой не публикуется — зафиксировать явным пунктом.
- **Метрика проверки:** нет — вопрос модели, не метрики
- **Эффект на соседние продукты:** да — правило «единый биллинг на аккаунт» (`05_METRICS §3.19`)
- **Решение борда N1:** `D-260817-03` — МОМЕНТЫ остаются бесплатными без изменений модели; флаг гейтинга остаётся выключен, страница `sites/moments.cmpas.ru` с ценой 399 ₽/мес не публикуется до отдельного пересмотра портфеля с CFO.
- **Статус:** сделана

#### B-260813-13 · Проверить листинги и текущую версию МОМЕНТОВ по чек-листу анти-паттернов
- **Продукт:** МОМЕНТЫ · **Приоритет:** P2 · **Владелец:** PO + Legal
- **Суть:** streak и счётчики дней, бейджи и уровни, напоминания с упрёком, запрос уведомлений до первой практики, обещания эффекта в названиях практик и в листинге, стоп-слова бренда, любая реклама внутри.
- **Критерий готовности:** список найденного; каждая строка либо исправлена, либо получила решение борда. Зависит от ссылок на листинги и сборку.
- **Обновлено 18.08 (ночной цикл):** чек-лист пройден по коду (`compas-voice`, `origin/main`) целиком, находок нет — streak/бейджи/уровни отсутствуют, тревожных напоминаний нет (напоминаний вообще ещё нет), `POST_NOTIFICATIONS` намеренно не запрашивается до появления напоминаний, обещаний эффекта в названиях нет, есть явный антидисклеймер в онбординге, рекламы внутри нет. Ссылки на листинги в сторах по-прежнему не проверены (не присылались) — это единственная непокрытая часть критерия готовности.
- **Статус:** сделана — код чист; проверка листингов в сторах остаётся отдельным пунктом без блокирующего срока

#### B-260813-14 · Вынести на борд продуктовый статус ЗАПИСОК
- **Продукт:** ЗАПИСКИ · **Приоритет:** P2 (решение борда 17.08) · **Владелец:** CPO
- **Суть:** в коде это самостоятельный заметочник с собственным аккаунтом, собственным биллингом и ценами 199/149 ₽ при триале 14 дней, без понятия сессии специалиста. В документах — часть среды специалиста с едиными тарифами 990/1990 и триалом 30 дней. Пока вопрос открыт, правило «один человек — один аккаунт» не выполняется, MRR и churn по портфелю не сходятся, а North Star продукта технически неизмерима.
- **Обновлено 15.08:** заказчик отключил оплату на MVP флагом `BILLING_ENABLED=false` (коммит `c9a0fc4`) — экраны тарифов спрятаны, деньги ни с кого не берутся. Собственная модель биллинга при этом не удалена, а отложена в коде под флагом; вопрос про единый аккаунт этим не закрыт, только временно не даёт себя почувствовать на реальных платежах.
- **Обновлено 16.08 (внеплановый цикл):** третья цена нашлась на публичной промостранице (`apps/web/public/promo/index.html:667–669`) — «ЗАПИСКИ+» 299 ₽/мес (249 ₽ при годовой оплате), триал 14 дней. Совпадает с кодом только по длительности триала. Итого три модели одновременно: код (199/149 ₽), документы (990/1990 ₽), промостраница (299/249 ₽). См. также `B-260816-01`.
- **Критерий готовности:** борд принял решение о продуктовой модели и зафиксировал его в журнале.
- **Решение борда N1:** `D-260817-04` — архитектурный вопрос уже закрыт уставом §1 («один аккаунт, один биллинг»): собственная модель биллинга ЗАПИСОК — технический долг, устраняется при реализации `B-260817-06`. Открытым остаётся только вопрос итоговой цены (990/1990 ₽ из документов не подтверждены учредителем как решение) — это тип 1, покрыт `H-260816-01`.
- **Статус:** в плане

#### B-260815-02 · Восстановить ветку main и процесс ревью в репозитории ЗАПИСКИ
- **Продукт:** ЗАПИСКИ · **Приоритет:** P2 · **Владелец:** CTO
- **Источник:** бриф PO-ЗАПИСКИ 15.08 — в `compas-psy/zapiski` нет ветки `main`: есть только `claude/cloud-deployment-cross-platform-9nenw7` (сейчас default branch) и `claude/task-from-package-9if81j`; PR и issues через API не находятся. Весь код идёт прямыми коммитами в рабочую ветку агента без отдельного процесса ревью/мержа.
- **Текущее поведение:** нет разделения «основная ветка / ветка в работе», нет PR-ревью; CI при этом зелёный на всех прогонах.
- **Желаемое поведение:** решение CTO — вернуть `main` и процесс ревью, либо явно принять текущую схему как рабочую и назвать причину.
- **Критерий готовности:** решение зафиксировано на борде 17.08 (в `12_DECISIONS`, если принято) — это не обвинение, а вопрос к CTO (`01_ROLES_PO §1.4`).
- **Решение борда N1:** `D-260817-02` — восстановить `main` от текущего HEAD (`c529b14`) без новых коммитов и включить процесс ревью; исполняется вторым PR после `B-260817-08`, сразу за устранением автодеплоя (`B-260817-04`).
- **Обновлено 18.08 (ночной цикл):** не сделано и не может быть штатно сделано этим циклом. Ночному циклу PRODUCTS_READ_TOKEN не передан переменной окружения (в отличие от трека «реализация» — там он есть); единственный доступный путь — извлечь токен из URL `git remote` и подставить его в команды `git`/`gh` вручную, что само по себе означает «токен попал в переписку» и требует отзыва по `CLAUDE.md §5.4`. Ночной цикл отказался это делать. Задача технически тривиальна (создать ветку `main` от текущего HEAD без единого нового коммита) — но должна пройти через трек «реализация» (`implement.yml`, GH_TOKEN уже в окружении штатно) или напрямую через учредителя, не через извлечение секрета вручную.
- **Дополнительно найдено 18.08:** ветка по умолчанию `zapiski` (та, от которой планировалось создать `main`) за 17.08 получила прямое слияние `compas-psy/zapiski#1` без ветки `main` и без выделенного ревью — см. `H-260818-01`. Восстанавливать `main` нужно от актуального HEAD, а не от `c529b14`, зафиксированного на момент борда.
- **Статус:** предложена — ждёт трека «реализация» или прямого действия человека

#### B-260813-05 · Заполнить карточки продуктов реальными значениями
- **Продукт:** все · **Приоритет:** P2
- **Источник:** `05_METRICS §2` — целевые значения на 3/6/12 месяцев выведены из стратегии и помечены как ориентиры
- **Критерий готовности:** у каждого продукта есть текущее значение North Star и цель, подтверждённая бордом.
- **Владелец:** CPO
- **Статус:** предложена

#### B-260815-03 · Починить сборку PDF деков теперь, когда цикл сам работает на раннере
- **Продукт:** платформа · **Приоритет:** P1 (поднято бордом N2, `D-260820-09` — шесть циклов подряд без результата, откат на HTML противоречит причине выбора PDF) · **Владелец:** COO
- **Источник:** доставка ночного цикла 15.08 — `tools/deck_to_pdf.py` не уложился в 90 секунд на этом же раннере GitHub Actions (те же симптомы dbus/GCM, что и в `D-260813-08`: `ERROR:dbus/object_proxy.cc`, `DEPRECATED_ENDPOINT`).
- **Текущее поведение:** `D-260813-08` решила, что PDF собирает «агент», а раннер только отправляет готовый файл — это разделение предполагало, что агент и раннер разные машины. После `D-260815-02` ночной цикл сам выполняется на раннере GitHub Actions, то есть агент и раннер — одна машина, и посылка сломана: PDF не собирается нигде. Сработал встроенный откат `deliver.py` на HTML (`D-260813-04`) — доставка не потеряна, но учредитель получил файл, который на телефоне не листается предпросмотром, а именно это `D-260813-04` называла причиной перехода на PDF.
- **Желаемое поведение:** PDF снова собирается — либо headless-браузер на раннере запускается с рабочим набором флагов под этот конкретный образ (не тот же перебор, что в `D-260813-08` — та серия попыток была на другом раннере/образе), либо решение `D-260813-08` пересматривается заново с учётом того, что «агент = раннер» теперь всегда.
- **Критерий готовности:** ночной цикл или борд доставляет PDF, а не откатывается на HTML.
- **Владелец:** COO
- **Эффект на соседние продукты:** нет — только доставка
- **Обновлено 21.08 (ночной цикл, эксперимент по `D-260820-09`):** причина не в бренде браузера — полная сборка Chromium из кэша Playwright зависает так же, как google-chrome; headless-only сборка (`chromium_headless_shell`) печатает тот же дек за секунды. `tools/deck_to_pdf.py` теперь ищет её первой (`D-260821-05`). Проверено доставкой: PDF `daily_2026-08-21.md` собран и отправлен учредителю.
- **Статус:** сделана

#### B-260813-06 · Закрыть пробелы измерения из списка «не умеем мерить»
- **Продукт:** платформа · **Приоритет:** P2
- **Источник:** `05_METRICS §6`
- **Критерий готовности:** список сокращён вдвое; для оставшегося есть план или явный отказ измерять.
- **Владелец:** CPO совместно с CTO
- **Статус:** предложена

---

## Отклонено

_пусто_
