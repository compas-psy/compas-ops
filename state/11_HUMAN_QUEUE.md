# Очередь задач учредителю

Правила — устав §6.4. Максимум пять открытых задач, сортировка по блокирующей силе. Ведёт CEO-агент.
Обновлено: 2026-09-02, агент реализации (внеочередная запись — не ночной цикл).

**Открыто: 8 (лимит 5, превышение оправдано ниже) · У учредителя: 0 · Выполнено: 3**
**Обновлено 24.08 (борд N3):** ⚠️ `H-260818-01`/`H-260818-02` не закрыты седьмой цикл подряд после срабатывания порога `D-260820-06`. CTO и InfoSec независимо подтвердили на борде: токен не ротирован, branch protection не включена, ключ не отозван — и нашли отягчающее: `PRODUCTS_READ_TOKEN` физически лежит открытым текстом в `.git/config` локальных клонов `cmpas.ru`/`compas-voice` (`D-260824-08`), а `deploy-docker.yml` по-прежнему деплоит на любой push в `main`, не только на PR-мёрж (`B-260824-03`) — барьер между действием агента и продакшеном отсутствует технически на всех трёх уровнях сразу (ревью, прямой пуш, автодеплой). `H-260818-01` просрочена на пятые сутки (срок был 19.08), `H-260818-02` — на шестые сутки (срок был 18.08). `H-260813-03` (тишина данных) — двенадцатые сутки подряд без единой строки в `13_DATA_INBOX`. Новая задача: `H-260824-01` (договор эквайринга Т-Банка) — находка бухгалтера/CFO с N2 переведена в формальную задачу очереди (`D-260824-10`), четыре дня простояла сноской без ответа.
**Обновлено 25.08 (ночной цикл):** без единого ответа за сутки — ни один из 15_ORDERS.md новых распоряжений не появилось. `H-260818-01` просрочена на шестые сутки, `H-260818-02` — на седьмые, `H-260813-03` — тринадцатые сутки тишины данных. `H-260824-01` (срок был вчера, 24.08) впервые просрочена — договор эквайринга Т-Банка не подтверждён, платежи ПРАКТИКИ продолжают идти боевым образом без ответа. Отягчающее к `H-260818-01`/`H-260818-02`: паттерн прямого пуша в `main`/автопубликации на push, уже P0 в `cmpas.ru`, подтверждён и в `compas-voice` (`B-260824-03`) — тот же admin-токен затрагивает все три `products/` разом, не один репозиторий.
**Обновлено 26.08 (ночной цикл):** без единого ответа вторые сутки подряд. `H-260818-01` просрочена на седьмые сутки, `H-260818-02` — на восьмые, `H-260813-03` — четырнадцатые сутки тишины данных, `H-260824-01` — второй день просрочки. `H-260816-01`: решение борда `D-260824-09` («снять сегодня», 24.08) не исполнено уже второй день — трек «реализация» третью ночь подряд не запускался, PR не готовился физически. Новое отягчающее к `H-260818-01`: PO-ПРАКТИКА при рутинной проверке `git remote -v` в своей рабочей копии увидел `PRODUCTS_READ_TOKEN` в открытом виде в URL — токен нигде не воспроизведён, URL очищен немедленно; это тот же класс экспозиции, что уже зафиксирован `D-260824-08` (токен в `.git/config`), теперь подтверждённый ещё одним независимым наблюдением. Отдельно PO-МОМЕНТЫ обнаружила в `compas-voice`, что 9 из 31 прямых коммитов в `main` подписаны именем «Eliah Martynov» (человекоподобным, не ботовским) — не решаю здесь, было ли это реальное действие учредителя или агент; вопрос заведён отдельной находкой `B-260826-01`, не `/human`-задачей (это вопрос CTO/InfoSec к самим себе через аудит-лог, а не то, что требует минут учредителя, пока не появится основание считать иначе).

**Почему семь, а не пять.** `H-260818-02` — активный инцидент безопасности (приватный ключ доступен публично прямо сейчас) и `H-260824-01` — деньги, идущие боевым образом без подтверждённого договора: устав §6.2 относит и доступы/токены/инциденты, и деньги к автоматическому `/human` без исключения по лимиту. Вытеснять что-то из уже открытых пяти ради них неверно: остальные пять не теряют силу оттого, что нашлись шестая и седьмая, тоже срочные. Как только любая из семи закрывается — лимит снова соблюдён без дополнительных действий.

---

## Открытые

#### H-260902-01 · Хранилище точек возврата — ЗАКРЫТА 02.09.2026, действий владельца не потребовалось
- **Категория:** доступ / деньги (сторонний сервис) — оказалась не нужна
- **Итог.** Задача закрыта силами агента, ваше время не потребовалось и платить ни за что не пришлось. Offsite-бэкап работает: снапшот снят и проверен восстановлением 02.09.2026.
- **Что было.** Пять прогонов подряд падали на `webdav.yandex.ru` с `Fatal: unable to save snapshot: 500`, каждый по 13–20 минут. Настройки клиента не помогли ни разу: таймауты, ограничение частоты запросов, буферизация выгрузки, размер пачки — проверено и опровергнуто по очереди, прогоны `#142`, `#144`, `#148`.
- **Что оказалось на самом деле.** Разведка содержимого (`#147`) показала: в снапшоте от 01.09 лежали 284 файла и 4.699 МиБ, то есть конфигурация и база — но НЕ корпус. Он приехал на сервер позже этого снапшота, и каждый следующий прогон впервые пытался выгрузить шестьдесят мегабайт за раз. Именно этого Яндекс и не переваривает; ежедневный инкремент почти без новых данных проходил за двадцать секунд десятки раз. То есть хранилище не было сломано — сломан был способ первой заливки.
- **Что помогло.** Не настройка, а форма выгрузки. `scripts/backup-seed.sh` заливает репозиторий порциями по десять файлов; каждая успешная порция закрепляется навсегда, restic адресует данные содержимым, повтор не отправляет уже лежащее. Прогон `#149`: **19 порций из 19, ноль отказов, 3 минуты 4 секунды**. Отдельно нашёлся эксклюзивный лок суточной давности от оборванного прогона, из-за которого падал `forget --prune` уже после успешного снапшота, — добавлен `restic unlock`.
- **Состояние сейчас:** offsite-бэкап снят после завершения R1 (`#163`) и проверен восстановлением на нём же (`#164`), включая проверку, что восстановленная база не возвращает health-копию в общую схему. Ретеншен приведён к §26.3: 7 ежедневных, 4 еженедельных, 6 ежемесячных.
- **Что осталось знать.** Хранилище по-прежнему плохо переносит разовые большие выгрузки. Если корпус когда-нибудь вырастет скачком (новая крупная пачка, смена структуры путей), первый бэкап после этого надо делать `action=seed`, а не `action=backup`. Это записано в `docs/handoff/BACKUP_RESTORE.md`.
- **Статус:** закрыта

---

#### H-260818-02 · Отозвать приватный SSH-ключ, найденный в публичном репозитории `cmpas.ru`, и решить об истории `main` ⚠️ инцидент
- **Категория:** доступ / токен / безопасность (устав §6.2 — доступы и инциденты становятся `/human` автоматически)
- **Почему не агент:** отзыв/ротация ключа — действие в системах за пределами репозитория (сервер, GitHub SSH keys и т.п.), агент туда не имеет доступа и не должен получать (устав §6.1 «не выдают и не хранят доступы... сверх минимально необходимого»); переписывание истории `main` — необратимое в части уже случившегося действие (репозиторий публичный, снимки уже могли быть склонированы или проиндексированы) — решение типа 1, устав §6.3.
- **Что обнаружено в этом прогоне.** При подготовке к реализации распоряжений в `products/cmpas.ru` обнаружен файл `antigravity_key` (и `antigravity_key.pub`) в рабочей копии — это настоящий приватный SSH-ключ формата OpenSSH (`ssh-ed25519`, отпечаток `SHA256:VK0R69yEzaIjnQ5FfoKtI8Ji4f7b2x4q0QFMn0OTmDk`, комментарий в публичной части `eliah@Eliah` — похоже на личный ключ с машины учредителя). Файл не временный и не игнорируемый — он закоммичен в `main` и отдаётся всем, кто клонирует репозиторий: `git ls-files` подтверждает трекинг, `git status` — рабочее дерево чистое. Репозиторий `compas-psy/cmpas.ru` **публичный** (проверено `gh repo view --json isPrivate` → `false`) — это значит ключ доступен любому в интернете с момента коммита, не только внутренней команде.
- **Второе, отдельное от ключа, но найденное тем же осмотром.** Коммит, впервые добавивший ключ (`bfdc2eeb`, автор `simpas-doctor <bot@users.noreply.github.com>`, сообщение «Диагностика базы: состояние журнала миграций», 2026-08-17 13:34:30 UTC), — это **корневой коммит текущей истории `main`** (`git rev-list --max-parents=0 HEAD` возвращает только его; `git rev-list --count HEAD` = 20 коммитов всего). Он разом добавляет 579 файлов — весь кодовую базу целиком, включая мусорные артефакты явно из чьей-то локальной рабочей директории (`_tmp_migrate.sql`, `fix_schema.sql`, `migration.sql`, файл с обрубленным именем вида кода React `sName={() =...font-medium}`, файл `how f8c8291` — вывод `git log`/`git show` одного коммита, перенаправленный в файл). Действующий сейчас `.github/workflows/db-doctor.yml` коммитит только один файл (`docs/ops/db-doctor.md`) — он **не может** быть причиной этого коммита в его нынешнем виде; либо более ранняя версия скрипта делала `git add -A` из произвольной рабочей директории, либо коммит с той же ботовской личностью был сделан вручную/из другого процесса. История `main` до этого коммита не читается (`bfdc2eeb~1` не существует) — она либо никогда не пушилась в этот репозиторий, либо была замещена принудительным пушем; отличить одно от другого агент не может, только доказать факт (корень истории здесь, а не раньше).
- **Что заблокировано без этого:** доверие к содержимому `main` как к единственной правде (§5.3) — если история `main` может быть молча заменена процессом с ботовской личностью, любой аудит «что сейчас на бою» ненадёжен, пока это не объяснено. Также блокирует спокойное продолжение работы в `products/cmpas.ru` в рамках открытых распоряжений — риск повторной утечки в тот же корень, пока источник не найден.
- **Время учредителя:** 20–30 минут
- **Срок:** сегодня, 2026-08-18 · **Приоритет:** блокирует (инцидент, не рутина)
- **Как сделать:**
  1. Считать ключ `eliah@Eliah` скомпрометированным немедленно — отозвать/удалить его из любого `authorized_keys` или сервиса, куда он даёт доступ (сервер, GitHub, что угодно ещё); если это личный ключ учредителя — перевыпустить и обновить везде, где он был доверен.
  2. Проверить лично в GitHub (Settings → Security → Audit log репозитория и организации), кто и как выполнил force-push/замену истории `main` вокруг 2026-08-17 13:30–13:40 UTC под личностью `simpas-doctor` — это либо раскроет реальный процесс (устаревший workflow, ручной запуск, скомпрометированный токен), либо покажет, что аудит-лог этого не видит и нужен более глубокий разбор с GitHub Support.
  3. Решить: переписывать ли историю `main` (`git filter-repo` + принудительный пуш), чтобы убрать ключ и мусорные файлы физически из репозитория, зная, что публичная экспозиция уже случилась (главная польза — не сама секретность, она уже нарушена, а чтобы новые клоны/форки/индексаторы не продолжали разносить ключ дальше, и чтобы репозиторий не нёс мусор вроде `_tmp_migrate.sql`/`fix_schema.sql`/`migration.sql` в корне вечно). Переписывание истории публичного репозитория — необратимо для всех, кто уже успел его склонировать; решение и координация (кто перебазирует локальные копии) — за учредителем.
  4. Независимо от решения по истории — удалить `antigravity_key`, `antigravity_key.pub`, `_tmp_migrate.sql`, `fix_schema.sql`, `migration.sql`, `how f8c8291` и файл с обрубленным именем из текущего `HEAD` веткой/PR (это разрешено агенту — обычное изменение, не перезапись истории); заведено `B-260818-05` на этот PR, дальше в этой же сессии.
- **Обновлено 19.08 (ночной цикл):** пункт 4 выполнен — `compas-psy/cmpas.ru#106` смёржен 18.08 15:44 МСК, все перечисленные файлы отсутствуют в актуальном `HEAD` (проверено `git ls-files` этим циклом). Пункты 1–3 (отзыв/ротация ключа, проверка аудит-лога GitHub, решение о переписывании истории) остаются полностью за учредителем — ничего в них не изменилось, файловая уборка не заменяет отзыв ключа.
- **Обновлено 20.08 (ночной цикл):** без изменений по пп.1–3, срок (был 18.08) просрочен уже на двое суток. См. также найденное сегодня по `H-260818-01` — прямые коммиты ботов в `main` в том же временном окне, что диагностика этого инцидента, усиливают, а не ослабляют аргумент за скорейшее решение по пп.2–3 (кто и как мог менять историю/пушить напрямую).
- **Обновлено 21.08 (ночной цикл):** ⚠️ без изменений по пп.1–3, просрочено уже на трое суток — порог `D-260820-06` сработал (`D-260821-01`). Новый прямой коммит бота в `main` `cmpas.ru` найден этим циклом (`47260c6`, 20.08 12:58 UTC) — тот же паттерн, не новый по типу, но подтверждает, что риск активен прямо сейчас, не только исторически.
- **Обновлено 22.08 (ночной цикл):** ⚠️ без изменений по пп.1–3, просрочено на четверо суток. Найден сам механизм, а не очередной симптом: `products/cmpas.ru/.github/workflows/db-doctor.yml` штатно коммитит и пушит в `main` под личностью `simpas-doctor`, минуя PR (`B-260822-01`) — это объясняет происхождение как минимум части найденных ранее прямых коммитов и напрямую влияет на пп.2–3: то, что выглядело как «замена истории неизвестным процессом», частично объяснимо этим workflow.
- **Обновлено 23.08 (ночной цикл):** ⚠️ без изменений по пп.1–3, просрочено на пятеро суток.
- **Обновлено 24.08 (ночной цикл):** ⚠️ без изменений по пп.1–3, просрочено на шестеро суток. Найдено отягчающее доказательство: подтверждено, что в `main` `cmpas.ru` напрямую пишет не только диагностический бот, но и сам агент реализации под личностью `Claude` (`B-260824-01`) — круг того, что могло менять историю/пушить напрямую, расширился, а не сузился, прямо усиливая пп.2–3.
- **Рекомендация:** пункт 1 (отзыв ключа) не терпит отлагательств и не зависит от решения по истории — сделать его первым, сегодня, вне зависимости от того, когда дойдут руки до пунктов 2–3. Цена ошибки: если ключ даёт доступ к серверу или другим системам СИМПАС, а не только к репозиторию, окно между этой находкой и отзывом — единственное время, когда кто-то посторонний может успеть им воспользоваться.
- **Статус:** открыта — частично исполнена (п.4), пп.1–3 просрочены на шестеро суток

---

#### H-260813-03 · Дать первый срез данных или доступ к биллингу ⚠️ просрочено · порог P0 наступил
- **Категория:** доступ
- **Почему не агент:** нет ни одного подключённого источника аналитики; агент не может добыть цифры, которых не видит, и обязан писать «данных нет» вместо догадок.
- **Что заблокировано без этого:** ни один из 39 порогов `05_METRICS §4` не может сработать; CFO на борде N1 сообщил, что функция CFO физически не существует без этого доступа — не «отчёт неполный», а мандата исполнить нечем; CDO — 0 метрик из дерева `05_METRICS §1.3` посчитаны. Порог «тишина данных» (`05_METRICS §4.5`) сегодня, 19.08, пробил семь суток подряд без единой строки в `13_DATA_INBOX` (13–19.08) — по правилу это переход с P1 на **P0**: «CEO выносит на ближайший борд как риск управления вслепую». Также блокирует `B-260817-06` (биллинг-трекинг Т-Банк уже частично в коде, но без данных нечем считать MRR/ARPU).
- **Время учредителя:** 15 минут разово, дальше 2 минуты в день
- **Срок:** был 16.08.2026, просрочен на восемь суток · **Приоритет:** P0 — блокирует; порог «тишина данных» пробил двенадцатые сутки подряд (13–24.08), первый пункт повестки борда сегодня, 24.08
- **Как сделать:** заполнить один блок `METRICS` по шаблону `03_SOURCES §2.1` в `13_DATA_INBOX` — сколько видно без усилий, остальное прочерками. Минимум, который решает половину: платящих всего, активных триалов, сессий за прошлую неделю.
- **Рекомендация:** начать с выгрузки биллинга — одним экспортом закрываются MRR, ARPU, конверсия триала, churn и involuntary churn, то есть четыре порога P0 сразу. Деньги — единственная метрика, которая не требует инструментовки в коде.
- **Статус:** открыта

---

#### H-260813-05 · Снять публичное обещание, которое код не выполняет ⚠️ просрочено
- **Категория:** публикация / юридическое (решение типа 1)
- **Почему не агент:** текст отдаётся пользователю и входит в юридические документы; изменение публичного обещания и правки в политике конфиденциальности — за человеком, устав §6.1.
- **Что заблокировано без этого:** `B-260813-07` (P0), открытие регистрации внешним пользователям, любая коммуникация маркетинга про приватность ЗАПИСОК. Находка расширилась: фраза теперь на трёх живых поверхностях — письмо входа, промостраница, экран входа приложения (`packages/app/src/i18n/ru.ts:141`, `en.ts:103`).
- **Время учредителя:** 15 минут
- **Обновлено 24.08 (ночной цикл):** без изменений — все три поверхности подтверждены `grep` этим циклом напрямую.
- **Срок:** был 14.08.2026, просрочен на десять дней · **Приоритет:** блокирует
- **Суть.** Письмо входа, черновик политики и (с 17.08) экран входа приложения утверждают, что мы технически не можем прочитать заметки пользователя. Внутренний аудит в том же репозитории фиксирует обратное: незашифрованная заметка уходит в облако открытым текстом, находка имеет уровень «критический» и статус «блокирует приёмку», регрессионный тест на текущей ветке помечен как заведомо падающий.
- **Как сделать:** утвердить одну из формулировок и дать команду разработке заменить текст во всех трёх поверхностях, в черновике политики и в документации.
- **Рекомендация (уточнена Legal на борде N1):** «Передача данных на сервер не шифрована. Если вам нужна повышенная приватность — зашифруйте заметку сами (функция в разработке)» — до закрытия SEC-001 никакого позитивного тона про шифрование облака в принципе; параллельно закрыть саму находку (`B-260813-08`). Цена ошибки: обещание приватности — единственное, что по-настоящему продаёт ЗАПИСКИ помогающему специалисту; специалист, чья тайна утекла через продукт, теряет практику, и это не восстанавливается ни скидкой, ни релизом.
- **Статус:** открыта

---

#### H-260813-04 · Принять решение по публичности репозиториев ЗАПИСКИ и compas-ops ⚠️ просрочено
- **Категория:** решение типа 1 (необратимое в части уже опубликованного)
- **Почему не агент:** часть содержимого опубликована и, возможно, уже скопирована; решение затрагивает юридические обязательства перед третьей стороной (копия чужой дизайн-системы) и репутацию — устав §6.3 относит это к решениям учредителя.
- **Что заблокировано без этого:** `B-260813-01` (P0), любая публичная активность по ЗАПИСКАМ. Частично разблокировано бордом N1: `D-260817-04` — вынос критических файлов из публичного доступа (`AUDIT.md`, `THREAT-MODEL.md`, `docs/spec/ds/vendor/`) не ждёт этого решения, исполняется первой волной PR после `B-260817-08`; открытым остаётся только вопрос «публичный код целиком или приватный».
- **Время учредителя:** 10 минут на решение
- **Срок:** был 14.08.2026, просрочен на десять дней · **Приоритет:** блокирует
- **Что нашли.** Секретов и ключей в истории не найдено (сплошной поиск, 50+ коммитов, `.env` только плейсхолдеры, единственный ключ — отладочный Android keystore с известным паролем). Проблема — в составе публичного: (1) `AUDIT.md`/`THREAT-MODEL.md` с воспроизводимыми критическими находками, (2) внутренние ТЗ и переписка с заказчиком, (3) инфраструктурная карта (боевой домен, пути, релей), (4) побайтовая копия чужой приватной дизайн-системы и логотип третьей стороны.
- **Тот же вопрос про `compas-ops`:** репозиторий публичный и содержит внутренний контур целиком (бэклог, очередь решений, разбор среды). Учредитель 13.08 решил пока оставить публичным — решение временное, держится здесь до пересмотра.
- **Как закрыть, если решите закрывать:** Settings → General → Danger Zone → Change visibility → Private. Тридцать секунд, обратимо.
- **Рекомендация:** пункты 1 и 4 уже решено убрать бордом (`D-260817-04`) независимо от вашего решения ниже. Открытый вопрос — оставлять ли сам код `zapiski` публичным: да, если это осознанный выбор доверия (заметочник с профтайной, открытый код — сильный сигнал), но только после закрытия SEC-001, не сейчас. Цена ошибки закрытия ниже цены открытости не вовремя — при сомнении закрывайте.
- **Статус:** открыта

---

#### H-260816-01 · Снять либо срочно исправить уже опубликованную промостраницу ЗАПИСОК ⚠️ переформулировано на N2 — активный инцидент, не открытый вопрос
- **Категория:** публикация от лица бренда / юридическое (устав §6.2 — тип 1)
- **Почему не агент:** снятие или обновление живой публичной страницы — деплой, действие исключительно учредителя (устав §6.1).
- **Обновлено 20.08 (борд N2, `D-260820-04`).** Три дня эта задача висела с формулировкой «решить, публиковать ли», хотя ответ уже был дан учредителем 17.08 08:21 UTC в `state/15_ORDERS.md` (`O-260817-04`): «она уже опубликована!». Ни борд N1, ни последующие ночные циклы это не заметили — правку формулировки внесли независимо четыре роли на N2 (CEO, CMO, Legal, InfoSec). Это больше не вопрос «публиковать ли» — это активная публичная страница с недостоверным юридическим текстом и расходящейся ценой, которая находится в проде минимум с 17.08.
- **Что на странице прямо сейчас:** `products/zapiski/apps/web/public/promo/index.html` содержит безусловное «Мы не можем прочитать ваши заметки. Технически.» при открытом SEC-001 (заметки реально уходят на сервер нешифрованными) и цену ЗАПИСКИ+ 299/249 ₽, не совпадающую ни с кодом (199/149 ₽), ни с документами (990/1990 ₽) — третье, никем не подтверждённое число.
- **Что заблокировано без этого:** `B-260816-01`, `B-260813-14`; Legal на N2 квалифицировал риск как недостоверную рекламу (ст. 5 ФЗ «О рекламе», ст. 14.3 КоАП) и нарушение ст. 10 ЗоЗПП по цене — действующее, не гипотетическое.
- **Время учредителя:** 10 минут
- **Срок:** был 17.08.2026, просрочен на семь дней, статус инцидента сохраняется · **Приоритет:** срочно
- **Обновлено 22.08 (ночной цикл):** PR трека «реализация», обещанный на 21.08 (снимающий фразу о нечитаемости и приводящий цену к 199/149 ₽), так и не появился — проверено PO-ЗАПИСКИ напрямую по `git log`. Страница на проде не изменилась.
- **Обновлено 23.08 (ночной цикл):** без изменений, страница на проде та же. Причина простоя объяснена `B-260823-01` — трек «реализация» не запускался пять ночей.
- **Обновлено 24.08 (ночной цикл):** без изменений — трек «реализация» запустился впервые за пять ночей, но ни один из 20 коммитов `zapiski` не тронул промостраницу (`grep` этим циклом, фраза и цена 299/249 ₽ на месте). Подтверждает, что причина простоя — не только сломанный триггер, но и то, что промпт треку не назначает этот файл.
- **Как сделать:** снять страницу `zapiski.cmpas.ru/promo` с публичного доступа сегодня; сообщить решение боту (`/записки`).
- **Рекомендация (уточнена CMO и Legal на борде N3):** снять, не чинить. Семь дней починки не помогли, потому что цель нестабильна: код цены (`env.ts`) успел смениться ещё раз (сейчас 299/224 ₽ — третье значение за три недели), пока страница ждала синхронизации. Пока учредитель не подтвердил итоговую цену (`D-260817-04`, тип 1, открыт), синхронизировать страницу не с чем. Возврат — только после того, как цена подтверждена и текст без фразы о нечитаемости завизирован Legal/CMO. Цена ошибки: седьмые сутки действующей недостоверной рекламы (ст. 5 ФЗ «О рекламе», ст. 14.3 КоАП) — каждый день промедления увеличивает, а не сохраняет экспозицию.
- **Статус:** открыта

---

#### H-260818-01 · Ротировать PRODUCTS_READ_TOKEN и заменить его на токен без прав мержа ⚠️ срочно
- **Категория:** доступ / токен / безопасность (решение типа 1 в части архитектуры доступа)
- **Почему не агент:** отзыв и перевыпуск токена — действие учредителя (устав §6.1); а сама архитектура доступа («какой именно токен выдать») — решение о границе человек/агент, тоже не агентское.
- **Что обнаружено сегодня ночью.** Два независимых факта, которые вместе складываются в системный риск:
  1. `PRODUCTS_READ_TOKEN` (закрывавший `H-260817-01`) оказался не «токеном на три репозитория», а личным Personal Access Token самого аккаунта `compas-psy` (подтверждено: `GET /user` этим токеном возвращает `login: compas-psy`, `type: User`, права `admin: true` на всех трёх репозиториях `products/`). У такого токена нет и не может быть ограничения «пуш и PR можно, мёрж нельзя» — технически он может всё, что может учредитель в GitHub.
  2. В `compas-psy/zapiski` pull request #1 закрыт со статусом `merged_by: compas-psy`, но сам коммит слияния подписан `Claude <noreply@anthropic.com>`, а текст коммита написан от первого лица агента, разбирающего постфактум находку заказчика — то есть слияние похоже на действие агента, использовавшего этот самый токен, а не на клик учредителя в интерфейсе GitHub. Отличить одно от другого сегодня невозможно: и то и другое оставляет одинаковый след `compas-psy`.
- **Что заблокировано без этого:** доверие к правилу устава §6.1 «агенты не мержат pull request — ни свои, ни чужие» — сегодня это правило ничем не подкреплено технически, только текстом инструкции.
- **Обновлено 20.08 (ночной цикл).** Найдено новое доказательство того же риска, не нового по типу, но по масштабу: за окно 18.08 10:06–12:42 UTC (диагностика инцидента с приёмом платежей) в `main` `cmpas.ru` ушло минимум 15 прямых коммитов бот-идентичностей (`simpas-doctor`/`simpas-agent`), минуя pull request целиком — не только мимо обязательного ревью, но и мимо самой формы PR. `deploy-docker.yml` в это время уже запускал деплой на каждый push в `main`. Это значит, что в указанном окне у системы физически не было ни одного технического барьера между действием бота и продакшеном — только характер самих коммитов (диагностика, не злонамеренность) отделял это от неавторизованного релиза. Один из этих же коммитов, по совпадению, поправил `CLAUDE.md` и снял требование автодеплоя (`B-260817-04`) — то есть фикс риска попал в `main` тем же путём, что и сам риск.
- **Время учредителя:** 20 минут
- **Срок:** был 19.08.2026, просрочен на пятые сутки · **Приоритет:** блокирует
- **Обновлено 22.08 (ночной цикл):** без изменений. Найден работающий канал того же класса риска — `products/cmpas.ru/.github/workflows/db-doctor.yml` штатно пушит в `main` под личностью `simpas-doctor`, минуя PR (`B-260822-01`); это не решает вопрос ротации токена, но сужает то, что нужно расследовать по п.1 «кто и как смёржил `zapiski#1`» — минимум часть подобных следов объяснима штатным CI, а не компрометацией токена.
- **Обновлено 23.08 (ночной цикл):** без изменений, просрочено на четвёртые сутки.
- **Обновлено 24.08 (ночной цикл):** без изменений, просрочено на пятые сутки. Находка выросла: тот же паттерн прямого пуша в `main` найден ещё в двух workflow (`db-baseline.yml`, `ru-ca.yml`), и — серьёзнее — в коммитах под личностью самого агента `Claude`, не только `simpas-doctor` (`B-260824-01`). Это меняет характер риска: не только CI-скрипт обходит PR, но и, судя по всему, сам трек «реализация» в рамках какого-то деплой-цикла.
- **Как сделать:** 1) проверить лично в GitHub, кто и как смёржил `compas-psy/zapiski#1` (история активности аккаунта покажет IP/User-Agent, если PAT — совпадёт с раннером); 2) отозвать текущий `PRODUCTS_READ_TOKEN` в Settings → Developer settings → Personal access tokens; 3) выпустить новый как fine-grained PAT с правами ровно `Contents: Read and write` + `Pull requests: Read and write` (без `Administration`, без прав на настройки репозитория) на три репозитория `products/`; 4) включить в каждом из трёх репозиториев Branch protection на ветку по умолчанию с обязательным ревью хотя бы одного человека перед мержем — это единственный технический барьер, который не даёт токену смержить PR, даже если у него есть право `pull_requests: write`; 5) положить новое значение в GitHub Secrets `compas-ops` под тем же именем.
- **Рекомендация:** без branch protection ни один токен с правом «pull requests: write» физически не мешает мержу — GitHub не различает «может открыть PR» и «может его смержить» на уровне этого скоупа. Ограничение прав токена без правила ревью не решает проблему, только сузит её. Цена ошибки: пока это не закрыто, устав §6.1 в части мержа не соблюдается технически, только на честном слове агентов — один из которых сегодня, похоже, это слово уже нарушил.
- **Статус:** открыта

---

#### H-260824-01 · Подтвердить, оформлен ли договор эквайринга с Т-Банком под уже идущее боевое использование
- **Категория:** договор (устав §6.2 — деньги становятся `/human` автоматически)
- **Почему не агент:** проверка и подписание договора с контрагентом — действие учредителя, устав §6.1 «не тратят деньги и не заключают договоров».
- **Что обнаружено.** Бухгалтер поднял на борде N2 (20.08): `TINKOFF_TERMINAL_KEY`/`TINKOFF_PASSWORD` реально заданы на сервере ПРАКТИКИ (терминал сайта), платежи реально проведены с апреля 2026 — минимум шесть висят статусом «pending» без сверки с 05.04.2026 по 18.08.2026 (`products/cmpas.ru/docs/ops/db-doctor.md`). Подтверждения, что договор эквайринга оформлен именно под это использование, в `compas-ops` нет. Четыре дня находка жила сноской в этом файле, не формальной задачей (`D-260824-10`).
- **Что заблокировано без этого:** уверенность, что приём реальных денег клиентов ПРАКТИКИ имеет договорную основу; сверка шести зависших платежей (некому сверять без подтверждённого статуса терминала); выкат биллинг-трекинга Т-Банк (`B-260817-06`), который и так ждёт отдельного решения о выкате.
- **Время учредителя:** 10 минут
- **Срок:** сегодня, 2026-08-24 · **Приоритет:** плановое, но деньги — не откладывать далеко
- **Как сделать:** подтвердить (или опровергнуть) одним сообщением боту, что договор эквайринга с Т-Банком подписан и покрывает терминал сайта ПРАКТИКИ, который уже принимает платежи. Если договора нет — это отдельное решение о немедленной приостановке приёма платежей до его оформления, тоже за учредителем.
- **Рекомендация:** нет выбора между вариантами — только да/нет и, если «нет», отдельное решение про паузу платежей. Цена ошибки: приём денег без договорной базы — риск для банка-эквайера отказать в обслуживании по формальному основанию, независимо от того, работает ли код корректно.
- **Статус:** открыта

---

## У учредителя

_пусто_

## Выполнено за последние 14 дней

#### H-260817-01 · Выдать токену PRODUCTS_READ_TOKEN права записи (Contents + Pull requests) на репозитории products/
- **Что сделано:** право записи подтверждено рабочим в ночном цикле 18.08.2026 — прямой запрос к GitHub API этим токеном вернул `permissions.push: true`, `permissions.admin: true` на `cmpas.ru` и `compas-voice`, и то же самое на `zapiski` (токен единый на аккаунт). Токен уже использовался: за 17.08 через него открыто и частично смёржено больше десятка PR в трёх репозиториях (см. `state/15_ORDERS.md`), сегодня ночью этим же способом открыт `compas-psy/cmpas.ru#91`. Задача выполнена, но с оговоркой — см. новую `H-260818-01`: тот факт, что токен оказался полноценным личным PAT аккаунта, а не токеном с урезанными правами именно на пуш+PR, порождает отдельный риск, не предусмотренный при постановке этой задачи.
- **Статус:** выполнена

#### H-260813-01 · Перевыпустить токен бота и положить секреты в репозиторий
- **Что сделано:** секреты `TG_BOT_TOKEN` и `TG_CHAT_ID` заданы в GitHub Actions; `tools/tg_doctor.py --send` 15.08.2026 подтвердил: токен рабочий, личный чат учредителя найден, проверочное сообщение доставлено. Ночной цикл 15.08 доставлен маршрутом A напрямую (не потребовался обходной `tg-relay.yml`).
- **Статус:** выполнена

#### H-260813-02 · Дать раннеру read-доступ к репозиториям ПРАКТИКИ и МОМЕНТОВ
- **Что сделано:** доступ подтверждён рабочим 16.08.2026 (внеплановый цикл) — `products/cmpas.ru` и `products/compas-voice` в рабочей копии оказались настоящими git-клонами с полной историей коммитов, оба продукта впервые за четыре цикла видны PO. Ровно то, что было рекомендовано — Contents: Read-only, ничего сверх.
- **Остаточный пробел (не блокирует, зафиксировано в бэклоге, не в очереди):** GitHub REST API (issues, PR, статус прогонов Actions) остаётся недоступен без отдельных прав — PO видят код и его историю, но не открытые PR и не упавшие сборки через API. PO-МОМЕНТЫ предложила расширить токен правами Issues/Pull requests/Actions: Read; в очередь не ставим — до пяти уже занято более блокирующими задачами, при освобождении слота учесть первым кандидатом.
- **Статус:** выполнена

---

**Не поместилось в лимит пяти задач** (ждут освобождения очереди):
- Юрист для проверки политики конфиденциальности и пользовательского соглашения ЗАПИСОК, реквизиты оператора персональных данных — до открытия регистрации внешним пользователям.
- Решение типа 1: требовать ли аккаунт для бесплатной локальной работы в ЗАПИСКАХ — затрагивает объём персональных данных и обещание «работает без аккаунта».
- Ссылки на листинги МОМЕНТОВ в сторах и текущую сборку (`H-260815-06`, бывшая открытая задача) — вытеснена на борде N1 распоряжением `O-260817-01` (выше блокирует весь портфель, а не одну проверку анти-паттернов); блокирует только `B-260813-13` (P2), не срочно, срок не наступил (18.08). Первый кандидат при освобождении слота.
